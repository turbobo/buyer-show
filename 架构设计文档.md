# 买家说（Buyer Show）架构设计文档

> 版本：v2.1 | 日期：2026-08-24 | 状态：方案设计
> 设计原则：全栈主流化、前后端分离、多端同构、容器化部署

---

## 一、项目概述

### 1.1 产品定位

「买家说」是一个购物种草社区应用，用户分享真实购物体验、发布商品评价、发现好物推荐。面向 18-35 岁网购用户，核心价值是**真实用户评价 + 社区发现**。

### 1.2 改造目标

将现有 Next.js + Supabase 单体应用改造为**前后端分离、多端适配、容器化部署**的现代化架构：

| 目标 | 说明 |
|------|------|
| 前后端分离 | API 独立服务，前端通过 HTTP 消费，不直接访问数据库 |
| 多端支持 | Web（PC + H5）、微信小程序、支付宝小程序、App（iOS/Android） |
| 容器化部署 | Docker + Docker Compose（开发）、Kubernetes（生产） |
| 独立扩展 | 前端和后端可独立扩缩容，互不影响 |
| 技术主流化 | 每一层都采用业界验证过的主流方案 |

### 1.3 技术栈总览

| 层级 | 技术方案 | 选型理由 |
|------|---------|---------|
| **Web 前端** | React 18 + Vite 5 + TypeScript 5 + Tailwind CSS 3 | 全球最主流前端框架，生态最完善 |
| **跨端移动** | uniapp X（DCloud）+ Vue 3 + TypeScript | 国内最主流跨端方案，一套代码出 H5 + 微信/支付宝/抖音小程序 + App |
| **API 服务** | Java 21 + Spring Boot 3.3 + MyBatis-Plus | 企业级最主流后端框架，招聘容易、生态成熟 |
| **数据库** | MySQL 8.0（主库） + Redis 7（缓存） | 最主流关系型数据库 + 最主流缓存 |
| **搜索引擎** | Elasticsearch 8 | 全文搜索行业标准 |
| **消息队列** | RabbitMQ 3.13 | 最主流消息中间件，社区活跃 |
| **对象存储** | MinIO（自托管）/ 阿里云 OSS / AWS S3 | S3 兼容协议，业界标准 |
| **API 网关** | Nginx 1.25 + Spring Cloud Gateway | 反向代理 + API 路由 + 限流 |
| **认证** | Spring Security + JWT（双 token）+ OAuth 2.0 | Java 生态标准安全框架 |
| **容器** | Docker 24 + Docker Compose v2 | 容器行业标准 |
| **编排** | Kubernetes 1.28 + Helm 3 | 容器编排行业标准 |
| **CI/CD** | GitHub Actions + ArgoCD | CI 自动化 + GitOps 持续部署 |
| **可观测标准** | OpenTelemetry + W3C Trace Context | 统一 Trace / Metrics / Logs 语义与上下文传播 |
| **采集网关** | Grafana Alloy（兼容 OTel Collector） | K8s 日志采集、OTLP 转发、过滤、采样与缓冲 |
| **可观测后端** | Prometheus + Loki + Tempo + Pyroscope + Grafana | 指标、日志、链路、持续剖析与统一查询 |

### 1.4 技术栈对比（为什么不用其他方案）

| 选型 | 淘汰方案 | 淘汰原因 |
|------|---------|---------|
| Spring Boot 3 | NestJS / Hono / Express | Node.js 后端在企业级项目中招聘难、大型项目维护成本高 |
| MySQL 8.0 | PostgreSQL | MySQL 在国内运维人才储备更多，云服务支持更好 |
| uniapp X | Taro / React Native / Flutter | 国内小程序覆盖率最高（微信+支付宝+抖音+百度+快手），一套 Vue 代码 |
| RabbitMQ | Kafka / RocketMQ | 社区应用体量不需要 Kafka 级吞吐量，RabbitMQ 更轻量成熟 |
| Elasticsearch | MeiliSearch / Typesense | ES 是搜索行业事实标准，生产验证最充分 |
| MyBatis-Plus | JPA/Hibernate / Drizzle / Prisma | 国内 Java 项目使用率最高，SQL 可控性好 |

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         客户端层                                  │
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │  Web PC  │  │  Web H5  │  │ 小程序   │  │  App (可选)   │  │
│  │  React   │  │  React   │  │ uniapp X │  │  uniapp X     │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬────────┘  │
│       └──────────────┴─────────────┴───────────────┘            │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTPS
                       ┌───────▼───────┐
                       │   Nginx / LB  │  SSL 终结 + 静态资源 + 反向代理
                       └───┬───────┬───┘
                           │       │
              ┌────────────┘       └────────────┐
              ▼                                  ▼
    ┌─────────────────┐               ┌─────────────────┐
    │  Web 静态资源    │               │  API Gateway    │
    │  (Nginx 托管)   │               │ (Spring Cloud   │
    │  React SPA     │               │  Gateway)       │
    └─────────────────┘               └────────┬────────┘
                                               │
                                    ┌──────────▼──────────┐
                                    │   API Server        │
                                    │   Spring Boot 3     │
                                    │   (多实例, 无状态)   │
                                    └──┬──────┬──────┬────┘
                                       │      │      │
                          ┌────────────┘  ┌───┘  ┌───┴───────────┐
                          ▼               ▼      ▼               ▼
                    ┌──────────┐  ┌──────────┐ ┌──────────┐ ┌────────┐
                    │ MySQL 8  │  │ Redis 7  │ │ ES 8     │ │ MinIO  │
                    │ (主从)   │  │ (集群)   │ │ (搜索)   │ │ / OSS  │
                    └──────────┘  └──────────┘ └──────────┘ └────────┘
                          │
                    ┌─────▼──────┐
                    │ RabbitMQ   │  异步任务：图片处理、通知推送、数据统计
                    └────────────┘
```

### 2.2 分层职责

| 层 | 职责 | 约束 |
|----|------|------|
| **客户端层** | UI 渲染、用户交互、状态管理、表单校验 | 禁止直接访问数据库和中间件 |
| **网关层** | SSL 终结、静态资源、请求路由、限流、跨域 | 不做业务逻辑 |
| **API 层** | 业务逻辑、数据校验、权限控制、数据聚合 | 无状态，可水平扩展 |
| **数据层** | 持久化、缓存、搜索索引、文件存储、消息队列 | 仅通过 API 层访问 |
| **基础设施层** | 容器编排、服务发现、配置管理、监控告警 | 声明式配置，GitOps |

### 2.3 请求流转

```
用户操作 → 客户端 → HTTPS → Nginx
  ├── 静态资源 → 直接返回 HTML/JS/CSS/图片
  └── /api/* → Spring Cloud Gateway
        ├── 限流校验（令牌桶算法）
        ├── JWT 认证（Spring Security Filter）
        ├── 请求路由 → API Server
        │     ├── Controller：参数校验（JSR 380 + 自定义 Validator）
        │     ├── Service：业务逻辑编排
        │     ├── Mapper（MyBatis-Plus）：数据库 CRUD
        │     ├── RedisTemplate：缓存读写
        │     ├── ElasticsearchClient：搜索查询
        │     ├── RabbitTemplate：异步消息发送
        │     └── MinIO Client：文件上传下载
        └── 统一响应格式化 → JSON
```

---

## 三、后端架构（Spring Boot 3）

### 3.1 项目结构

```
buyer-show-api/
├── pom.xml                         # Maven 配置
├── Dockerfile
├── src/main/java/com/buyershow/
│   ├── BuyerShowApplication.java   # 启动类
│   ├── config/                     # 配置类
│   │   ├── SecurityConfig.java     # Spring Security + JWT
│   │   ├── RedisConfig.java        # Redis 连接池
│   │   ├── ElasticsearchConfig.java
│   │   ├── RabbitMQConfig.java
│   │   ├── MinIOConfig.java
│   │   ├── WebMvcConfig.java       # CORS、拦截器
│   │   └── SwaggerConfig.java      # OpenAPI 3 文档
│   │
│   ├── controller/                 # REST 控制器（薄层，仅做参数校验+调用 Service）
│   │   ├── AuthController.java
│   │   ├── UserController.java
│   │   ├── PostController.java
│   │   ├── CommentController.java
│   │   ├── LikeController.java
│   │   ├── FavoriteController.java
│   │   ├── FollowController.java
│   │   ├── TagController.java
│   │   ├── SearchController.java
│   │   ├── UploadController.java
│   │   └── AdminController.java
│   │
│   ├── service/                    # 业务逻辑层
│   │   ├── AuthService.java
│   │   ├── UserService.java
│   │   ├── PostService.java
│   │   ├── CommentService.java
│   │   ├── InteractionService.java  # 点赞/收藏/关注统一管理
│   │   ├── SearchService.java
│   │   ├── UploadService.java
│   │   ├── FeedService.java         # Feed 聚合（Redis + ES + DB）
│   │   └── AdminService.java
│   │
│   ├── mapper/                     # MyBatis-Plus Mapper 接口
│   │   ├── UserMapper.java
│   │   ├── PostMapper.java
│   │   ├── CommentMapper.java
│   │   ├── LikeMapper.java
│   │   ├── FavoriteMapper.java
│   │   ├── FollowMapper.java
│   │   ├── TagMapper.java
│   │   └── SearchHistoryMapper.java
│   │
│   ├── entity/                     # 数据库实体（对应表结构）
│   │   ├── User.java
│   │   ├── Post.java
│   │   ├── Comment.java
│   │   ├── Like.java
│   │   ├── Favorite.java
│   │   ├── Follow.java
│   │   ├── Tag.java
│   │   └── SearchHistory.java
│   │
│   ├── dto/                        # 数据传输对象
│   │   ├── request/                # 请求 DTO（含 JSR 380 校验注解）
│   │   │   ├── LoginRequest.java
│   │   │   ├── RegisterRequest.java
│   │   │   ├── CreatePostRequest.java
│   │   │   ├── UpdateProfileRequest.java
│   │   │   └── ...
│   │   └── response/               # 响应 DTO
│   │       ├── ApiResponse.java    # 统一响应包装
│   │       ├── PageResponse.java   # 分页响应
│   │       ├── UserDTO.java
│   │       ├── PostCardDTO.java    # Feed 列表精简字段
│   │       ├── PostDetailDTO.java  # 详情完整字段
│   │       └── ...
│   │
│   ├── vo/                         # 视图对象（复杂聚合查询结果）
│   │   ├── FeedItemVO.java
│   │   └── UserProfileVO.java
│   │
│   ├── common/                     # 公共模块
│   │   ├── result/
│   │   │   ├── R.java              # 统一响应 Result<T>
│   │   │   └── ErrorCode.java      # 错误码枚举
│   │   ├── exception/
│   │   │   ├── BusinessException.java
│   │   │   ├── AuthException.java
│   │   │   └── GlobalExceptionHandler.java  # @RestControllerAdvice
│   │   ├── security/
│   │   │   ├── JwtTokenProvider.java
│   │   │   ├── JwtAuthFilter.java
│   │   │   └── UserDetailsServiceImpl.java
│   │   ├── annotation/
│   │   │   ├── RequireLogin.java   # 自定义注解：需登录
│   │   │   └── RequireAdmin.java   # 自定义注解：需管理员
│   │   └── util/
│   │       ├── CursorUtils.java    # 游标分页工具
│   │       ├── RedisUtils.java
│   │       └── ImageUtils.java
│   │
│   ├── consumer/                   # RabbitMQ 消息消费者
│   │   ├── ImageProcessConsumer.java   # 图片压缩/WebP 转换
│   │   ├── NotificationConsumer.java   # 通知推送
│   │   └── StatsConsumer.java          # 统计数据异步更新
│   │
│   └── job/                        # 定时任务
│       ├── HotTagJob.java          # 热门标签定时刷新
│       └── FeedCacheJob.java       # Feed 预热缓存
│
├── src/main/resources/
│   ├── application.yml             # 主配置
│   ├── application-dev.yml         # 开发环境
│   ├── application-prod.yml        # 生产环境
│   ├── mapper/                     # MyBatis XML（复杂 SQL）
│   │   ├── PostMapper.xml
│   │   └── FeedMapper.xml
│   └── db/migration/              # Flyway 数据库迁移
│       ├── V1__init_schema.sql
│       ├── V2__add_indexes.sql
│       └── V3__add_fulltext.sql
│
└── src/test/java/                  # 单元测试 + 集成测试
```

### 3.2 统一响应格式

```java
// R.java - 统一响应包装
public class R<T> {
    private int code;       // 0=成功，1xxx=业务错误
    private String message;
    private T data;

    public static <T> R<T> ok(T data) { ... }
    public static <T> R<T> fail(int code, String message) { ... }
    public static <T> R<T> fail(ErrorCode error) { ... }
}

// PageResponse.java - 分页响应
public class PageResponse<T> {
    private List<T> list;
    private String cursor;      // 游标（下一页起点）
    private boolean hasMore;
    private long total;         // 可选
}
```

### 3.3 错误码规范

```java
public enum ErrorCode {
    // 认证 1000-1999
    AUTH_FAILED(1001, "用户名或密码错误"),
    TOKEN_EXPIRED(1002, "登录已过期，请重新登录"),
    TOKEN_INVALID(1003, "无效的认证信息"),
    NO_PERMISSION(1004, "无权限执行此操作"),
    ACCOUNT_BANNED(1005, "账号已被封禁"),

    // 用户 2000-2999
    USER_NOT_FOUND(2001, "用户不存在"),
    USERNAME_EXISTS(2002, "用户名已被注册"),
    NICKNAME_EXISTS(2003, "昵称已被使用"),

    // 帖子 3000-3999
    POST_NOT_FOUND(3001, "帖子不存在或已删除"),
    POST_NO_EDIT(3002, "只能编辑自己的帖子"),
    POST_IMAGE_EMPTY(3003, "至少需要一张图片"),

    // 评论 4000-4999
    COMMENT_NOT_FOUND(4001, "评论不存在"),
    COMMENT_NO_DELETE(4002, "只能删除自己的评论"),

    // 上传 5000-5999
    FILE_TOO_LARGE(5001, "文件大小不能超过 10MB"),
    FILE_TYPE_INVALID(5002, "仅支持 jpg/png/webp 格式"),

    // 系统 9000-9999
    SYSTEM_ERROR(9001, "服务暂时不可用，请稍后重试"),
    RATE_LIMITED(9002, "请求过于频繁，请稍后再试");
}
```

### 3.4 认证机制（Spring Security + JWT）

```
注册流程：
  客户端 → POST /api/v1/auth/register { username, password, nickname }
  服务端 → JSR 380 校验 → BCrypt 哈希密码 → 写入 users 表 → 签发 TokenPair

登录流程：
  客户端 → POST /api/v1/auth/login { username, password }
  服务端 → UserDetailsService 查找 → BCrypt 比对 → 签发 TokenPair

Token 设计：
  access_token:  JWT (HS256), 30 分钟过期
                 payload = { sub: userId, role: "USER", iat, exp }
  refresh_token: UUID 随机串, 7 天过期, 存 Redis Hash
                 key = "refresh:{userId}", value = tokenValue

安全过滤器链：
  HttpSecurity
    .addFilterBefore(JwtAuthFilter, UsernamePasswordAuthenticationFilter)
    .authorizeHttpRequests(auth -> auth
        .requestMatchers("/api/v1/auth/**").permitAll()
        .requestMatchers("/api/v1/posts", "/api/v1/posts/**").permitAll()  // 浏览免登录
        .requestMatchers("/api/v1/admin/**").hasRole("ADMIN")
        .anyRequest().authenticated()
    )

微信小程序登录：
  客户端 → wx.login() 获取 code
  客户端 → POST /api/v1/auth/wechat { code }
  服务端 → 调用微信 code2session 接口 → 获取 openid
         → 查找 users 表（openid 关联）→ 不存在则自动注册
         → 签发 TokenPair
```

### 3.5 Feed 策略（Redis + DB 混合）

```
读取流程（拉模式 + 缓存）：
  1. 查 Redis 缓存：GET feed:cursor:{cursor}
  2. 命中 → 直接返回
  3. 未命中 → 查 MySQL（走索引 idx_posts_status_created）
  4. 写入 Redis 缓存（TTL 5 分钟）
  5. 返回结果

写入流程（异步更新计数）：
  1. 发布帖子 → 写入 MySQL posts 表
  2. 发送 RabbitMQ 消息：{ type: "POST_CREATED", postId, userId }
  3. Consumer 异步处理：
     - 更新 Elasticsearch 索引
     - 预热 Feed 缓存
     - 推送关注用户通知

搜索流程：
  1. 用户输入关键词 → 前端防抖 300ms
  2. GET /api/v1/search?q=关键词&cursor=xxx
  3. Service 调用 Elasticsearch bool query（title + content + tags）
  4. 高亮匹配结果，返回分页
```

---

## 四、数据库设计（MySQL 8.0）

### 4.1 ER 关系图

```
users ─────────┐
  │            │
  ├──< posts ──┤
  │     │      │
  │     ├──< comments ──┐
  │     │               │
  │     ├──< likes      │
  │     │               │
  │     └──< favorites  │
  │                     │
  ├──< follows ─────> users
  │
  ├──< favorite_comments ──> comments
  ├──< favorite_tags
  └──< search_history
```

### 4.2 核心表结构

#### users（用户表）

```sql
CREATE TABLE users (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    username        VARCHAR(50)  NOT NULL UNIQUE,
    email           VARCHAR(255) DEFAULT NULL UNIQUE,
    phone           VARCHAR(20)  DEFAULT NULL UNIQUE,
    password_hash   VARCHAR(255) NOT NULL,
    nickname        VARCHAR(50)  NOT NULL,
    avatar_url      VARCHAR(500) DEFAULT NULL,
    bio             VARCHAR(500) DEFAULT NULL,
    post_count      INT          NOT NULL DEFAULT 0,
    follower_count  INT          NOT NULL DEFAULT 0,
    following_count INT          NOT NULL DEFAULT 0,
    role            TINYINT      NOT NULL DEFAULT 0  COMMENT '0=普通用户 1=管理员',
    status          TINYINT      NOT NULL DEFAULT 0  COMMENT '0=正常 1=封禁 2=注销',
    openid          VARCHAR(100) DEFAULT NULL UNIQUE COMMENT '微信openid',
    last_login_at   DATETIME     DEFAULT NULL,
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_username (username),
    INDEX idx_status (status),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### posts（帖子表）

```sql
CREATE TABLE posts (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id         BIGINT UNSIGNED NOT NULL,
    title           VARCHAR(200) NOT NULL,
    content         TEXT         NOT NULL,
    images          JSON         NOT NULL COMMENT '图片URL数组',
    tags            JSON         DEFAULT NULL COMMENT '标签数组',
    product_name    VARCHAR(200) DEFAULT NULL,
    price           DECIMAL(10,2) DEFAULT NULL,
    rating          TINYINT      DEFAULT NULL COMMENT '评分1-5',
    like_count      INT          NOT NULL DEFAULT 0,
    comment_count   INT          NOT NULL DEFAULT 0,
    favorite_count  INT          NOT NULL DEFAULT 0,
    status          TINYINT      NOT NULL DEFAULT 0 COMMENT '0=公开 1=隐藏 2=删除',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_user_id (user_id, created_at DESC),
    INDEX idx_status_created (status, created_at DESC),
    INDEX idx_tags ((CAST(tags AS CHAR(200) ARRAY))),  -- MySQL 8 JSON索引
    FULLTEXT INDEX ft_title_content (title, content) WITH PARSER ngram,

    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### comments（评论表）

```sql
CREATE TABLE comments (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    post_id         BIGINT UNSIGNED NOT NULL,
    user_id         BIGINT UNSIGNED NOT NULL,
    parent_id       BIGINT UNSIGNED DEFAULT NULL COMMENT '父评论ID，NULL=顶级评论',
    content         TEXT         NOT NULL,
    reply_count     INT          NOT NULL DEFAULT 0,
    like_count      INT          NOT NULL DEFAULT 0,
    status          TINYINT      NOT NULL DEFAULT 0 COMMENT '0=正常 1=删除',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_post_created (post_id, created_at),
    INDEX idx_parent (parent_id),
    INDEX idx_user (user_id),

    FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (parent_id) REFERENCES comments(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### likes（点赞表）

```sql
CREATE TABLE likes (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id         BIGINT UNSIGNED NOT NULL,
    post_id         BIGINT UNSIGNED NOT NULL,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,

    UNIQUE KEY uk_user_post (user_id, post_id),
    INDEX idx_post (post_id),

    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### favorites / follows / favorite_tags / search_history

结构类似，均含 `UNIQUE KEY` 防重复 + 外键关联 + 时间戳。

#### tags（标签字典表）

```sql
CREATE TABLE tags (
    id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    name            VARCHAR(50) NOT NULL UNIQUE,
    post_count      INT         NOT NULL DEFAULT 0,
    status          TINYINT     NOT NULL DEFAULT 0 COMMENT '0=正常 1=禁用',
    created_at      DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,

    INDEX idx_status_count (status, post_count DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.3 数据库迁移（Flyway）

```
src/main/resources/db/migration/
├── V1__init_schema.sql          # 全部建表语句
├── V2__add_indexes.sql          # 补充索引
├── V3__add_fulltext_index.sql   # ngram 全文索引
├── V4__add_wechat_openid.sql    # 微信登录字段
└── ...                          # 后续增量迁移
```

Flyway 在 Spring Boot 启动时自动执行未跑过的迁移，版本号只增不减。

---

## 五、中间件设计

### 5.1 Redis 缓存策略

```
缓存用途：
  ├── Session：refresh_token 存储（Hash, TTL 7天）
  ├── Feed 缓存：feed:{tag}:{cursor} → 帖子列表 JSON（TTL 5分钟）
  ├── 热门数据：hot:tags → 热门标签（TTL 1小时, 定时刷新）
  ├── 计数缓存：count:post:{id}:likes → 点赞数（写时递增, 定时回写DB）
  ├── 限流：ratelimit:{ip|userId} → 令牌桶计数（TTL 1分钟）
  └── 验证码：sms:{phone} → 短信验证码（TTL 5分钟）

缓存策略：
  ├── 读：Cache-Aside（先查缓存 → 未命中查DB → 写缓存）
  ├── 写：Write-Through（先更新DB → 再删缓存）
  └── 热点：定时任务预热 + 逻辑过期（防缓存雪崩）

防穿透：
  ├── 空值缓存：查询不存在的 key 也缓存空值（TTL 30s）
  └── 布隆过滤器：用户 ID 合法性预判断
```

### 5.2 RabbitMQ 消息队列

```
交换机设计：
  ├── buyershow.topic（Topic Exchange）
  │     ├── post.created     → 帖子发布事件
  │     ├── post.liked       → 点赞事件
  │     ├── comment.created  → 评论事件
  │     └── user.followed    → 关注事件
  │
  └── buyershow.direct（Direct Exchange）
        ├── image.process    → 图片压缩任务
        └── notification.push → 通知推送任务

消费者：
  ├── ImageProcessConsumer：接收图片上传事件 → 压缩 → WebP 转换 → 上传 OSS
  ├── NotificationConsumer：接收互动事件 → 生成通知 → 推送到用户
  ├── StatsConsumer：接收计数变更 → 批量更新 posts.like_count 等冗余字段
  └── FeedCacheConsumer：接收帖子变更 → 刷新 Feed 缓存

可靠性保障：
  ├── 消息持久化（delivery_mode = 2）
  ├── 手动 ACK（处理成功才确认）
  ├── 死信队列（处理失败 3 次后进 DLX）
  └── 幂等消费（基于消息 ID 去重）
```

### 5.3 Elasticsearch 搜索

```
索引设计：
  PUT /buyer_posts
  {
    "settings": {
      "number_of_shards": 1,
      "analysis": {
        "analyzer": {
          "ik_smart_analyzer": { "type": "custom", "tokenizer": "ik_smart" }
        }
      }
    },
    "mappings": {
      "properties": {
        "id":         { "type": "long" },
        "user_id":    { "type": "long" },
        "title":      { "type": "text", "analyzer": "ik_smart_analyzer" },
        "content":    { "type": "text", "analyzer": "ik_smart_analyzer" },
        "tags":       { "type": "keyword" },
        "product_name": { "type": "text", "analyzer": "ik_smart_analyzer" },
        "rating":     { "type": "byte" },
        "like_count": { "type": "integer" },
        "status":     { "type": "byte" },
        "created_at": { "type": "date" }
      }
    }
  }

搜索查询：
  GET /buyer_posts/_search
  {
    "query": {
      "bool": {
        "must": [
          { "multi_match": { "query": "关键词", "fields": ["title^3", "content", "product_name"] } }
        ],
        "filter": [
          { "term": { "status": 0 } }
        ]
      }
    },
    "highlight": {
      "fields": { "title": {}, "content": { "fragment_size": 100 } }
    },
    "sort": [{ "_score": "desc" }, { "created_at": "desc" }],
    "search_after": [cursor_score, cursor_time],
    "size": 20
  }

数据同步（Canal / 双写）：
  帖子写入 MySQL → RabbitMQ 消息 → ES Sync Consumer → 更新 ES 索引
  延迟 < 1s，最终一致性
```

---

## 六、前端架构（React + Vite）

### 6.1 Web 端技术栈

| 维度 | 方案 | 说明 |
|------|------|------|
| 框架 | React 18 | 全球最主流，生态最大 |
| 构建 | Vite 5 | 极速 HMR，esbuild 预构建 |
| 路由 | React Router v6 | SPA 路由标准方案 |
| 状态管理 | Zustand + React Query | Zustand 管客户端态，React Query 管服务端态 |
| 请求 | axios | 拦截器、自动重试、取消请求 |
| 样式 | Tailwind CSS 3 | 原子化 CSS，响应式 |
| 动画 | Framer Motion 11 | 声明式动画 |
| 表单 | React Hook Form + Zod | 类型安全校验 |
| 图表 | ECharts（可选） | 后台管理数据可视化 |

### 6.2 跨端移动架构（uniapp X）

```
uniapp X 优势（vs Taro / React Native / Flutter）：
  ├── 一套 Vue 3 代码 → 微信/支付宝/抖音/百度/快手小程序 + H5 + App
  ├── 国内开发者最多，插件市场最丰富
  ├── 条件编译：#ifdef MP-WEIXIN / #ifdef H5 / #ifdef APP
  ├── 原生渲染引擎（App 端非 WebView）
  └── 与 Vue 生态完全兼容（Pinia, VueUse, etc.）

代码共享策略：
  ├── 100% 共享：types/, constants/, utils/, services/（API 层）
  ├── 80% 共享：pages/ 页面逻辑、store/ 状态管理
  ├── 60% 共享：components/（条件编译处理平台差异）
  └── 0% 共享：平台特有 API（wx.login, uni.chooseImage）

目录结构：
  apps/mini/
  ├── src/
  │   ├── pages/           # 页面（与 Web 对齐）
  │   │   ├── index/       # 首页 Feed
  │   │   ├── post-detail/ # 帖子详情
  │   │   ├── publish/     # 发布
  │   │   ├── search/      # 搜索
  │   │   ├── profile/     # 个人中心
  │   │   └── login/       # 登录
  │   ├── components/      # 跨端组件
  │   ├── services/        # uni.request 封装
  │   ├── store/           # Pinia 状态管理
  │   ├── utils/           # 工具函数（从 shared 包导入）
  │   └── static/          # 静态资源
  ├── pages.json           # uniapp 页面配置
  ├── manifest.json        # 小程序/App 配置
  └── package.json
```

---

## 七、API 路由设计

### 7.1 完整路由表

```
认证模块
  POST   /api/v1/auth/register          注册
  POST   /api/v1/auth/login             登录
  POST   /api/v1/auth/refresh           刷新 access_token
  POST   /api/v1/auth/logout            登出
  POST   /api/v1/auth/wechat            微信小程序登录
  POST   /api/v1/auth/sms/send          发送短信验证码
  POST   /api/v1/auth/sms/login         短信验证码登录

用户模块
  GET    /api/v1/users/me               当前用户信息
  PUT    /api/v1/users/me               更新个人资料
  POST   /api/v1/users/me/avatar        更新头像
  POST   /api/v1/users/me/password      修改密码
  DELETE /api/v1/users/me               注销账号（软删除）
  GET    /api/v1/users/:id              用户公开信息
  GET    /api/v1/users/:id/posts        用户帖子列表
  GET    /api/v1/users/:id/followers    粉丝列表
  GET    /api/v1/users/:id/following    关注列表

帖子模块
  GET    /api/v1/posts                  Feed 列表（游标分页, ?tag=&cursor=&size=20）
  GET    /api/v1/posts/:id              帖子详情
  POST   /api/v1/posts                  发布帖子
  PUT    /api/v1/posts/:id              编辑帖子
  DELETE /api/v1/posts/:id              删除帖子

互动模块
  POST   /api/v1/posts/:id/like         点赞
  DELETE /api/v1/posts/:id/like         取消点赞
  POST   /api/v1/posts/:id/favorite     收藏
  DELETE /api/v1/posts/:id/favorite     取消收藏
  POST   /api/v1/users/:id/follow       关注
  DELETE /api/v1/users/:id/follow       取消关注

评论模块
  GET    /api/v1/posts/:id/comments     评论列表（含 1 级嵌套）
  POST   /api/v1/posts/:id/comments     发表评论
  DELETE /api/v1/comments/:id           删除评论
  POST   /api/v1/comments/:id/like      评论点赞

标签模块
  GET    /api/v1/tags                   热门标签
  GET    /api/v1/tags/:name/posts       标签帖子列表
  POST   /api/v1/tags/:name/favorite    收藏标签
  DELETE /api/v1/tags/:name/favorite    取消收藏标签

搜索模块
  GET    /api/v1/search                 全文搜索（ES 驱动）
  GET    /api/v1/search/history         搜索历史
  DELETE /api/v1/search/history          清空搜索历史

上传模块
  POST   /api/v1/upload/image           单图上传（返回 URL）
  POST   /api/v1/upload/images          批量上传（最多 9 张）

管理模块
  GET    /api/v1/admin/users            用户管理列表
  PUT    /api/v1/admin/users/:id/ban    封禁
  PUT    /api/v1/admin/users/:id/unban  解封
  PUT    /api/v1/admin/users/:id/role   修改角色
  GET    /api/v1/admin/tags             标签管理列表
  POST   /api/v1/admin/tags             创建标签
  PUT    /api/v1/admin/tags/:id         编辑标签
  DELETE /api/v1/admin/tags/:id         删除标签
  GET    /api/v1/admin/stats            统计数据

系统
  GET    /api/v1/health                 健康检查
  GET    /api/v1/version                版本信息
```

---

## 八、Docker 与 Kubernetes

### 8.1 docker-compose.yml（本地开发）

```yaml
services:
  # ── 基础设施 ──
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpass}
      MYSQL_DATABASE: buyershow
      MYSQL_USER: app
      MYSQL_PASSWORD: ${DB_PASSWORD:-devpassword}
    ports: ["3306:3306"]
    volumes:
      - mysql_data:/var/lib/mysql
      - ./apps/api/src/main/resources/db/migration:/docker-entrypoint-initdb.d
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    ports: ["6379:6379"]
    volumes: ["redis_data:/data"]

  elasticsearch:
    image: elasticsearch:8.14.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports: ["9200:9200"]
    volumes: ["es_data:/usr/share/elasticsearch/data"]

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    ports:
      - "5672:5672"     # AMQP
      - "15672:15672"   # Management UI
    environment:
      RABBITMQ_DEFAULT_USER: app
      RABBITMQ_DEFAULT_PASS: ${RABBIT_PASSWORD:-devpassword}
    volumes: ["rabbitmq_data:/var/lib/rabbitmq"]

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes: ["minio_data:/data"]

  # ── 应用服务 ──
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    ports: ["8080:8080"]
    environment:
      SPRING_PROFILES_ACTIVE: dev
      DB_URL: jdbc:mysql://mysql:3306/buyershow?useUnicode=true&characterEncoding=utf8mb4
      DB_USERNAME: app
      DB_PASSWORD: ${DB_PASSWORD:-devpassword}
      REDIS_HOST: redis
      ES_HOST: elasticsearch
      RABBITMQ_HOST: rabbitmq
      MINIO_ENDPOINT: http://minio:9000
      JWT_SECRET: ${JWT_SECRET:-dev-jwt-secret-must-be-256-bits-long}
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_started }
      elasticsearch: { condition: service_started }
      rabbitmq: { condition: service_started }
      minio: { condition: service_started }

  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    ports: ["3000:80"]
    depends_on: ["api"]

volumes:
  mysql_data:
  redis_data:
  es_data:
  rabbitmq_data:
  minio_data:
```

### 8.2 API Dockerfile

```dockerfile
# apps/api/Dockerfile

# ── Build ──
FROM maven:3.9-eclipse-temurin-21 AS builder
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline -B
COPY src ./src
RUN mvn package -DskipTests -B

# ── Runtime ──
FROM eclipse-temurin:21-jre-alpine
WORKDIR /app

RUN addgroup -g 1001 -S spring && adduser -S app -u 1001
USER app

COPY --from=builder /app/target/*.jar app.jar

EXPOSE 8080
HEALTHCHECK --interval=30s --timeout=3s \
  CMD wget -qO- http://localhost:8080/api/v1/health || exit 1

ENTRYPOINT ["java", "-XX:+UseZGC", "-XX:MaxRAMPercentage=75.0", "-jar", "app.jar"]
```

### 8.3 K8s 部署清单

```yaml
# infra/k8s/api-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: buyershow-api
  namespace: buyershow
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxUnavailable: 1, maxSurge: 1 }
  selector:
    matchLabels: { app: buyershow-api }
  template:
    metadata:
      labels: { app: buyershow-api }
    spec:
      containers:
        - name: api
          image: registry.cn-hangzhou.aliyuncs.com/buyershow/api:${TAG}
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: buyershow-config }
            - secretRef: { name: buyershow-secrets }
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: 1000m, memory: 1Gi }
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 8080 }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 8080 }
            initialDelaySeconds: 60
            periodSeconds: 20
          startupProbe:
            httpGet: { path: /api/v1/health, port: 8080 }
            failureThreshold: 30
            periodSeconds: 10
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: buyershow-api
  namespace: buyershow
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: buyershow-api
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 65 }
    - type: Resource
      resource:
        name: memory
        target: { type: Utilization, averageUtilization: 75 }
```

---

## 九、CI/CD 流水线

### 9.1 GitHub Actions

```yaml
# .github/workflows/ci.yml
name: CI
on:
  pull_request:
    branches: [main, dev]
jobs:
  backend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: 21, distribution: temurin, cache: maven }
      - run: cd apps/api && mvn verify -B

  frontend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: pnpm }
      - run: cd apps/web && pnpm install --frozen-lockfile && pnpm lint && pnpm build

---
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  build-push-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build & Push API Image
        run: |
          docker build -t registry.cn-hangzhou.aliyuncs.com/buyershow/api:${{ github.sha }} apps/api/
          docker push registry.cn-hangzhou.aliyuncs.com/buyershow/api:${{ github.sha }}

      - name: Build & Push Web Image
        run: |
          docker build -t registry.cn-hangzhou.aliyuncs.com/buyershow/web:${{ github.sha }} apps/web/
          docker push registry.cn-hangzhou.aliyuncs.com/buyershow/web:${{ github.sha }}

      - name: Deploy to K8s (ArgoCD sync)
        run: |
          # 更新 Helm values 中的镜像 tag
          sed -i "s/tag:.*/tag: ${{ github.sha }}/" infra/helm/buyershow/values.yaml
          git add infra/helm/buyershow/values.yaml
          git commit -m "chore: bump image tag to ${{ github.sha }}"
          git push
          # ArgoCD 自动检测 Git 变更并同步
```

---

## 十、安全规范

| 维度 | 措施 |
|------|------|
| **传输安全** | 全站 HTTPS (Let's Encrypt), HSTS, TLS 1.3 |
| **认证** | Spring Security + JWT 双 token, BCrypt 密码哈希 |
| **授权** | RBAC 角色控制（USER / ADMIN）, 资源级权限检查 |
| **输入校验** | JSR 380 Bean Validation + 自定义 Validator, SQL 参数化（MyBatis-Plus 防注入） |
| **XSS 防护** | 输出转义, CSP 头, 禁止 innerHTML |
| **CSRF 防护** | SameSite Cookie + CSRF Token（Web 端） |
| **文件上传** | MIME 白名单 + 大小限制(10MB) + 病毒扫描 + 重命名存储 |
| **限流** | Gateway 层令牌桶（IP 60次/分钟, 用户 120次/分钟） |
| **敏感数据** | K8s Secret 注入, 日志脱敏, 禁止硬编码 |
| **审计日志** | 关键操作（登录/删除/封禁）记录审计日志 |

---

## 十一、监控、Trace 与日志管理

### 11.1 统一可观测性架构

采用 OpenTelemetry 作为厂商中立的遥测标准，以 Grafana Alloy（兼容 OpenTelemetry Collector）作为 Kubernetes 采集与网关层，统一关联 Metrics、Logs、Traces 和 Profiles。

```
Spring Boot Pod
├── OpenTelemetry Java Agent
│   ├── HTTP / JDBC / Redis / RabbitMQ 自动 Trace
│   ├── W3C traceparent / baggage 上下文传播
│   └── OTLP gRPC → Alloy Gateway :4317
├── Micrometer + Spring Boot Actuator
│   └── /actuator/prometheus → Prometheus
├── Logback JSON
│   ├── stdout（Kubernetes 主采集通道）
│   └── /var/log/buyer-show/application.json.log（短期本地缓冲）
└── Pyroscope Java Agent（P2，可选）
    └── CPU / Allocation / Lock 持续剖析

Kubernetes
├── Grafana Alloy DaemonSet
│   ├── 采集 /var/log/containers/*.log
│   ├── 解析 JSON、补充 k8s.* 标签、脱敏与限流
│   └── 写入 Loki
├── Grafana Alloy Gateway Deployment
│   ├── 接收 OTLP gRPC / HTTP
│   ├── memory_limiter + batch + tail_sampling
│   ├── Trace → Tempo
│   ├── Metrics → Prometheus / Mimir
│   └── Logs → Loki
└── Grafana
    ├── PromQL 查询 Metrics
    ├── LogQL 查询 Logs
    ├── TraceQL 查询 Traces
    ├── Profiles 查询 Pyroscope
    └── Trace ↔ Log ↔ Metrics ↔ Profile 双向跳转
```

**技术职责**：

| 信号 | 应用侧 | 采集/网关 | 存储与查询 | 用途 |
|------|-------|----------|-----------|------|
| Metrics | Micrometer + Actuator | Prometheus / Alloy | Prometheus（规模化后 Mimir） | RED/USE、SLO、业务指标 |
| Logs | Logback JSON + MDC | Alloy DaemonSet | Loki | 错误排查、审计、Trace 反查 |
| Traces | OTel Java Agent | Alloy Gateway | Tempo | 跨服务、JDBC、Redis、MQ 调用链 |
| Profiles | Pyroscope Java Agent | Alloy | Pyroscope | CPU、内存分配、锁竞争定位 |

不再使用 Spring Cloud Sleuth；Spring Boot 3 使用 Micrometer Observation/Tracing 与 OpenTelemetry。Jaeger、SkyWalking 作为备选，不作为本项目默认主链路。

### 11.2 Spring Boot 自动插桩与资源标识

生产环境优先通过 OpenTelemetry Operator 自动注入 Java Agent，避免业务代码直接依赖厂商 SDK：

```yaml
apiVersion: opentelemetry.io/v1alpha1
kind: Instrumentation
metadata:
  name: buyer-show-java
  namespace: buyer-show
spec:
  exporter:
    endpoint: http://alloy-gateway.observability.svc:4317
  propagators: [tracecontext, baggage]
  sampler:
    type: parentbased_always_on
  java:
    env:
      - name: OTEL_METRICS_EXPORTER
        value: none
      - name: OTEL_LOGS_EXPORTER
        value: none
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: buyer-show-server
spec:
  template:
    metadata:
      annotations:
        instrumentation.opentelemetry.io/inject-java: buyer-show-java
```

Metrics 由 Micrometer/Prometheus 提供，日志由 stdout/Alloy 提供，因此 Java Agent 仅导出 Trace，避免同一信号重复采集。

统一资源属性：

```yaml
OTEL_SERVICE_NAME: buyer-show-server
OTEL_RESOURCE_ATTRIBUTES: >-
  service.namespace=buyer-show,
  service.version=${APP_VERSION},
  deployment.environment=${ENVIRONMENT},
  k8s.namespace.name=buyer-show
OTEL_EXPORTER_OTLP_ENDPOINT: http://alloy-gateway.observability.svc:4317
OTEL_EXPORTER_OTLP_PROTOCOL: grpc
```

链路必须遵循 W3C Trace Context。HTTP、异步线程、RabbitMQ Producer/Consumer 和定时任务均需传播 `traceparent`；跨线程任务使用 OTel context wrapper，禁止手动生成无父级 traceId。

### 11.3 结构化日志规范

日志统一输出单行 JSON；`trace_id`、`span_id`、`request_id` 由 MDC 自动注入，用于在 Grafana 中从日志一键跳转 Tempo Trace。

```json
{
  "timestamp": "2026-09-08T14:30:00.123+08:00",
  "level": "ERROR",
  "service": "buyer-show-server",
  "environment": "prod",
  "trace_id": "4bf92f3577b34da6a3ce929d0e0e4736",
  "span_id": "00f067aa0ba902b7",
  "request_id": "req_01J...",
  "logger": "com.buyershow.service.PostService",
  "thread": "http-nio-8080-exec-12",
  "event": "post_publish_failed",
  "user_id_hash": "sha256:...",
  "message": "帖子发布失败",
  "exception": "..."
}
```

**字段与标签规则**：

- Loki Label 仅使用低基数字段：`service_name`、`environment`、`namespace`、`pod`、`level`、`log_type`。
- `trace_id`、`span_id`、`request_id`、`user_id`、`post_id` 放在 JSON 字段中，禁止作为 Loki Label，防止标签爆炸。
- 禁止记录密码、JWT、Refresh Token、验证码、身份证、手机号、邮箱原文、Cookie、完整请求体。
- 用户 ID、手机号等定位字段必须哈希或脱敏；异常栈仅在 ERROR 日志中记录。
- 每个业务异常只记录一次；禁止 Controller、Service、全局异常处理器重复打印同一异常。
- 审计日志 `log_type=audit` 独立记录登录、账号注销、删帖、封禁、权限变更，并写明操作人、资源、结果和原因。

### 11.4 本地日志文件管理与滚动策略

Kubernetes 以 stdout 为权威采集源；本地日志文件只作为短期故障缓冲，不作为长期日志存储。Spring Boot 使用 Logback `SizeAndTimeBasedRollingPolicy`：

```xml
<property name="LOG_PATH" value="${LOG_PATH:-./logs}"/>
<property name="LOG_MAX_HISTORY" value="${LOG_FILE_MAX_HISTORY_DAYS:-7}"/>
<property name="LOG_TOTAL_SIZE_CAP" value="${LOG_FILE_TOTAL_SIZE_CAP:-2GB}"/>
<property name="LOG_MAX_FILE_SIZE" value="${LOG_FILE_MAX_SIZE:-100MB}"/>

<appender name="ROLLING_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
  <file>${LOG_PATH}/application.json.log</file>
  <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
    <fileNamePattern>${LOG_PATH}/archive/application.%d{yyyy-MM-dd}.%i.json.log.gz</fileNamePattern>
    <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
    <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
    <totalSizeCap>${LOG_TOTAL_SIZE_CAP}</totalSizeCap>
    <cleanHistoryOnStart>true</cleanHistoryOnStart>
  </rollingPolicy>
</appender>
```

| 环境 | 本地文件保留 | 单文件上限 | 总容量上限 | 说明 |
|------|-------------|-----------|-------------|------|
| local | 7 天 | 100 MB | 2 GB | 便于本机调试 |
| test | 3 天 | 100 MB | 2 GB | 集中日志已进入 Loki |
| staging | 3 天 | 100 MB | 5 GB | 仅作采集失败缓冲 |
| prod | 1 天 | 200 MB | 5 GB | Pod 本地文件非持久，不依赖其归档 |

Docker Compose 将 `./logs/server:/var/log/buyer-show` 作为可选 bind mount。Kubernetes 使用 `emptyDir` 作为短期缓冲；禁止为普通应用日志挂长期 PVC，长期保存统一交给 Loki 对象存储。

Kubernetes 节点侧同时配置容器日志轮转：

```yaml
# kubelet configuration
containerLogMaxSize: 100Mi
containerLogMaxFiles: 5
```

### 11.5 集中日志保存时长

日志、Trace、Metrics 的保存期限全部参数化，按环境和信号分层。默认策略：

| 数据类型 | local | test | staging | prod | 说明 |
|----------|-------|------|---------|------|------|
| INFO/WARN 应用日志 | 7 天 | 7 天 | 14 天 | 30 天 | 常规问题排查 |
| ERROR/FATAL 日志 | 7 天 | 14 天 | 30 天 | 90 天 | 故障与复盘 |
| 审计日志 | 30 天 | 90 天 | 180 天 | 365 天 | 权限与安全审计 |
| Trace（普通采样） | 3 天 | 3 天 | 7 天 | 7 天 | 控制存储成本 |
| Trace（错误/慢请求） | 7 天 | 14 天 | 30 天 | 30 天 | tail sampling 强制保留 |
| Metrics | 15 天 | 30 天 | 60 天 | 180 天 | 容量规划与趋势分析 |
| Continuous Profiles | 3 天 | 7 天 | 14 天 | 14 天 | 性能定位 |

Loki 使用 Compactor 管理全局和按流保留时间：

```yaml
compactor:
  retention_enabled: true
  delete_request_store: s3

limits_config:
  retention_period: 720h  # 普通日志 30 天
  retention_stream:
    - selector: '{log_type="audit"}'
      priority: 3
      period: 8760h       # 审计日志 365 天
    - selector: '{level=~"ERROR|FATAL"}'
      priority: 2
      period: 2160h       # 错误日志 90 天
```

Tempo 默认 `compactor.compaction.block_retention: 168h`（7 天）；错误和慢 Trace 若需保存 30 天，应由 Collector 路由到独立 Tempo tenant 或对象存储归档桶。Prometheus 单机通过 `--storage.tsdb.retention.time` 设置；生产长期指标迁移至 Mimir，对象存储生命周期配置 180 天。

所有保留时间由 Helm values 管理：

```yaml
observability:
  retention:
    logsDefault: 720h
    logsError: 2160h
    logsAudit: 8760h
    tracesDefault: 168h
    tracesPriority: 720h
    metrics: 4320h
    profiles: 336h
```

### 11.6 Trace 采样策略

| 环境 | 应用侧 | Collector Tail Sampling |
|------|-------|-------------------------|
| local/test | 100% | 100% 保留 |
| staging | always_on | ERROR 100%、延迟 >1s 100%、其余 20% |
| prod | always_on | ERROR 100%、HTTP 5xx 100%、延迟 >1s 100%、关键接口 100%、其余 5% |

Tail Sampling 必须在 Alloy/OTel Collector Gateway 进行；所有同一 Trace 的 Span 必须被路由到同一 Collector 实例，可使用负载均衡导出器或按 Trace ID 一致性哈希。流量极大时再在应用侧增加 head sampling，避免提前丢失错误 Trace。

关键接口包括：注册、登录、发帖、删帖、图片上传、私信发送、管理员操作。异步 RabbitMQ Trace 必须覆盖 Producer send、Broker wait、Consumer process 三段耗时。

### 11.7 Grafana 关联查询

Grafana Datasource 关联规则：

1. Loki 从 JSON 字段提取 `trace_id`，配置 Derived Field 跳转 Tempo。
2. Tempo 配置 `tracesToLogsV2`，按 `service.name`、Pod 和 Trace 时间窗口查 Loki。
3. Prometheus Histogram 开启 Exemplars，从延迟尖峰跳转具体 Trace。
4. Pyroscope 按 `service.name`、`span_name` 与 Trace 关联，定位慢 Span 的 CPU/分配热点。

排障流程统一为：

```
Grafana 告警 → Metrics 定位服务/接口
             → Exemplar 跳转 Trace
             → Trace 找到慢 Span / 错误 Span
             → trace_id 跳转关联日志
             → 必要时查看对应时间段 Profile
```

### 11.8 核心监控指标与告警

| 类别 | 指标 | 告警阈值 |
|------|------|---------|
| **API RED** | 请求率、错误率、P50/P95/P99 延迟 | P95 > 500ms 持续 5min；5xx > 1% |
| **SLO** | 可用性、延迟 SLO、错误预算消耗 | 1h/6h 双窗口 Burn Rate 告警 |
| **JVM** | 堆/非堆、GC、线程、类加载、CPU | 堆 > 80%；Full GC > 3次/分 |
| **连接池** | Hikari 活跃/空闲/等待连接 | 等待线程 > 0 持续 2min |
| **MySQL** | QPS、慢查询、锁等待、连接数、主从延迟 | 慢查询 > 10次/分；复制延迟 > 5s |
| **Redis** | 命中率、内存、Eviction、连接数 | 命中率 < 90%；内存 > 80% |
| **Elasticsearch** | 查询延迟、Pending Task、集群状态 | P95 > 200ms；状态 Yellow/Red |
| **RabbitMQ** | Ready/Unacked、消费延迟、死信数 | 堆积 > 1000；死信 > 0 |
| **Kubernetes** | Pod 重启、CPU/内存、调度失败、OOMKilled | 10min 内重启 > 3；出现 OOMKilled |
| **业务** | DAU、注册、发帖、评论、私信成功率 | 核心指标日环比下降 > 30% |
| **采集链路** | Collector 接收/丢弃/导出失败、队列长度 | dropped > 0；export error > 0 |

### 11.9 数据安全与成本控制

- Alloy Collector 在导出前删除 `authorization`、`cookie`、`password`、`token`、手机号和邮箱等字段。
- Loki 禁止高基数 Label；Tempo 禁止把用户 ID、帖子 ID 写入 Resource Attribute。
- 对象存储启用服务端加密、版本控制与生命周期删除；Loki/Tempo/Mimir 使用独立 Bucket。
- Grafana 使用 OIDC + RBAC；生产、测试数据源隔离，审计日志只对安全管理员开放。
- 监控平台自身也必须监控：Alloy 队列、Loki ingestion、Tempo distributor、Prometheus WAL 和对象存储错误。
- 日志量按服务、Level 和租户设置每日配额；DEBUG 默认仅 local 开启，生产临时开启最长 30 分钟并自动回落 INFO。

---

## 十二、Monorepo 仓库结构

```
buyer-show/
├── apps/
│   ├── api/                  # Spring Boot 后端（Java 21 + Maven）
│   │   ├── src/
│   │   ├── pom.xml
│   │   └── Dockerfile
│   ├── web/                  # React 前端（Vite + TypeScript）
│   │   ├── src/
│   │   ├── package.json
│   │   ├── vite.config.ts
│   │   ├── nginx.conf
│   │   └── Dockerfile
│   └── mini/                 # uniapp X 跨端（Vue 3 + TypeScript）
│       ├── src/
│       ├── pages.json
│       └── package.json
│
├── packages/
│   └── shared/               # 共享类型 + 常量 + 工具
│       ├── src/
│       │   ├── types/        # API DTO 类型定义（前端消费）
│       │   ├── constants/    # 状态码、错误码
│       │   └── utils/        # 纯函数工具
│       └── package.json
│
├── infra/
│   ├── docker/
│   │   ├── docker-compose.yml
│   │   ├── docker-compose.prod.yml
│   │   └── .env.example
│   ├── k8s/
│   │   ├── namespace.yaml
│   │   ├── api/
│   │   ├── web/
│   │   ├── mysql/
│   │   ├── redis/
│   │   ├── elasticsearch/
│   │   ├── rabbitmq/
│   │   ├── minio/
│   │   ├── ingress.yaml
│   │   ├── configmap.yaml
│   │   └── secrets.yaml
│   └── helm/buyershow/
│       ├── Chart.yaml
│       ├── values.yaml
│       └── templates/
│
├── docs/
│   ├── api-spec.yaml         # OpenAPI 3.0
│   ├── 架构设计文档.md        # 本文档
│   ├── 产品设计文档.md
│   ├── 技术开发规范.md
│   └── 部署指南.md
│
├── .github/workflows/
│   ├── ci.yml
│   └── deploy.yml
│
├── pnpm-workspace.yaml       # 前端 Monorepo
├── turbo.json                # Turborepo（前端构建编排）
├── package.json              # 根 workspace
└── README.md
```

---

## 十三、开发工作流

### 13.1 本地启动

```bash
# 1. 一键启动全部基础设施
docker compose -f infra/docker/docker-compose.yml up -d

# 2. 后端开发
cd apps/api
mvn spring-boot:run -Dspring-boot.run.profiles=dev
# API → http://localhost:8080
# Swagger → http://localhost:8080/swagger-ui.html

# 3. 前端开发
cd apps/web
pnpm install && pnpm dev
# Web → http://localhost:5173

# 4. 小程序开发
cd apps/mini
pnpm install && pnpm dev:mp-weixin
# 微信开发者工具打开 dist/dev/mp-weixin

# 5. 管理界面
# RabbitMQ → http://localhost:15672 (app/devpassword)
# MinIO   → http://localhost:9001 (minioadmin/minioadmin)
```

### 13.2 分支策略

```
main          ← 生产分支，push 自动部署
  └── dev     ← 集成测试分支，PR 合入后自动部署到 staging
       ├── feature/user-auth
       ├── feature/post-crud
       ├── feature/search-es
       ├── fix/feed-performance
       └── hotfix/login-crash
```

### 13.3 提交规范

```
<type>(<scope>): <subject>

type:  feat | fix | refactor | perf | test | docs | chore | ci
scope: api | web | mini | db | infra | shared

示例：
feat(api): 实现帖子发布接口和参数校验
feat(web): 实现首页瀑布流和标签过滤
feat(mini): 微信小程序首页适配
fix(api): 修复 Feed 分页游标重复数据问题
perf(db): 优化帖子查询添加复合索引
ci: 添加 Docker 镜像自动构建流水线
```

---

## 十四、迁移路径

采用**绞杀者模式**渐进式迁移，12 周完成：

| 阶段 | 周期 | 内容 | 交付物 |
|------|------|------|--------|
| **1. 项目初始化** | 第 1-2 周 | Monorepo 搭建、Spring Boot 骨架、MySQL 建表、Docker 环境 | 可运行空 API + 数据库 |
| **2. 核心 API** | 第 3-5 周 | 用户注册登录、帖子 CRUD、评论、点赞收藏关注 | 全部 API 接口 + Swagger 文档 |
| **3. 中间件接入** | 第 5-6 周 | Redis 缓存、ES 搜索、RabbitMQ 异步任务 | 搜索可用 + 缓存生效 |
| **4. Web 前端** | 第 6-8 周 | React SPA 开发，对接 API | 全部页面功能完成 |
| **5. 小程序** | 第 8-10 周 | uniapp X 开发，对接同一 API | 微信/支付宝小程序 |
| **6. 数据迁移** | 第 10-11 周 | Supabase → MySQL 迁移脚本，灰度切换 | 数据完整迁移 |
| **7. 部署上线** | 第 11-12 周 | K8s 部署、域名切换、监控配置、压力测试 | 正式上线 |

---

## 十五、性能指标目标

| 指标 | 目标值 |
|------|--------|
| API 响应 P95 | < 200ms |
| API 响应 P99 | < 500ms |
| 首页加载 FCP | < 1.5s |
| 首屏可交互 TTI | < 3s |
| Lighthouse 评分 | > 90 |
| API 可用性 | > 99.9% |
| 首屏 JS gzip | < 150KB |
| 搜索响应 | < 100ms |
| Feed 缓存命中率 | > 95% |
