# 买家说（Buyer Show）架构设计文档

> 版本：v3.0 | 更新：2026-09-14 | 定位：目标态架构与演进方向
> 设计原则：全栈主流化、前后端分离、多端同构、容器化部署
> 说明：本文档聚焦**目标态架构与演进方向**；实现现状、API 契约与数据库以《技术方案文档》为准；产品功能与 UI 规范见《产品设计文档》。

---

## 一、定位与目标

### 1.1 产品定位

「买家说」是一个购物种草社区应用，用户分享真实购物体验、发布商品评价、发现好物推荐。面向 18-35 岁网购用户，核心价值是**真实用户评价 + 社区发现**。

### 1.2 改造目标

将原有 Next.js + Supabase 单体应用改造为**前后端分离、多端适配、容器化部署**的现代化架构：

| 目标 | 说明 | 当前进度 |
|------|------|---------|
| 前后端分离 | API 独立服务，前端通过 HTTP 消费，不直接访问数据库 | ✅ 已完成 |
| 多端支持 | Web（PC + H5）、微信小程序、支付宝小程序、App（iOS/Android） | ⚠️ Web 完成；小程序/App 待实现 |
| 容器化部署 | Docker + Docker Compose（开发）、Kubernetes（生产） | ⚠️ Compose 可用；K8s 待配置 |
| 独立扩展 | 前端和后端可独立扩缩容，互不影响 | ✅ 架构支持 |
| 技术主流化 | 每一层都采用业界验证过的主流方案 | ✅ 已完成 |

### 1.3 技术栈选型决策（为什么不用其他方案）

| 选型 | 淘汰方案 | 淘汰原因 |
|------|---------|---------|
| Spring Boot 3 | NestJS / Hono / Express | Node.js 后端在企业级项目中招聘难、大型项目维护成本高 |
| MySQL 8.0 | PostgreSQL | MySQL 在国内运维人才储备更多，云服务支持更好 |
| uniapp X | Taro / React Native / Flutter | 国内小程序覆盖率最高（微信+支付宝+抖音+百度+快手），一套 Vue 代码 |
| RabbitMQ | Kafka / RocketMQ | 社区应用体量不需要 Kafka 级吞吐量，RabbitMQ 更轻量成熟 |
| Elasticsearch | MeiliSearch / Typesense | ES 是搜索行业事实标准，生产验证最充分 |
| MyBatis-Plus | JPA/Hibernate / Prisma | 国内 Java 项目使用率最高，SQL 可控性好 |

---

## 二、目标态系统架构

### 2.1 整体架构图

> 下图为目标态（含规划组件：Spring Cloud Gateway、ES、RabbitMQ、MySQL 主从）；当前已落地部分见《技术方案文档》。

```
┌─────────────────────────────────────────────────────────────────┐
│                         客户端层                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │  Web PC  │  │  Web H5  │  │ 小程序   │  │  App (可选)   │  │
│  │  React   │  │  React   │  │ uniapp X │  │  uniapp X     │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬────────┘  │
│       └──────────────┴─────────────┴───────────────┘            │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTPS
                       ┌───────▼───────┐
                       │   Nginx / LB  │  SSL 终结 + 静态资源 + 反向代理
                       └───┬───────┬───┘
                           │       │
              ┌────────────┘       └────────────┐
              ▼                                  ▼
    ┌─────────────────┐               ┌─────────────────┐
    │  Web 静态资源    │               │  API Gateway    │
    │  (Nginx 托管)   │               │ (Spring Cloud   │
    │  React SPA     │               │  Gateway，规划)  │
    └─────────────────┘               └────────┬────────┘
                                               │
                                    ┌──────────▼──────────┐
                                    │   API Server        │
                                    │   Spring Boot 3     │
                                    │   (多实例, 无状态)   │
                                    └──┬──────┬──────┬────┘
                                       │      │      │
                          ┌────────────┘  ┌───┘  ┌───┴───────────┐
                          ▼               ▼      ▼               ▼
                    ┌──────────┐  ┌──────────┐ ┌──────────┐ ┌────────┐
                    │ MySQL 8  │  │ Redis 7  │ │ ES 8     │ │ MinIO  │
                    │ (主从,规划)│  │ (规划)   │ │ (规划)   │ │ / OSS  │
                    └──────────┘  └──────────┘ └──────────┘ └────────┘
                          │
                    ┌─────▼──────┐
                    │ RabbitMQ   │  异步任务：图片处理、通知推送、数据统计
                    └────────────┘
```

### 2.2 分层职责

| 层 | 职责 | 约束 |
|----|------|------|
| **客户端层** | UI 渲染、用户交互、状态管理、表单校验 | 禁止直接访问数据库和中间件 |
| **网关层** | SSL 终结、静态资源、请求路由、限流、跨域 | 不做业务逻辑 |
| **API 层** | 业务逻辑、数据校验、权限控制、数据聚合 | 无状态，可水平扩展 |
| **数据层** | 持久化、缓存、搜索索引、文件存储、消息队列 | 仅通过 API 层访问 |
| **基础设施层** | 容器编排、服务发现、配置管理、监控告警 | 声明式配置，GitOps |

### 2.3 请求流转（目标态）

```
用户操作 → 客户端 → HTTPS → Nginx
  ├── 静态资源 → 直接返回 HTML/JS/CSS/图片
  └── /api/* → Spring Cloud Gateway（规划）
        ├── 限流校验（令牌桶算法）
        ├── JWT 认证（Spring Security Filter，已落地）
        ├── 请求路由 → API Server
        │     ├── Controller：参数校验（JSR 380 + 自定义 Validator）
        │     ├── Service：业务逻辑编排
        │     ├── Mapper（MyBatis-Plus）：数据库 CRUD
        │     ├── RedisTemplate：缓存读写（规划）
        │     ├── ElasticsearchClient：搜索查询（规划）
        │     ├── RabbitTemplate：异步消息发送（规划）
        │     └── MinIO Client：文件上传下载（已落地）
        └── 统一响应格式化 → JSON
```

---

## 三、关键链路设计（目标态）

### 3.1 Feed 策略

**当前实现**：MySQL 主键游标直查（`idx_post_feed(status, moderation_status, id DESC)`），无缓存层。

**目标态（Redis + DB 混合）**：

```
读取流程（拉模式 + 缓存）：
  1. 查 Redis 缓存：GET feed:cursor:{cursor}
  2. 命中 → 直接返回
  3. 未命中 → 查 MySQL（走 Feed 主键游标索引）
  4. 写入 Redis 缓存（TTL 5 分钟）
  5. 返回结果

写入流程（异步更新）：
  1. 发布帖子 → 写入 MySQL posts 表
  2. 发送 RabbitMQ 消息：{ type: "POST_CREATED", postId, userId }
  3. Consumer 异步处理：
     - 更新 Elasticsearch 索引
     - 预热 Feed 缓存
     - 推送关注用户通知

搜索流程（规划）：
  1. 用户输入关键词 → 前端防抖 300ms
  2. GET /api/v1/search?q=关键词&cursor=xxx
  3. Service 调用 Elasticsearch bool query（title + content + tags）
  4. 高亮匹配结果，返回分页
```

### 3.2 Redis 缓存策略（规划）

```
缓存用途：
  ├── Feed 缓存：feed:{tag}:{cursor} → 帖子列表 JSON（TTL 5分钟）
  ├── 热门数据：hot:tags → 热门标签（TTL 1小时, 定时刷新）
  ├── 计数缓存：count:post:{id}:likes → 点赞数（写时递增, 定时回写DB）
  ├── 限流：ratelimit:{ip|userId} → 令牌桶计数（TTL 1分钟）
  ├── 验证码：sms:{phone} → 短信验证码（TTL 5分钟）
  └── 登出黑名单：jwt:blacklist:{jti}（预留；当前 refresh token 为无状态 JWT）

缓存策略：
  ├── 读：Cache-Aside（先查缓存 → 未命中查DB → 写缓存）
  ├── 写：Write-Through（先更新DB → 再删缓存）
  └── 热点：定时任务预热 + 逻辑过期（防缓存雪崩）

防穿透：
  ├── 空值缓存：查询不存在的 key 也缓存空值（TTL 30s）
  └── 布隆过滤器：用户 ID 合法性预判断
```

### 3.3 RabbitMQ 消息队列（规划）

```
交换机设计：
  ├── buyershow.topic（Topic Exchange）
  │     ├── post.created     → 帖子发布事件
  │     ├── post.liked       → 点赞事件
  │     ├── comment.created  → 评论事件
  │     └── user.followed    → 关注事件
  │
  └── buyershow.direct（Direct Exchange）
        ├── image.process    → 图片压缩任务
        └── notification.push → 通知推送任务

消费者：
  ├── ImageProcessConsumer：接收图片上传事件 → 压缩 → WebP 转换 → 更新对象存储
  ├── NotificationConsumer：接收互动事件 → 生成通知 → 推送到用户
  ├── StatsConsumer：接收计数变更 → 批量更新 posts.like_count 等冗余字段
  └── FeedCacheConsumer：接收帖子变更 → 刷新 Feed 缓存

可靠性保障：
  ├── 消息持久化（delivery_mode = 2）
  ├── 手动 ACK（处理成功才确认）
  ├── 死信队列（处理失败 3 次后进 DLX）
  └── 幂等消费（基于消息 ID 去重）
```

### 3.4 Elasticsearch 搜索（规划）

```
索引设计：
  PUT /buyer_posts
  {
    "settings": {
      "number_of_shards": 1,
      "analysis": {
        "analyzer": {
          "ik_smart_analyzer": { "type": "custom", "tokenizer": "ik_smart" }
        }
      }
    },
    "mappings": {
      "properties": {
        "id":         { "type": "long" },
        "user_id":    { "type": "long" },
        "title":      { "type": "text", "analyzer": "ik_smart_analyzer" },
        "content":    { "type": "text", "analyzer": "ik_smart_analyzer" },
        "tags":       { "type": "keyword" },
        "product_name": { "type": "text", "analyzer": "ik_smart_analyzer" },
        "rating":     { "type": "byte" },
        "like_count": { "type": "integer" },
        "status":     { "type": "byte" },
        "created_at": { "type": "date" }
      }
    }
  }

搜索查询：
  GET /buyer_posts/_search
  {
    "query": {
      "bool": {
        "must": [
          { "multi_match": { "query": "关键词", "fields": ["title^3", "content", "product_name"] } }
        ],
        "filter": [
          { "term": { "status": 0 } }
        ]
      }
    },
    "highlight": {
      "fields": { "title": {}, "content": { "fragment_size": 100 } }
    },
    "sort": [{ "_score": "desc" }, { "created_at": "desc" }],
    "search_after": [cursor_score, cursor_time],
    "size": 20
  }

数据同步（双写，替代 Canal）：
  帖子写入 MySQL → RabbitMQ 消息 → ES Sync Consumer → 更新 ES 索引
  延迟 < 1s，最终一致性
```

### 3.5 认证扩展链路（规划）

```
微信小程序登录：
  客户端 → wx.login() 获取 code
  客户端 → POST /api/v1/auth/wechat { code }
  服务端 → 调用微信 code2session 接口 → 获取 openid
         → 查找 users 表（openid 关联）→ 不存在则自动注册
         → 签发 TokenPair

短信验证码登录：
  客户端 → POST /api/v1/auth/sms { phone }
  服务端 → 生成验证码 → Redis 存储（TTL 5 分钟）→ 短信网关下发
  客户端 → POST /api/v1/auth/login { phone, code } → 校验 → 签发 TokenPair
```

---

## 四、跨端架构（uniapp X）

```
uniapp X 优势（vs Taro / React Native / Flutter）：
  ├── 一套 Vue 3 代码 → 微信/支付宝/抖音/百度/快手小程序 + H5 + App
  ├── 国内开发者最多，插件市场最丰富
  ├── 条件编译：#ifdef MP-WEIXIN / #ifdef H5 / #ifdef APP
  ├── 原生渲染引擎（App 端非 WebView）
  └── 与 Vue 生态完全兼容（Pinia, VueUse, etc.）

代码共享策略：
  ├── 100% 共享：types/, constants/, utils/, services/（API 层，与 Web 对齐协议）
  ├── 80% 共享：pages/ 页面逻辑、store/ 状态管理
  ├── 60% 共享：components/（条件编译处理平台差异）
  └── 0% 共享：平台特有 API（wx.login, uni.chooseImage）

目录结构（app/，与技术方案文档一致）：
  app/
  ├── src/
  │   ├── pages/           # 页面（与 Web 对齐）
  │   │   ├── index/       # 首页 Feed
  │   │   ├── post-detail/ # 帖子详情
  │   │   ├── publish/     # 发布
  │   │   ├── search/      # 搜索
  │   │   ├── profile/     # 个人中心
  │   │   └── login/       # 登录
  │   ├── components/      # 跨端组件
  │   ├── services/        # uni.request 封装
  │   ├── store/           # Pinia 状态管理
  │   ├── utils/           # 工具函数
  │   └── static/          # 静态资源
  ├── pages.json           # uniapp 页面配置
  ├── manifest.json        # 小程序/App 配置
  └── package.json
```

---

## 五、部署与交付（目标态）

### 5.1 Kubernetes 部署清单

```yaml
# infra/k8s/api-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: buyershow-api
  namespace: buyershow
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxUnavailable: 1, maxSurge: 1 }
  selector:
    matchLabels: { app: buyershow-api }
  template:
    metadata:
      labels: { app: buyershow-api }
    spec:
      containers:
        - name: api
          image: registry.cn-hangzhou.aliyuncs.com/buyershow/api:${TAG}
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: buyershow-config }
            - secretRef: { name: buyershow-secrets }
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: 1000m, memory: 1Gi }
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 8080 }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /api/v1/health, port: 8080 }
            initialDelaySeconds: 60
            periodSeconds: 20
          startupProbe:
            httpGet: { path: /api/v1/health, port: 8080 }
            failureThreshold: 30
            periodSeconds: 10
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: buyershow-api
  namespace: buyershow
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: buyershow-api
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 65 }
    - type: Resource
      resource:
        name: memory
        target: { type: Utilization, averageUtilization: 75 }
```

其他清单：`namespace.yaml`、`web/`（Nginx）、`mysql/`（主从 StatefulSet）、`redis/`、`ingress.yaml`、`configmap.yaml`、`secrets.yaml`。本地 Compose 编排与启动命令见《技术方案文档》§七。

### 5.2 CI/CD 流水线（规划）

```yaml
# .github/workflows/ci.yml
name: CI
on:
  pull_request:
    branches: [main, dev]
jobs:
  backend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: 21, distribution: temurin, cache: maven }
      - run: cd server && mvn verify -B

  frontend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: cd frontend && npm ci && npm run lint && npm run build

---
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  build-push-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build & Push API Image
        run: |
          docker build -t registry.cn-hangzhou.aliyuncs.com/buyershow/api:${{ github.sha }} server/
          docker push registry.cn-hangzhou.aliyuncs.com/buyershow/api:${{ github.sha }}

      - name: Build & Push Web Image
        run: |
          docker build -t registry.cn-hangzhou.aliyuncs.com/buyershow/web:${{ github.sha }} frontend/
          docker push registry.cn-hangzhou.aliyuncs.com/buyershow/web:${{ github.sha }}

      - name: Deploy to K8s (ArgoCD sync)
        run: |
          sed -i "s/tag:.*/tag: ${{ github.sha }}/" infra/helm/buyershow/values.yaml
          git add infra/helm/buyershow/values.yaml
          git commit -m "chore: bump image tag to ${{ github.sha }}"
          git push
          # ArgoCD 自动检测 Git 变更并同步
```

---

## 六、安全设计

| 维度 | 措施 | 状态 |
|------|------|------|
| **传输安全** | 全站 HTTPS (Let's Encrypt), HSTS, TLS 1.3 | 规划 |
| **认证** | Spring Security + JWT 双 token, BCrypt 密码哈希 | ✅ 已落地 |
| **授权** | RBAC 角色控制（USER / ADMIN），资源级权限检查 | ✅ 已落地（管理端实时角色校验） |
| **输入校验** | JSR 380 Bean Validation + 自定义 Validator，SQL 参数化（MyBatis-Plus 防注入） | ✅ 已落地 |
| **XSS 防护** | 输出转义，CSP 头，禁止 innerHTML | 部分（React 默认转义） |
| **CSRF 防护** | SameSite Cookie + CSRF Token（Web 端） | 规划（当前无 Cookie 会话） |
| **文件上传** | MIME/签名白名单 + 大小限制(10MB) + 重命名存储 | ✅ 已落地；病毒扫描规划 |
| **内容安全** | 敏感词 + 待审状态 + 举报 + 人工审核台 | ✅ 已落地；第三方 AI 审核规划 |
| **限流** | Gateway 层令牌桶（IP 60次/分钟, 用户 120次/分钟） | 规划 |
| **敏感数据** | K8s Secret 注入，日志脱敏，禁止硬编码 | 部分（配置外置见待办 P1.8） |
| **审计日志** | 关键操作（登录/删除/封禁）记录审计日志 | 规划（格式见 §7.3） |

---

## 七、可观测性设计

### 7.1 统一可观测性架构

采用 OpenTelemetry 作为厂商中立的遥测标准，以 Grafana Alloy（兼容 OpenTelemetry Collector）作为 Kubernetes 采集与网关层，统一关联 Metrics、Logs、Traces 和 Profiles。

```
Spring Boot Pod
├── OpenTelemetry Java Agent
│   ├── HTTP / JDBC / Redis / RabbitMQ 自动 Trace
│   ├── W3C traceparent / baggage 上下文传播
│   └── OTLP gRPC → Alloy Gateway :4317
├── Micrometer + Spring Boot Actuator
│   └── /actuator/prometheus → Prometheus
├── Logback JSON
│   ├── stdout（Kubernetes 主采集通道）
│   └── /var/log/buyer-show/application.json.log（短期本地缓冲）
└── Pyroscope Java Agent（P2，可选）
    └── CPU / Allocation / Lock 持续剖析

Kubernetes
├── Grafana Alloy DaemonSet
│   ├── 采集 /var/log/containers/*.log
│   ├── 解析 JSON、补充 k8s.* 标签、脱敏与限流
│   └── 写入 Loki
├── Grafana Alloy Gateway Deployment
│   ├── 接收 OTLP gRPC / HTTP
│   ├── memory_limiter + batch + tail_sampling
│   ├── Trace → Tempo
│   ├── Metrics → Prometheus / Mimir
│   └── Logs → Loki
└── Grafana
    ├── PromQL 查询 Metrics
    ├── LogQL 查询 Logs
    ├── TraceQL 查询 Traces
    ├── Profiles 查询 Pyroscope
    └── Trace ↔ Log ↔ Metrics ↔ Profile 双向跳转
```

**技术职责**：

| 信号 | 应用侧 | 采集/网关 | 存储与查询 | 用途 |
|------|-------|----------|-----------|------|
| Metrics | Micrometer + Actuator | Prometheus / Alloy | Prometheus（规模化后 Mimir） | RED/USE、SLO、业务指标 |
| Logs | Logback JSON + MDC | Alloy DaemonSet | Loki | 错误排查、审计、Trace 反查 |
| Traces | OTel Java Agent | Alloy Gateway | Tempo | 跨服务、JDBC、Redis、MQ 调用链 |
| Profiles | Pyroscope Java Agent | Alloy | Pyroscope | CPU、内存分配、锁竞争定位 |

不再使用 Spring Cloud Sleuth；Spring Boot 3 使用 Micrometer Observation/Tracing 与 OpenTelemetry。Jaeger、SkyWalking 作为备选，不作为本项目默认主链路。

### 7.2 Spring Boot 自动插桩与资源标识

生产环境优先通过 OpenTelemetry Operator 自动注入 Java Agent，避免业务代码直接依赖厂商 SDK：

```yaml
apiVersion: opentelemetry.io/v1alpha1
kind: Instrumentation
metadata:
  name: buyer-show-java
  namespace: buyer-show
spec:
  exporter:
    endpoint: http://alloy-gateway.observability.svc:4317
  propagators: [tracecontext, baggage]
  sampler:
    type: parentbased_always_on
  java:
    env:
      - name: OTEL_METRICS_EXPORTER
        value: none
      - name: OTEL_LOGS_EXPORTER
        value: none
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: buyer-show-server
spec:
  template:
    metadata:
      annotations:
        instrumentation.opentelemetry.io/inject-java: buyer-show-java
```

Metrics 由 Micrometer/Prometheus 提供，日志由 stdout/Alloy 提供，因此 Java Agent 仅导出 Trace，避免同一信号重复采集。

统一资源属性：

```yaml
OTEL_SERVICE_NAME: buyer-show-server
OTEL_RESOURCE_ATTRIBUTES: >-
  service.namespace=buyer-show,
  service.version=${APP_VERSION},
  deployment.environment=${ENVIRONMENT},
  k8s.namespace.name=buyer-show
OTEL_EXPORTER_OTLP_ENDPOINT: http://alloy-gateway.observability.svc:4317
OTEL_EXPORTER_OTLP_PROTOCOL: grpc
```

链路必须遵循 W3C Trace Context。HTTP、异步线程、RabbitMQ Producer/Consumer 和定时任务均需传播 `traceparent`；跨线程任务使用 OTel context wrapper，禁止手动生成无父级 traceId。

### 7.3 结构化日志规范

日志统一输出单行 JSON；`trace_id`、`span_id`、`request_id` 由 MDC 自动注入，用于在 Grafana 中从日志一键跳转 Tempo Trace。

```json
{
  "timestamp": "2026-09-08T14:30:00.123+08:00",
  "level": "ERROR",
  "service": "buyer-show-server",
  "environment": "prod",
  "trace_id": "4bf92f3577b34da6a3ce929d0e0e4736",
  "span_id": "00f067aa0ba902b7",
  "request_id": "req_01J...",
  "logger": "com.buyershow.service.PostService",
  "thread": "http-nio-8080-exec-12",
  "event": "post_publish_failed",
  "user_id_hash": "sha256:...",
  "message": "帖子发布失败",
  "exception": "..."
}
```

**字段与标签规则**：

- Loki Label 仅使用低基数字段：`service_name`、`environment`、`namespace`、`pod`、`level`、`log_type`。
- `trace_id`、`span_id`、`request_id`、`user_id`、`post_id` 放在 JSON 字段中，禁止作为 Loki Label，防止标签爆炸。
- 禁止记录密码、JWT、Refresh Token、验证码、身份证、手机号、邮箱原文、Cookie、完整请求体。
- 用户 ID、手机号等定位字段必须哈希或脱敏；异常栈仅在 ERROR 日志中记录。
- 每个业务异常只记录一次；禁止 Controller、Service、全局异常处理器重复打印同一异常。
- 审计日志 `log_type=audit` 独立记录登录、账号注销、删帖、封禁、权限变更，并写明操作人、资源、结果和原因。

### 7.4 本地日志文件管理与滚动策略

Kubernetes 以 stdout 为权威采集源；本地日志文件只作为短期故障缓冲，不作为长期日志存储。Spring Boot 使用 Logback `SizeAndTimeBasedRollingPolicy`：

```xml
<property name="LOG_PATH" value="${LOG_PATH:-./logs}"/>
<property name="LOG_MAX_HISTORY" value="${LOG_FILE_MAX_HISTORY_DAYS:-7}"/>
<property name="LOG_TOTAL_SIZE_CAP" value="${LOG_FILE_TOTAL_SIZE_CAP:-2GB}"/>
<property name="LOG_MAX_FILE_SIZE" value="${LOG_FILE_MAX_SIZE:-100MB}"/>

<appender name="ROLLING_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
  <file>${LOG_PATH}/application.json.log</file>
  <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
    <fileNamePattern>${LOG_PATH}/archive/application.%d{yyyy-MM-dd}.%i.json.log.gz</fileNamePattern>
    <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
    <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
    <totalSizeCap>${LOG_TOTAL_SIZE_CAP}</totalSizeCap>
    <cleanHistoryOnStart>true</cleanHistoryOnStart>
  </rollingPolicy>
</appender>
```

| 环境 | 本地文件保留 | 单文件上限 | 总容量上限 | 说明 |
|------|-------------|-----------|-------------|------|
| local | 7 天 | 100 MB | 2 GB | 便于本机调试 |
| test | 3 天 | 100 MB | 2 GB | 集中日志已进入 Loki |
| staging | 3 天 | 100 MB | 5 GB | 仅作采集失败缓冲 |
| prod | 1 天 | 200 MB | 5 GB | Pod 本地文件非持久，不依赖其归档 |

Docker Compose 将 `./logs/server:/var/log/buyer-show` 作为可选 bind mount。Kubernetes 使用 `emptyDir` 作为短期缓冲；禁止为普通应用日志挂长期 PVC，长期保存统一交给 Loki 对象存储。

Kubernetes 节点侧同时配置容器日志轮转：

```yaml
# kubelet configuration
containerLogMaxSize: 100Mi
containerLogMaxFiles: 5
```

### 7.5 集中日志保存时长

日志、Trace、Metrics 的保存期限全部参数化，按环境和信号分层。默认策略：

| 数据类型 | local | test | staging | prod | 说明 |
|----------|-------|------|---------|------|------|
| INFO/WARN 应用日志 | 7 天 | 7 天 | 14 天 | 30 天 | 常规问题排查 |
| ERROR/FATAL 日志 | 7 天 | 14 天 | 30 天 | 90 天 | 故障与复盘 |
| 审计日志 | 30 天 | 90 天 | 180 天 | 365 天 | 权限与安全审计 |
| Trace（普通采样） | 3 天 | 3 天 | 7 天 | 7 天 | 控制存储成本 |
| Trace（错误/慢请求） | 7 天 | 14 天 | 30 天 | 30 天 | tail sampling 强制保留 |
| Metrics | 15 天 | 30 天 | 60 天 | 180 天 | 容量规划与趋势分析 |
| Continuous Profiles | 3 天 | 7 天 | 14 天 | 14 天 | 性能定位 |

Loki 使用 Compactor 管理全局和按流保留时间：

```yaml
compactor:
  retention_enabled: true
  delete_request_store: s3

limits_config:
  retention_period: 720h  # 普通日志 30 天
  retention_stream:
    - selector: '{log_type="audit"}'
      priority: 3
      period: 8760h       # 审计日志 365 天
    - selector: '{level=~"ERROR|FATAL"}'
      priority: 2
      period: 2160h       # 错误日志 90 天
```

Tempo 默认 `compactor.compaction.block_retention: 168h`（7 天）；错误和慢 Trace 若需保存 30 天，应由 Collector 路由到独立 Tempo tenant 或对象存储归档桶。Prometheus 单机通过 `--storage.tsdb.retention.time` 设置；生产长期指标迁移至 Mimir，对象存储生命周期配置 180 天。

所有保留时间由 Helm values 管理：

```yaml
observability:
  retention:
    logsDefault: 720h
    logsError: 2160h
    logsAudit: 8760h
    tracesDefault: 168h
    tracesPriority: 720h
    metrics: 4320h
    profiles: 336h
```

### 7.6 Trace 采样策略

| 环境 | 应用侧 | Collector Tail Sampling |
|------|-------|-------------------------|
| local/test | 100% | 100% 保留 |
| staging | always_on | ERROR 100%、延迟 >1s 100%、其余 20% |
| prod | always_on | ERROR 100%、HTTP 5xx 100%、延迟 >1s 100%、关键接口 100%、其余 5% |

Tail Sampling 必须在 Alloy/OTel Collector Gateway 进行；所有同一 Trace 的 Span 必须被路由到同一 Collector 实例，可使用负载均衡导出器或按 Trace ID 一致性哈希。流量极大时再在应用侧增加 head sampling，避免提前丢失错误 Trace。

关键接口包括：注册、登录、发帖、删帖、图片上传、私信发送、管理员操作。异步 RabbitMQ Trace 必须覆盖 Producer send、Broker wait、Consumer process 三段耗时。

### 7.7 Grafana 关联查询

Grafana Datasource 关联规则：

1. Loki 从 JSON 字段提取 `trace_id`，配置 Derived Field 跳转 Tempo。
2. Tempo 配置 `tracesToLogsV2`，按 `service.name`、Pod 和 Trace 时间窗口查 Loki。
3. Prometheus Histogram 开启 Exemplars，从延迟尖峰跳转具体 Trace。
4. Pyroscope 按 `service.name`、`span_name` 与 Trace 关联，定位慢 Span 的 CPU/分配热点。

排障流程统一为：

```
Grafana 告警 → Metrics 定位服务/接口
             → Exemplar 跳转 Trace
             → Trace 找到慢 Span / 错误 Span
             → trace_id 跳转关联日志
             → 必要时查看对应时间段 Profile
```

### 7.8 核心监控指标与告警

| 类别 | 指标 | 告警阈值 |
|------|------|---------|
| **API RED** | 请求率、错误率、P50/P95/P99 延迟 | P95 > 500ms 持续 5min；5xx > 1% |
| **SLO** | 可用性、延迟 SLO、错误预算消耗 | 1h/6h 双窗口 Burn Rate 告警 |
| **JVM** | 堆/非堆、GC、线程、类加载、CPU | 堆 > 80%；Full GC > 3次/分 |
| **连接池** | Hikari 活跃/空闲/等待连接 | 等待线程 > 0 持续 2min |
| **MySQL** | QPS、慢查询、锁等待、连接数、主从延迟 | 慢查询 > 10次/分；复制延迟 > 5s |
| **Redis** | 命中率、内存、Eviction、连接数 | 命中率 < 90%；内存 > 80% |
| **Elasticsearch** | 查询延迟、Pending Task、集群状态 | P95 > 200ms；状态 Yellow/Red |
| **RabbitMQ** | Ready/Unacked、消费延迟、死信数 | 堆积 > 1000；死信 > 0 |
| **Kubernetes** | Pod 重启、CPU/内存、调度失败、OOMKilled | 10min 内重启 > 3；出现 OOMKilled |
| **业务** | DAU、注册、发帖、评论、私信成功率 | 核心指标日环比下降 > 30% |
| **采集链路** | Collector 接收/丢弃/导出失败、队列长度 | dropped > 0；export error > 0 |

### 7.9 数据安全与成本控制

- Alloy Collector 在导出前删除 `authorization`、`cookie`、`password`、`token`、手机号和邮箱等字段。
- Loki 禁止高基数 Label；Tempo 禁止把用户 ID、帖子 ID 写入 Resource Attribute。
- 对象存储启用服务端加密、版本控制与生命周期删除；Loki/Tempo/Mimir 使用独立 Bucket。
- Grafana 使用 OIDC + RBAC；生产、测试数据源隔离，审计日志只对安全管理员开放。
- 监控平台自身也必须监控：Alloy 队列、Loki ingestion、Tempo distributor、Prometheus WAL 和对象存储错误。
- 日志量按服务、Level 和租户设置每日配额；DEBUG 默认仅 local 开启，生产临时开启最长 30 分钟并自动回落 INFO。

---

## 八、性能指标目标

| 指标 | 目标值 |
|------|--------|
| API 响应 P95 | < 200ms |
| API 响应 P99 | < 500ms |
| 首页加载 FCP | < 1.5s |
| 首屏可交互 TTI | < 3s |
| Lighthouse 评分 | > 90 |
| API 可用性 | > 99.9% |
| 首屏 JS gzip | < 150KB |
| 搜索响应 | < 100ms |
| Feed 缓存命中率 | > 95% |

---

## 九、竞品对标

> 对标小红书、得物、什么值得买；「买家说现状」按 2026-09 实际实现更新。

### 9.1 小红书（RED）

**规模**：3 亿+用户，日均发布 300 万+笔记，日活 1 亿+。

| 能力 | 主流实现方案 | 买家说现状 |
|------|-------------|-----------|
| Feed 推荐 | 多路召回（协同过滤、内容理解、实时热点）+ 精排模型 + 重排 | ❌ 仅时间/游标排序，无个性化 |
| 内容审核 | 三级审核：AI 初筛（NLP + CV）→ 机器复审 → 人工抽检 | ⚠️ 本地敏感词 + 人工审核台已上线；第三方 AI 审核待接入 |
| 图片处理 | 智能裁剪 + 多分辨率缩略图 + CDN + WebP 自适应 | ⚠️ 上传校验与 pending→published 已落地；缩略图/CDN 待接入 |
| 搜索 | 查询理解 + 多路召回 + 个性化排序 + A/B 实验 | ❌ 无搜索（ES 规划中） |
| 实时通知 | WebSocket + 离线消息队列 + 多通道推送 | ❌ 表结构就绪，服务未实现 |
| 反作弊 | 设备指纹 + 行为模型 + 内容去重 + 刷量检测 | ❌ 无 |
| 数据平台 | 实时数仓 + 离线数仓 + 用户画像 + A/B 平台 | ❌ 无 |

### 9.2 得物（Dewu/Poizon）

**规模**：1 亿+用户，聚焦潮流电商社区。

| 能力 | 主流实现方案 | 买家说现状 |
|------|-------------|-----------|
| 商品识别 | AI 图像识别商品品牌/型号，自动关联商品库 | ❌ 纯手动输入商品名 |
| 鉴定服务 | 专业鉴定流程 + AI 辅助鉴定 + 鉴定证书 | N/A（非核心需求） |
| 社区 + 电商融合 | 笔记直接挂商品链接，购买转化链路完整 | ⚠️ 有商品名+价格+来源，无购买链接 |
| 3D 展示 | 商品 3D 模型展示，AR 试穿 | N/A |

### 9.3 什么值得买（SMZDM）

**规模**：5000 万+用户，聚焦消费决策。

| 能力 | 主流实现方案 | 买家说现状 |
|------|-------------|-----------|
| 优惠聚合 | 全网比价 + 历史价格曲线 + 优惠爬取 | ❌ 无价格追踪 |
| 标签体系 | 多层级标签（品类→品牌→型号→属性）+ 关联推荐 | ⚠️ 标签字典 + 关系表已建，单层 |
| SEO 优化 | SSR 渲染 + 结构化数据 + Sitemap + 分享卡片 | ❌ 纯 SPA，SEO 友好度低（P1.7） |

---

## 十、演进路线

> 与《产品设计文档》「代办功能」台账一一对应，本节为技术侧视图。

### 10.1 阶段规划

```
┌─────────────────────────────────────────────────────────────┐
│ Phase 1 (Month 1-2): 安全合规                                │
│  ├── ◐ 内容审核系统（本地敏感词 + 审核台已上线；AI 审核待接入）  │
│  ├── ◐ 图片处理流水线（上传/转码已落地；缩略图 + CDN 待做）      │
│  ├── ✗ 通知系统（表结构就绪，服务待实现）                      │
│  └── ✗ MySQL 主从 + 备份                                    │
├─────────────────────────────────────────────────────────────┤
│ Phase 2 (Month 3-4): 体验提升                                │
│  ├── ◐ 热门排序 Feed（游标分页已落地；热度排序待做）            │
│  ├── ✗ 搜索增强（查询建议、热搜词）                            │
│  ├── ✗ 反作弊基础（频率限制 + 设备指纹）                       │
│  └── ◐ 基础 SEO（meta 已有；Sitemap/结构化数据待做）           │
├─────────────────────────────────────────────────────────────┤
│ Phase 3 (Month 5-6): 增长引擎                                │
│  ├── ✗ 个性化推荐（标签匹配 + 协同过滤）                       │
│  ├── ✗ 数据埋点 + 基础分析看板                                │
│  ├── ✗ 多级缓存架构（Redis 接入）                             │
│  └── ✗ 标签体系升级                                          │
├─────────────────────────────────────────────────────────────┤
│ Phase 4 (Month 7-12): 规模化                                 │
│  ├── ✗ 深度学习推荐系统                                      │
│  ├── ✗ 向量搜索（语义搜索）                                   │
│  ├── ✗ 高可用架构（熔断、降级、多活）                          │
│  └── ✗ A/B 实验平台                                         │
└─────────────────────────────────────────────────────────────┘
```

### 10.2 新增技术组件总览

| 组件 | 用途 | 技术选型 | 引入阶段 | 状态 |
|------|------|---------|---------|------|
| 内容审核服务 | 文本/图片自动审核 | 本地敏感词（已落地）→ 阿里云内容安全 API | Phase 1 | ◐ 部分落地 |
| 图片处理 Worker | 缩略图/裁剪/水印 | Java 图像处理（ImageIO/Thumbnailator）+ RabbitMQ Consumer | Phase 1 | ✗ 待开发 |
| 通知服务 | 实时推送 + 离线消息 | Spring WebSocket + 极光推送 | Phase 1 | ✗ 待开发 |
| 审核工作台 | 人工审核管理 | React 管理后台模块 | Phase 1 | ✅ 已上线 |
| 推荐引擎 | Feed 个性化排序 | Redis ZSet → Python ML 服务 | Phase 2→4 | ✗ 待开发 |
| 设备指纹 | 反作弊识别 | FingerprintJS + 自建规则引擎 | Phase 2 | ✗ 待开发 |
| 埋点 SDK | 用户行为采集 | 自研轻量 SDK + ClickHouse | Phase 3 | ✗ 待开发 |
| 向量检索 | 语义搜索 + 推荐召回 | Milvus / Elasticsearch kNN | Phase 4 | ✗ 待开发 |
| A/B 实验 | 功能灰度测试 | 自建 Feature Flag 系统 | Phase 4 | ✗ 待开发 |

### 10.3 与主流的差距总览

| 维度 | 当前水平 | 主流水平 | 差距 | 追赶难度 |
|------|---------|---------|------|---------|
| 基础功能（发帖/评论/点赞） | ✅ 90% | 100% | 小 | ⭐ 低 |
| 内容审核 | ◐ 60% | 100% | 中 | ⭐⭐ 中 |
| 图片处理 | ◐ 40% | 100% | 大 | ⭐⭐ 中 |
| 通知系统 | ◐ 20% | 100% | 极大 | ⭐⭐ 中 |
| 推荐系统 | ❌ 0% | 100% | 极大 | ⭐⭐⭐ 高 |
| 搜索体验 | ❌ 10% | 100% | 极大 | ⭐⭐ 中 |
| 反作弊 | ❌ 0% | 100% | 极大 | ⭐⭐ 中 |
| SEO | ◐ 20% | 100% | 大 | ⭐ 低 |
| 数据分析 | ❌ 0% | 100% | 极大 | ⭐⭐⭐ 高 |
| 高可用 | ◐ 30% | 100% | 大 | ⭐⭐ 中 |

**核心结论**：基础功能层已达标；短板集中在**安全合规层**（AI 审核、反作弊）与**增长引擎层**（推荐、数据分析）。按 Phase 1→4 路线推进：优先补齐监管项（审核/图片/通知），再提升体验（搜索/SEO），最后构建数据壁垒。

---

*文档版本：v3.0 | 最后更新：2026-09-14*
