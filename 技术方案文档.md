# 买家说 — 技术方案文档

> 版本：v3.1 | 更新：2026-09-14 | 架构：前后端分离 Monorepo

**文档分工**（2026-09 文档合并后）：
- 本文档：技术栈、系统结构、数据库、API 契约、部署与运行手册
- 《产品设计文档》：产品定位、功能范围、UI 设计规范、代办事项
- 《架构设计文档》：目标态架构、中间件与可观测性设计、演进方向

---

## 一、项目概述

「买家说」是购物种草社区应用，采用**前后端分离 + Monorepo** 架构，支持 PC Web、手机浏览器、小程序多端访问。

**改造前**：Next.js 14 + Supabase 单体应用，部署到 EdgeOne Pages。
**改造后**：Vite + React 前端 + Spring Boot 后端 + MySQL 数据库，Docker + Kubernetes 部署。

---

## 二、技术栈

### 前端（frontend/）

| 层面 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 构建 | Vite | 8.x | 极速 HMR，esbuild 预构建 |
| 框架 | React | 19.x | 全球最主流前端框架 |
| 语言 | TypeScript | 6.x | 类型安全 |
| 组件库 | shadcn/ui | latest | Tailwind 原生组件，按需安装 |
| 样式 | Tailwind CSS | 4.x | 原子化 CSS，`@theme` Token |
| 路由 | react-router-dom | 7.x | SPA 路由 |
| 图标 | lucide-react | latest | 轻量 SVG 图标 |
| 状态 | TanStack Query（服务端状态）+ Zustand（跨页 UI 状态）| 5.x | `useMe` 全站共享用户缓存；Feed 无限滚动缓存；`ui-store` 未读数/主题单一事实源 |

### 后端（server/）

| 层面 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Spring Boot | 3.3 | Java 企业级标准 |
| 语言 | Java | 21 | LTS 版本 |
| ORM | MyBatis-Plus | 3.5 | 国内最主流 ORM |
| 数据库 | MySQL | 8.0 | 国内运维人才最多 |
| 缓存 | Redis | 7.x | Spring Cache：验证码/限流/在线标记/用户资料/匿名 Feed 首屏缓存（P1.2） |
| 搜索 | Elasticsearch | 8.14（spring-data-elasticsearch 5.3.6） | G5 已接入：帖子索引 + ngram 中文分词 + 高亮 + MySQL LIKE 降级（§5.9） |
| 消息队列 | RabbitMQ | 3.13 | G11 已接入：fanout 事件总线 `buyershow.events`（通知/私信/公告实时推送） |
| 对象存储 | MinIO / 阿里云 OSS | 8.x 客户端 | S3 兼容协议，已接入 |
| 认证 | Spring Security + JWT | - | 双 token 机制 |
| 迁移 | Flyway | 10.x | SQL 版本管理 |
| API 文档 | SpringDoc OpenAPI | 2.x | 自动生成 Swagger |

### 部署

| 层面 | 选型 | 说明 |
|------|------|------|
| 容器 | Docker + Docker Compose | 本地开发环境（已可用） |
| 编排 | Kubernetes 1.28 + Helm 3 | 生产环境（规划） |
| CI/CD | GitHub Actions + ArgoCD | GitOps 持续部署（规划） |
| 可观测标准 | OpenTelemetry + W3C Trace Context | 详见《架构设计文档》可观测性章节 |
| 网关 | Nginx | 静态托管与反向代理（已可用）；Spring Cloud Gateway 为规划项 |

---

## 三、项目结构（Monorepo）

```
buyer-show/
├── frontend/                         # Web 前端（Vite + React 19）
│   ├── src/
│   │   ├── App.tsx                   # 入口 + 路由声明
│   │   ├── main.tsx                  # React 挂载
│   │   ├── index.css                 # Tailwind 4 + @theme 设计 Token
│   │   ├── components/ui/            # shadcn/ui 组件
│   │   ├── lib/utils.ts              # 工具函数
│   │   ├── services/                 # API 请求层（http/auth/posts/comments/uploads/reports/admin）
│   │   └── flows/                    # 业务 Flow
│   │       ├── shared/               # 共享类型 + 本地常量
│   │       ├── home-feed/            # 首页 Feed（标签筛选 + 游标分页）
│   │       ├── post-detail/          # 帖子详情（互动 + 评论 + 举报）
│   │       ├── publish-post/         # 发布（图片上传 + 商品信息）
│   │       ├── auth/                 # 登录/注册
│   │       ├── admin/                # 管理员审核台
│   │       └── messages/             # 私信通知（静态展示）
│   ├── vite.config.ts                # Vite + Tailwind 插件 + @ alias + /api 代理
│   ├── tsconfig.json / tsconfig.app.json
│   └── package.json
│
├── server/                           # Java 后端（Spring Boot 3.3，已实现核心 API）
│   ├── src/main/java/com/buyershow/
│   │   ├── BuyerShowApplication.java
│   │   ├── config/                   # SecurityConfig, MinioConfig, MinioBucketInitializer
│   │   ├── controller/               # Auth/Post/Comment/Upload/ContentReport/AdminModeration/Health
│   │   ├── service/                  # 业务逻辑层（认证、帖子、评论、审核、上传等）
│   │   ├── mapper/                   # MyBatis-Plus Mapper（含自定义 SQL）
│   │   ├── entity/                   # 数据库实体
│   │   ├── dto/                      # 请求/响应 DTO
│   │   └── common/                   # R<T>、ErrorCode、异常处理、JWT 安全
│   └── src/main/resources/
│       ├── application.yml
│       ├── mapper/*.xml              # MyBatis XML（如有）
│       └── db/migration/             # Flyway SQL（V1__init_schema.sql ... V5）
│
├── app/                              # 移动端 uniapp X（待实现）
│
├── docs/assets/                      # logo 等静态资源
├── ui-prototype.html                 # 移动端 UI 原型（15 屏）
├── docker-compose.yml                # 全栈本地编排
├── start-dev.sh / stop-dev.sh        # 一键启动/停止开发环境
│
├── AGENTS.md / CLAUDE.md / README.md # 规范与入口
├── 产品设计文档.md                    # 产品功能 + UI 规范 + 待办
├── 技术方案文档.md                    # 本文档
├── 架构设计文档.md                    # 目标态架构与演进
└── buyer-show-backup/                # 旧版 Next.js/Supabase 应用归档（独立目录）
```

---

## 四、前端架构

### 4.1 响应式布局

| 断点 | 宽度 | 导航 | Feed 列数 |
|------|------|------|----------|
| Mobile | < 768px | 底部 TabBar | 2 列 |
| Tablet | 768-1023px | 底部 TabBar | 3 列 |
| Desktop | 1024-1279px | 顶部 Nav + 侧边栏 | 3 列 |
| Wide | ≥ 1280px | 顶部 Nav + 侧边栏 | 4-5 列 |

### 4.2 Flow 清单（4 Flow · 已实现）

| Flow | 核心交互 |
|------|---------|
| 首页 Feed | 列表 + 标签筛选 + 搜索入口 |
| 帖子详情 | 图片 + 商品信息卡 + 评论树 + 互动 + 举报 |
| 发布分享 | 图片上传（MinIO）→ 商品信息（来源/价格/评分）→ 正文 + 审核反馈 |
| 私信通知 | 对话列表 + 聊天 + 互动通知（静态展示，待通知服务） |

> 移动端 15 屏完整定义见《产品设计文档》§四；设计 Token 与组件规范见《产品设计文档》§五。
> 前端质量门禁：`tsc -b` 类型检查 + `vite build`（含 PWA 生成）为构建必过项；`npm run size:check` 首屏 JS gzip 门禁（阈值 150KB，当前 143.84）；`npm run build:report` 生成 bundle 可视化报告（dist/stats.html）；单元测试使用 Vitest + Testing Library（`npm run test:run`，当前 13 个测试文件 / 99 用例，覆盖 services 全量 API 层 + 核心流程纯逻辑（waterfall 分发）与无状态子组件（image-grid/action-bar/post-content））。
> 发版健壮性：懒加载 chunk 因资源哈希变化 404 时，`vite:preloadError` 监听会做一次性自动刷新（10 秒防抖防循环）；Service Worker 启用 `cleanupOutdatedCaches` 清理旧缓存；SW 新版本接管（`controllerchange`）时自动刷新一次，确保已打开的旧页面不会长期停留在缓存版本。
> 布局稳定性：`html { scrollbar-gutter: stable }` 固定滚动条占位，避免内容长短不同的页面（如主页 ↔ 粉丝/关注列表）切换时居中容器横向跳动、加载态抖动。
> Feed 瀑布流：采用「最短列分配 + flex 列渲染」（`distributePosts`，按图片比例估算卡高）替代 CSS columns，避免卡片少/高度不均时的列尾大段留白。

### 4.3 服务端状态层（TanStack Query）

**接入范围**（2026-09-18，P4.1）：

| 模块 | 机制 | 效果 |
|------|------|------|
| 当前用户 | `hooks/use-me.ts`：`useMe` + `useSessionCache`（queryKey `['me']`，单飞共享） | Header/UserMenu/Feed/个人主页同一事实源；编辑资料或登录后 `invalidateQueries` 全站同步，消除重复拉取 |
| Feed 列表 | `useInfiniteQuery`（queryKey `['feed', tag, sort]`，`getFeed` 接收 AbortSignal） | 标签/排序切换按 key 独立缓存秒开；`keepPreviousData` 防闪烁；旧请求自动取消（替代 requestVersion 手动守卫）；下拉刷新重拉首页后裁剪旧页 |
| 互动同步 | 详情页 `patchFeedPost`（`setQueriesData(['feed'])` 精准 patch 单卡片） | 点赞/收藏/评论计数与状态局部更新，免整列表重拉 |

**约定**：服务端数据一律走 Query 缓存；仅局部 UI 状态（搜索词、浮层开关、下拉手势等）保留 useState。跨页 UI 状态（P4.2）由 Zustand `stores/ui-store.ts` 承载：未读通知数（App 根 `UnreadCountPoller` 全局单一轮询，替代原先各入口各自轮询 + window 事件广播；消息页「全部已读」直接 `setUnreadCount` 同步各入口徽标）与主题偏好（localStorage 持久化，多入口共享同一主题 state）。

---

## 五、后端架构

### 5.1 分层设计

```
Controller（参数校验 + 调用 Service + 返回 R<T>）
    ↓
Service（业务逻辑 + 事务管理）
    ↓
Mapper（MyBatis-Plus CRUD + 自定义 SQL）
    ↓
MySQL 8.0（Redis 已接入：缓存/限流/验证码/在线标记；RabbitMQ 已接入（G11 事件总线）；ES 为规划中，代码未接入）
```

### 5.2 统一响应

```java
public class R<T> {
    private int code;       // 0=成功，其他=业务错误
    private String message; // 用户可见消息
    private T data;

    public static <T> R<T> ok(T data) { ... }
    public static <T> R<T> fail(int code, String message) { ... }
    public static <T> R<T> fail(ErrorCode error) { ... }
}
```

### 5.3 错误码规范

| 范围 | 模块 | 示例 |
|------|------|------|
| 1000-1999 | 认证 | TOKEN_EXPIRED, NO_PERMISSION, ACCOUNT_BANNED |
| 2000-2999 | 用户 | USER_NOT_FOUND, USERNAME_EXISTS, ACCOUNT_EXISTS |
| 3000-3999 | 帖子 | POST_NOT_FOUND, POST_NO_EDIT |
| 4000-4999 | 评论 | COMMENT_NOT_FOUND, COMMENT_REPLY_DEPTH |
| 5000-5999 | 上传 | FILE_TOO_LARGE, FILE_TYPE_INVALID, FILE_UPLOAD_FAILED |
| 6000-6999 | 校验 | PARAM_INVALID, PARAM_MISSING |
| 7000-7999 | 内容安全 | CONTENT_REJECTED, REPORT_DUPLICATED, CONTENT_NOT_PENDING |
| 9000-9999 | 系统 | SYSTEM_ERROR, RATE_LIMITED |

### 5.4 认证机制

```
注册 → BCrypt 哈希密码 → 写入 users（用户名唯一，手机号/邮箱可选绑定且唯一） → 签发 TokenPair
登录 → 按用户名/手机号/邮箱定位账号 → BCrypt 比对 → 签发 TokenPair

access_token:  JWT (HS256), 30 分钟过期, payload={userId, role}
refresh_token: JWT (HS256), 7 天过期, payload={userId, type=refresh}，刷新时重新签发 TokenPair
```

### 5.5 API 契约

**已实现（✅）**：

```
公开标签：GET /api/v1/tags?limit=20  # 热门标签聚合（JSON_TABLE，公开帖口径：status=0 且 moderation_status=0；供 Feed 筛选与发布页选择）
认证：POST /api/v1/auth/{register,login,refresh,change-password}（change-password 需登录态；其余三者公开）；GET /api/v1/auth/captcha 图形验证码（注册必验，登录连续失败 3 次后要求；SVG + Redis 5 分钟一次性）
帖子：GET    /api/v1/posts             # 游标分页 + tag 过滤 + sort=new|hot + scope=all|following（G1：following 关注流，需登录）
      GET    /api/v1/posts/{id}        # G4：响应含 mentions[{nickname,userId}]（正文 @提及 用户，详情页实时解析，供前端高亮跳转）
      GET    /api/v1/posts/{id}/related?limit=6  # G3 相关推荐：同标签最新公开帖优先；不足时以最新公开帖补足；默认 6、最大 12，排除当前帖与已召回项
      POST   /api/v1/posts             # 发布（含内容审核；images 1-9 张；G4：正文 #话题# 自动并入 tags 且审核按合并后标签，@提及 上限 10 并通知被提及人；G12：productLink 白名单域名校验，非法 8008）
      PUT    /api/v1/posts/{id}        # 编辑（仅作者本人，保存后重新过审；已下架帖子禁止编辑；images 1-9 张；G4：同样执行话题并入与提及通知；G12：同样校验 productLink）
      DELETE /api/v1/posts/{id}        # 作者删除
      POST   /api/v1/posts/{id}/appeal # 发起申诉（仅已下架帖子的作者，待处理期间不可重复提交）
互动：POST   /api/v1/posts/{id}/like
      POST   /api/v1/posts/{id}/favorite   # G7：body 可选 {folderId}，未收藏时收藏到指定夹（缺省=默认夹）；已收藏则取消（忽略 folderId）
评论：GET    /api/v1/posts/{postId}/comments   # 评论树（sort=latest|hot；latest 支持 cursor 分页；G8：回复含 replyToId/replyToNickname 组装「回复 @昵称」）
      POST   /api/v1/posts/{postId}/comments   # G8：body 可选 replyToId 回复楼内任意评论（服务端推导楼根、越楼拒绝）
      PUT    /api/v1/comments/{commentId}       # 编辑评论（作者、5 分钟内，重新过审）
      DELETE /api/v1/comments/{commentId}
      POST   /api/v1/comments/{commentId}/like      # 点赞/取消点赞（幂等切换）
      POST   /api/v1/comments/{commentId}/favorite  # 收藏/取消收藏（幂等切换）
举报：POST   /api/v1/reports
私信：GET    /api/v1/conversations           # 会话列表（对方信息/最后消息/未读/在线状态 peerOnline）
      POST   /api/v1/conversations           # 发起/复用会话（对方关注我后可发起）
      GET    /api/v1/conversations/{id}/messages  # 消息（afterId 增量 / beforeId 历史）
      POST   /api/v1/conversations/{id}/messages  # 发送消息（敏感词拦截）
      POST   /api/v1/conversations/{id}/read      # 标记已读
上传：POST   /api/v1/upload/image
      DELETE /api/v1/upload/image?objectName=
用户：GET    /api/v1/users/me          # 当前登录用户资料
      PUT    /api/v1/users/me          # 更新当前用户资料
      GET    /api/v1/users/{userId}    # 指定用户公开资料（G6：响应含 blockedByMe 拉黑状态）
      GET    /api/v1/users/{userId}/posts  # 用户公开帖子（游标分页）
      GET    /api/v1/users/me/posts    # 我的帖子（含待审/未通过，仅本人）
      GET    /api/v1/users/me/comments # 我的评论（含待审/未通过，仅本人，游标分页）
      GET    /api/v1/users/me/favorite-comments # 我收藏的评论（仅本人，游标分页）
      GET    /api/v1/users/{userId}/favorites  # 用户收藏的公开帖（按收藏时间倒序；G7：本人可传 folderId 按夹浏览，0=默认夹、不传=全部）
      GET    /api/v1/users/{userId}/likes      # 用户赞过的公开帖（按点赞时间倒序）
      GET    /api/v1/users/{userId}/followers  # 粉丝列表（游标分页，含互关标识）
      GET    /api/v1/users/{userId}/following  # 关注列表（游标分页，含互关标识）
收藏夹：GET    /api/v1/users/me/folders   # 我的收藏夹列表（含 postCount 聚合，仅本人）
      POST   /api/v1/users/me/folders   # 创建（名称 trim 后 1-30 字符，同用户不重名）
      PUT    /api/v1/users/me/folders/{folderId}   # 重命名（仅本人，重名拒绝）
      DELETE /api/v1/users/me/folders/{folderId}   # 删除（夹内收藏移回默认夹）
      POST   /api/v1/users/me/favorites/{postId}/move  # 移动收藏到指定夹（folderId 为空=默认夹；未收藏拒绝）
搜索：GET    /api/v1/posts/search        # 关键词搜索（标题/正文/标签/商品名，G5：ES 相关性排序 + highlights 高亮片段，ES 不可用降级 MySQL LIKE）
      GET    /api/v1/posts/search/hot-tags
      GET    /api/v1/posts/search/suggest
关注：POST   /api/v1/users/{userId}/follow   # 切换关注
拉黑：POST   /api/v1/users/{userId}/block   # 拉黑用户（G6：幂等；内容互不可见 + 禁私信 + 自动双向取关）
      DELETE /api/v1/users/{userId}/block   # 解除拉黑（幂等；不恢复已解除的关注）
      GET    /api/v1/users/me/blocks        # 我的拉黑列表（按拉黑时间倒序）
创作：GET    /api/v1/users/me/creator-stats?days=7|30  # 创作数据总览（G9：仅本人；days 仅 7/30，其他归一化 7）
运营：GET    /api/v1/banners            # 启用中的 Banner（排序升序；G10）
      GET    /api/v1/topics             # 启用中的话题（含帖子数聚合；G10）
      GET    /api/v1/topics/{id}        # 话题详情（停用返回 8003）
      GET    /api/v1/posts?sort=featured # 精选流（G10：公开+过审+精选，游标分页）
      GET    /api/v1/admin/banners      # 全部 Banner（管理）
      POST   /api/v1/admin/banners      # 创建（标题查重 8002、link_type 白名单 post|topic|url）
      PUT    /api/v1/admin/banners/{id} # 更新（含启用/停用）
      DELETE /api/v1/admin/banners/{id}
      GET    /api/v1/admin/topics       # 全部话题（含帖子数）
      POST   /api/v1/admin/topics       # 创建（重名 8004）
      PUT    /api/v1/admin/topics/{id}  # 更新（含启用/停用）
      DELETE /api/v1/admin/topics/{id}
      GET    /api/v1/admin/posts?featured=0|1  # 帖子精选管理列表（cursor 分页）
      PUT    /api/v1/admin/posts/{postId}/featured  # 设置/取消精选（仅审核通过帖，否则 8005）
通知：GET    /api/v1/notifications    # type=like|comment|follow|system|mention|reply（G4：mention=帖子中 @提及 了你；G8：reply=有人回复了你的评论）
      GET    /api/v1/notifications/unread-count
      POST   /api/v1/notifications/mark-all-read
分析：POST   /api/v1/analytics/events     # 前端埋点上报
      GET    /api/v1/analytics/{overview,stats,top-posts,trend}   # 管理员看板
SEO： GET    /sitemap.xml
管理：GET    /api/v1/admin/moderation/{posts,comments}
      GET    /api/v1/admin/reports
      GET    /api/v1/admin/pending-counts   # 待办计数（待审帖/评/举报，看板用）
      GET    /api/v1/admin/users            # 用户列表（搜索/状态筛选/分页）
      POST   /api/v1/admin/users/{id}/ban|unban   # 封禁用户（理由）/ 解封
      GET    /api/v1/admin/users/{id}/posts # 用户帖子（支持 search/moderationStatus 筛选，管理维度）
      GET    /api/v1/admin/posts/{id}       # 管理端帖子详情（含封禁/待审内容，供预览）
      POST   /api/v1/admin/posts/{id}/ban|unban   # 封禁帖子 / 解封
      POST   /api/v1/admin/moderation/posts/{postId}
      POST   /api/v1/admin/moderation/comments/{commentId}
      POST   /api/v1/admin/reports/{reportId}   # 处理举报（ACCEPT 采纳下架 / DISMISS 驳回；可选 banAuthor=true 同时封禁作者）
      GET    /api/v1/admin/appeals          # 申诉列表（待处理优先，状态筛选）
      POST   /api/v1/admin/appeals/{id}     # 处理申诉（APPROVE 通过并解封 / REJECT 驳回）
      GET    /api/v1/admin/audit-logs       # 审计日志（管理员操作全记录，actions 逗号分隔筛选）
      GET    /api/v1/admin/tags             # 标签聚合统计（keyword 过滤，按使用量倒序）
      POST   /api/v1/admin/tags/rename      # 重命名标签 { source, target }
      POST   /api/v1/admin/tags/merge       # 合并标签 { source, target }（同帖去重）
      POST   /api/v1/admin/tags/delete      # 删除标签 { source }
公告：GET    /api/v1/announcements/latest  # 最近一条已发布公告（公开，无需登录；无则 data 为 null）
      GET    /api/v1/admin/announcements   # 全部公告（管理）
      POST   /api/v1/admin/announcements   # 创建（status 仅 0草稿/1已发布；新建拒绝 2）
      PUT    /api/v1/admin/announcements/{id}   # 更新（草稿→已发布时广播；8006 不存在）
      PUT    /api/v1/admin/announcements/{id}/offline  # 下线（status=2）
      DELETE /api/v1/admin/announcements/{id}
实时：WS    /ws（STOMP over 原生 WebSocket；CONNECT 帧 Authorization Bearer 鉴权，无 token 拒绝）
      订阅  /user/queue/notifications（通知事件）/ user/queue/messages（私信消息）/ topic/announcements（公告广播）
健康：GET    /api/v1/health
```

> 写接口通过 `@RateLimit` 注解（Redis Lua 滑动窗口）限流，超限返回 429；登录失败尝试由 `LoginRateLimiter` 限制（15 分钟 5 次）。

**正文 @提及 + #话题# 解析（G4）**：

- `MentionExtractor`（common/util）统一解析规则，预编译 Pattern：`@昵称`（2-20 位中英文数字下划线连字符，负向 lookbehind `(?<![\w@])` 防邮箱误匹配）与 `#话题#`（1-20 位非 # 非空白字符）；提及去重上限 10、话题去重上限 8。
- 发布/编辑时话题自动并入 posts.tags：`mergeTags` 手选标签在前、话题去重追加，总上限 8；内容审核 evaluate 使用合并后标签文本。
- 提及用户解析：`UserMapper.selectActiveUsersByNicknames` 批量查活跃用户（status=0）；发布/编辑后对被提及人发 mention 通知（`NotificationService.notifyMention`，content=标题截 50、targetType=post、@自己过滤）；`GET /posts/{id}` 实时解析 mentions 返回，不做落库冗余。
- 前端：详情页正文分段渲染（`renderContentSegments` 纯函数，规则与后端一致）——话题点击跳 `/?tag=` 话题 Feed、提及仅当命中 mentions 时跳 `/user/{userId}`（未命中仅高亮不跳转）；Feed 从 URL `?tag=` 初始化标签筛选且点击标签同步 URL（话题页可分享/可刷新，冷门话题自动追加进标签栏）。

**规划（未实现）**：

```
标签：GET /api/v1/tags、POST/DELETE /api/v1/tags/{name}/favorite
认证扩展：POST /api/v1/auth/{logout,wechat,sms}
私信：/api/v1/chat/*
管理扩展：/api/v1/admin/users/*、/api/v1/admin/tags/*
```

### 5.6 日志与可观测性架构

#### 5.6.1 主流方案对比

| 方案 | 组件 | 存储成本 | 运维复杂度 | 适用场景 |
|------|------|---------|-----------|---------|
| **ELK** | Logstash + Elasticsearch + Kibana | 高 | 高（分片/索引管理） | 大型企业、全文搜索需求 |
| **EFK** | Fluentd/Fluent Bit + ES + Kibana | 高 | 中高 | K8s + 需 ES 查询能力 |
| **PLG** | Promtail/Alloy + Loki + Grafana | 低 | 低 | K8s 环境、已有 Grafana 生态 |
| **OpenTelemetry** | OTel Collector + 混合后端 | 中 | 中 | 微服务、端到端链路追踪 |
| **SaaS** | Datadog/New Relic/Splunk | 极高（按量计费） | 极低 | 预算充足、团队小 |
| **云原生** | CloudWatch/Cloud Logging | 中 | 低 | 单一云平台 |

#### 5.6.2 选型决策

本项目采用 **PLG + OpenTelemetry 混合方案**：

```
Spring Boot App
├── Logback JSON + MDC (trace_id, request_id, user_id)
└── OTel Java Agent (自动注入 span)
         │
         ▼
   Grafana Alloy (采集层)
   ├── 日志：读取容器 stdout / 文件
   ├── 指标：抓取 /actuator/prometheus
   └── 链路：接收 OTLP traces
         │
    ┌────┼────┐
    ▼    ▼    ▼
  Loki  Prom  Tempo    ← 存储层
  (日志)(指标)(链路)
    └────┼────┘
         ▼
      Grafana          ← 统一查询面板
```

**选型理由**：
- **成本低**：Loki 只索引标签（不索引日志内容），存储比 ES 便宜 10-50 倍
- **运维轻**：单二进制部署，无需分片管理
- **一体化**：日志、指标、链路在一个 Grafana 面板查看，trace_id 串联全链路
- **扩展性**：OTel Agent 可随时接入更多后端，不锁定厂商

#### 5.6.3 应用层日志规范

**后端 Java**：

```
日志格式（dev）：  HH:mm:ss.SSS LEVEL [trace_id] [request_id] logger | message
日志格式（prod）： 单行 JSON（LogstashEncoder），含 timestamp/level/logger/message/trace_id/request_id/user_id

级别策略：
  com.buyershow.service     → INFO   业务操作（发帖/登录/审核/举报）
  com.buyershow.mapper      → WARN   仅记录慢 SQL 与异常
  org.springframework       → WARN   关闭框架噪音
  org.flywaydb              → INFO   迁移必须可见
  MyBatis StdOutImpl        → dev only，prod 用 NopLogging
```

**MDC 上下文**（每个 HTTP 请求自动注入）：

| 字段 | 来源 | 说明 |
|------|------|------|
| trace_id | W3C traceparent header 或自动生成 | 跨服务链路追踪 |
| request_id | UUID 生成 | 单次请求唯一标识 |
| user_id | Spring Security 上下文 | 当前认证用户（匿名不写入） |

**前端 React**：

```
HTTP 层：开发环境 console.log 自动输出 [HTTP] METHOD URL → STATUS (ms)
ErrorBoundary：捕获渲染崩溃，输出组件栈 + 错误信息
生产环境：HTTP 日志静默，ErrorBoundary 错误可上报 Sentry（SDK 已集成，DSN 懒加载零首屏成本，ErrorBoundary 接线待办）
```

#### 5.6.4 分阶段落地计划

| 阶段 | 内容 | 复杂度 | 状态 |
|------|------|--------|------|
| Phase 1 | Logback JSON + MDC Filter + 前端 ErrorBoundary + HTTP 日志 | 低（纯代码） | 已实现 |
| Phase 2 | Loki + Alloy Docker Compose 部署 | 中（加 3 个容器） | 规划中 |
| Phase 3 | OTel Java Agent 自动注入 span | 中（加 JVM 参数） | 规划中 |
| Phase 4 | 前端 Sentry 错误上报 | 中（接入 SDK） | SDK 已接入（DSN 懒加载），ErrorBoundary 接线待办 |

#### 5.6.5 日志查询示例（Loki LogQL）

```logql
{app="buyer-show", level="ERROR"}                    # 所有错误日志
{app="buyer-show", trace_id="a1b2c3d4"}              # 某次请求全链路
{app="buyer-show"} |= "创建帖子" | json              # 搜索发帖操作
{app="buyer-show", user_id="42"}                     # 某用户所有操作
sum(rate({app="buyer-show", level="ERROR"}[5m]))     # 错误率趋势
```

### 5.7 状态与角色枚举

所有状态和角色字段统一使用 Java 枚举管理，消除魔法数字。数据库存储为 TINYINT，Java 层通过 `getValue()` 获取整数值。

| 枚举类 | 值 | 含义 | 用途 |
|--------|------|------|------|
| **PostStatus** | PUBLIC(0) | 公开 | 帖子可见状态 |
| | HIDDEN(1) | 隐藏 | 作者主动隐藏 |
| | DELETED(2) | 删除 | 软删除 |
| **CommentStatus** | ACTIVE(0) | 正常 | 评论可见状态 |
| | DELETED(1) | 删除 | 软删除 |
| **ModerationStatus** | APPROVED(0) | 审核通过 | 内容审核结果 |
| | PENDING(1) | 待人工审核 | 自动审核未通过，等待人工 |
| | REJECTED(2) | 审核驳回 | 内容违规 |
| **UserRole** | USER(0) | 普通用户 | JWT payload 与权限判断 |
| | ADMIN(1) | 管理员 | 管理端接口鉴权 |
| **UserStatus** | ACTIVE(0) | 正常 | 账号可用状态 |
| | BANNED(1) | 封禁 | 禁止登录 |
| | DELETED(2) | 注销 | 账号已注销 |

**使用规范**：
- Service 层一律使用枚举比较（`post.getStatus() == PostStatus.DELETED.getValue()`），禁止硬编码数字
- 新建状态字段必须定义枚举，`common/` 包下维护
- 枚举 `getValue()` 返回 TINYINT 值，与数据库 schema 对齐

### 5.8 业务缓存（P1.2）

Spring Cache 注解 + Redis CacheManager（`RedisConfig`），按 cache name 独立 TTL；写操作主动失效 + 短 TTL 兜底，缓存不可用时降级直查（CacheErrorHandler 默认吞异常）。

| cache name | TTL | 内容 | 失效策略 |
|------|------|------|---------|
| `user:profile` | 10 分钟 | 用户公开资料（昵称/头像/统计） | 资料更新/头像变更时按 userId 精准失效 |
| `post:detail` | 5 分钟 | 帖子详情 | 帖子更新/删除时失效 |
| `rate:login` | 15 分钟 | 登录失败计数 | 登录成功清除 |
| `feed:anonymous` | 60 秒 | 匿名 Feed 首屏（无游标第一页） | 13 个写点 allEntries 失效（发帖/删帖/编辑/审核/举报处置/用户封禁/用户解封/帖子封禁/帖子解封/申诉处置/标签改名/标签合并/标签删除） |

**`feed:anonymous` 设计要点（P1.2 核心）**：

- **仅匿名用户 + 无游标首屏 + 全站 scope 命中**：`@Cacheable(condition = "getCurrentUserId() == null && #cursor == null && (#scope == null || #scope.equals('all'))")`——登录用户 Feed 含 isLiked/isFavorited 用户维度、翻页游标不稳定，均不缓存；关注流（G1，scope=following）需登录且内容因人而异，亦不缓存（condition 白名单防串流）；key 为 `tag|sort|limit`（`CursorUtils.feedCacheKey`）。
- **一致性**：Feed 读路径不缓存用户维度数据；所有可能改变 Feed 可见集合的写点统一 `@CacheEvict(allEntries = true)`（多缓存同步失效用 `@Caching(evict = {...})`，`@CacheEvict` 非可重复注解），另设 60s TTL 兜底（缓存驱逐/漏改注解时不超 1 分钟旧数据）；点赞/评论/收藏仅改计数不改变 Feed 集合，不主动失效（高频写放大），计数漂移由 60s TTL 兜底。
- **序列化兼容**：缓存值经 `GenericJackson2JsonRedisSerializer`（类型信息 + JavaTimeModule）序列化，被缓存的 DTO（`CursorPage`、`PostDTO`）必须带无参构造器（`@NoArgsConstructor` + `@AllArgsConstructor`），否则命中时抛 `InvalidDefinitionException`；`RedisCacheSerializerTest` 做序列化往返回归（非空列表 + 空列表两用例）防回归。

**关注流（G1）查询设计**：`PostMapper.selectFollowingFeedRows` 用 `SELECT STRAIGHT_JOIN` 强制以 follows 子查询（`idx_follow_follower_feed` 索引）为驱动表，关联主表走 `idx_post_feed`，避免关注列表膨胀时优化器误选扫描；`scope` 参数在 `PostService.getFeed` 内分支（`all` 走原 Feed 查询，`following` 先 `requireCurrentUserId` 校验登录态，未登录返回 1003）。

### 5.9 搜索索引（G5 ES 接入）

**链路**：发帖/编辑/删除/审核/封禁等写点发布索引事件（`PostIndexEvents`，事务内 `publishEvent`）→ `PostSearchIndexService` 监听器（`@TransactionalEventListener(phase = AFTER_COMMIT, fallbackExecution = true)` + `@Async`）在**事务提交后**异步写 ES。搜索 `GET /posts/search` 走 ES `multi_match`（title^3 / tags^2 / productName^1.5 / content^1，minimumShouldMatch 60% + userStatus=0 过滤）→ 按 ES 打分顺序回查 MySQL（二次过滤 + isLiked/isFavorited 组装）→ 结果附 `highlights`（`<em>` 标记片段，前端拆分渲染防注入）；ES 异常自动降级 MySQL LIKE（降级路径同样生成高亮片段，前端体验一致）。

| 设计点 | 方案 |
|------|------|
| 索引名/口径 | `posts` 索引；仅公开且审核通过帖入索引（`status=0 AND moderation_status=0`，`PostMapper.selectIndexRow` 口径），用户状态入索引查询期过滤封禁用户 |
| 中文分词 | 自定义 `ngram_search` 分析器（min_gram=1 / max_gram=2），实现 1-2 字符词元的中文子串匹配 |
| 排序 | `_score` 相关性排序 + `postId` 数字字段同分 tiebreaker（新帖优先，与 MySQL 路径 id desc 一致） |
| 一致性 | AFTER_COMMIT 事件保证索引线程读到已提交数据；写入失败仅告警不阻断主流程，最终一致 |

**踩坑记录（spring-data-elasticsearch 5.3.6）**：

- `@Async` 直接调用会与事务竞态：异步线程在事务提交前读库读不到新帖，文档静默漏写 → 必须 AFTER_COMMIT 事件。
- `LocalDateTime` 字段往返转换不可靠：写路径 conversion chain 会降级为 `LocalDate→String`（`2026-09-21`），读回 `LocalDateTime` 失败导致搜索全部降级（`MappingConversionException` 消息中的数字只是 document id）→ `createdAt` 存 ISO 字符串 + `Keyword` mapping。
- `@Id` 字段在 ES mapping 中强制 keyword（字典序），数值排序不可靠 → 另存 `postId` Long 字段做 tiebreaker。
- 5.3.6 API 细节：`IndexOperations` 位于 `core` 包（非 `.index` 子包）；`Settings` 用 `new Settings(Map)` 构造（无 builder）；高亮用 `withHighlightQuery(new HighlightQuery(...))`（无 `withHighlight`）。

### 5.10 拉黑屏蔽（G6）

**链路**：`POST /users/{userId}/block` → `UserBlockService.block`（事务内：写 `user_blocks` + 双向删除 follows 并同步双方计数；重复拉黑幂等短路）→ 全站公开查询统一追加双向拉黑过滤实现内容互不可见；私信在发起与发送两处校验任意方向拉黑即拒绝（既有会话历史保留，仅禁止新消息）。

| 设计点 | 方案 |
|------|------|
| 表结构 | `user_blocks(blocker_id, blocked_id)`，唯一键 `uk_blocker_blocked` 防重复 + `idx_blocked` 反向索引（V13） |
| 过滤口径 | 拉黑后**双向不可见**：Feed/关注流/热门/搜索（MySQL 降级）/相关推荐/用户主页帖子/收藏/赞过/详情/评论/粉丝/关注列表/ES 回查共 13 处公开查询统一追加 `NOT EXISTS` 双向条件（`ub.blocker_id = 作者 AND ub.blocked_id = 当前用户` + 反向） |
| 私信限制 | `ConversationService` 在 `startConversation` 与 `sendMessage` 均调 `requireNotBlocked`（任意方向存在拉黑即拒绝），与既有「对方关注我才能发起」规则叠加 |
| 关注副作用 | 拉黑时双向取关（`removeFollowDirection` 两个方向）并原子同步 follower/following 计数；解除拉黑**不恢复**关注 |
| 响应字段 | `GET /users/{userId}` 响应新增 `blockedByMe`，前端据此在主页/评论/会话内切换拉黑/解除入口 |

> 设计取舍：拉黑只做「隐藏」不做删除——帖子/评论/关注/点赞记录全部保留，解除拉黑即恢复可见，避免删除补偿与计数回滚的复杂度；个人帖子接口 `GET /users/me/posts`（仅本人）不过滤。

### 5.11 收藏夹分类（G7）

**链路**：详情页未收藏时点击收藏 → 弹出收藏夹选择器（选夹/新建）→ `POST /posts/{id}/favorite` 携带 `{folderId}`（缺省=默认夹）；已收藏时点击直接取消。本人主页收藏 tab 顶部展示夹 chips（全部/默认收藏夹/各夹+收藏数）与「管理」入口，管理弹窗支持新建/内联重命名/删除。

| 设计点 | 方案 |
|------|------|
| 表结构 | `favorite_folders(id, user_id, name, created_at, updated_at)` 唯一键 `uk_user_name`（同用户不重名）；`favorites.folder_id` 可为 NULL（=默认夹），`idx_user_folder_feed` 支撑按夹浏览（V14） |
| 默认夹语义 | 「默认收藏夹」不落库（folder_id IS NULL）；浏览接口 folderId=0 表示默认夹、>0 指定夹、不传=全部 |
| 收藏切换 | `PostService.toggleFavorite(postId, folderId)`：已收藏则取消（忽略 folderId）；未收藏时校验 `requireOwnFolder` 后 `INSERT IGNORE` 带 folder_id |
| 移动/删除 | `moveFavorite`：未收藏拒绝（POST_NOT_FOUND），目标夹校验归属；`deleteFolder`：先 `clearFolder` 夹内收藏置回 NULL，再删夹 |
| 权限校验 | `requireOwnFolder`（收藏夹存在且属于当前用户，NO_PERMISSION）供收藏、移动、重命名、删除复用；重名由 `uk_user_name` 唯一键 + `DuplicateKeyException` 捕获转 PARAM_INVALID |

> 设计取舍：删除夹不删除收藏——夹内收藏移回默认夹，避免误删用户数据；收藏是双向幂等切换（已收藏→取消），与点赞交互一致。

### 5.12 评论盖楼（G8）

**链路**：评论树保持两级结构（顶级评论 + 楼内回复），`comments.reply_to_id` 指向被回复评论实现「回复的回复」；回复任意评论时服务端自动推导楼根（`parentId` 恒为楼根、`replyToId` 为被回复对象），越楼/回复已删除评论拒绝；回复他人评论触发 `reply` 通知（被回复人=帖子作者时不重复发）。

| 设计点 | 方案 |
|------|------|
| 表结构 | `comments.reply_to_id BIGINT UNSIGNED NULL`（NULL=直接回复楼主），无外键（V15） |
| 楼根推导 | `resolveReplyTarget`：replyToId 为空走既有路径（parentId 即楼根）；非空则校验目标同帖有效，`rootId = target.parentId ?? target.id`，与请求 parentId 交叉校验（不一致即越楼拒绝 COMMENT_NOT_FOUND） |
| 树组装 | `selectVisibleReplies` LEFT JOIN 被回复评论与用户，DTO 附 `replyToId`/`replyToNickname`；被回复人已删除时昵称为 null，前端降级为普通文案 |
| 通知 | `reply` 类型：被回复人 ≠ 自己且 ≠ 帖子作者时发（与 comment 通知去重）；老客户端仅传 parentId 回复楼根时被回复人即楼根作者 |
| 前端 | 所有评论层级显示「回复」按钮；回复内容前高亮「回复 @昵称：」；楼中楼默认展示前 3 条、超出折叠「展开 N 条回复」 |

> 设计取舍：不引入三级树（path 列），保持「顶级评论 + 扁平楼内回复」结构——树高固定两层，查询与计数逻辑不变（reply_count 始终计在楼根）；回复对象删除后历史回复保留（LEFT JOIN 降级展示），不做级联清理。

---

### 5.13 创作数据（G9 创作者中心）

**链路**：个人主页「创作数据」入口（仅本人）→ `GET /users/me/creator-stats?days=7|30` 聚合返回 summary/trend/topPosts → 前端概览卡片 + 纯 CSS 柱状图（指标切换）+ Top5 榜单；复用 V6 `analytics_events` 埋点基建，无新表无迁移。

| 设计点 | 方案 |
|------|------|
| 数据口径 | summary：postCount=全部未删除帖子数；totalLikes/totalFavorites/totalComments=posts 表 Σ 累计值；totalViews=近 N 天 post_view 埋点求和（阅读量唯一来源）；trend=近 N 天逐日埋点；topPosts=近 N 天浏览 Top5 + 累计赞藏 |
| 趋势聚合 SQL | `analytics_events` JOIN `posts`（`p.user_id=#{userId}` 仅本人帖）按天 CASE WHEN 分组；点赞/收藏仅计正向事件（`JSON_UNQUOTE(JSON_EXTRACT(metadata,'$.liked'))='true'`），取消事件自然排除 |
| Top 帖 SQL | posts LEFT JOIN 近 N 天 post_view 计数子查询，`ORDER BY views DESC, id DESC LIMIT 5`；待审/未通过帖也纳入（本人视角） |
| 窗口与补零 | days 仅 7/30 合法（其他归一化 7）；服务端按窗口逐日补零（缺日各指标为 0），前端柱状图直出；未登录 401，仅本人可见（阅读量属创作者敏感运营数据） |
| 前端 | `/profile/creator-stats` 直链页（ProtectedRoute）：近 7/30 天切换、5 张概览卡、阅读/点赞/收藏/评论指标切换柱状图（hover 显示数值）、Top5 点击跳帖子详情 |

> 设计取舍：不建每日汇总表（V6 `analytics_daily_stats` 未启用）——当前规模聚合 SQL 直接查 `idx_event_type(event_type, created_at)` 即可，避免离线任务链路；点赞/收藏累计用 posts 表计数（真实操作驱动、天然准确），趋势用埋点（窗口反馈），两者口径分离由前端 hint 文案（「累计」/「近 N 天」）明示。

### 5.14 运营位与话题（G10）

**链路**：管理后台「运营管理」（Banner 运营位/精选管理/话题管理三 Tab）→ 首页 Banner 横滑 + Feed「精选」档 + 话题广场/话题聚合页；V16 迁移新增 `site_banners`、`topics` 两表与 `posts.is_featured` 精选标记。

| 设计点 | 方案 |
|------|------|
| Banner 位 | `site_banners`（title 查重 8002、link_type 白名单 post\|topic\|url、status 0启用1停用、sort_order 升序）；公开端 `GET /banners` 仅返回启用位；点击跳转：post→`/posts/{id}`、topic→`/topics/{id}`、url→新窗口（仅 https?:// 放行） |
| 精选流 | `posts.is_featured` + `idx_featured(is_featured, id)`；Feed `sort=featured` 走 `FORCE INDEX (idx_featured)` 查询（仅公开+过审+精选，游标分页）；打标仅限审核通过帖（否则 8005），取消打标不校验审核态 |
| 运营话题 | `topics`（name UNIQUE uk_name、status、sort_order、cover/description）；话题名=帖子标签同名关联，帖子数由 `JSON_TABLE(posts.tags)` 聚合；话题聚合页复用 `GET /posts?tag=话题名` 零新查询；停用话题仅影响话题入口，同名标签 Feed 不受影响 |
| 权限与安全 | 管理端全部挂 `/api/v1/admin` 前缀（hasRole ADMIN 既有规则）；公开读 `GET /banners`、`GET /topics/**` 加入 SecurityConfig permitAll；创建/更新请求 @Valid + 服务层二次校验（长度/白名单/重名） |
| 前端 | `/topics` 话题广场 + `/topics/{id}` 聚合页（TanStack 无限滚动复用 getFeed）；`/admin/operations` 三 Tab（Banner CRUD/精选打标列表/话题 CRUD）；Feed SORT_OPTIONS 增「精选」档；Banner 仅「最新+全部」档展示 |

> 设计取舍：话题不建独立帖子关联表——话题名与标签同名，「话题下的帖子」即「带该标签的帖子」，聚合页零新查询复用标签 Feed；精选打标不做审批流（管理端操作即生效，审计日志覆盖）。

### 5.15 WebSocket 实时推送与系统公告（G11）

**链路**：业务写点（通知/私信/公告）→ RabbitMQ fanout 事件总线 → 本实例 STOMP 监听器 → `/ws` 推送前端；V17 迁移新增 `announcements` 表；前端 STOMP 客户端（@stomp/stompjs）+ 全局公告横幅 + 运营后台「系统公告」Tab。

| 设计点 | 方案 |
|------|------|
| 事件总线 | exchange `buyershow.events`（fanout）+ 每实例 `AnonymousQueue`（auto-delete）绑定——消息广播到全部在线实例，多实例水平扩展零配置；单一事件类 `RealtimeEvent{type,userId,notificationId,message,announcement}`，Jackson2JsonMessageConverter（复用 Spring ObjectMapper，日期格式与 REST 一致） |
| 事件类型 | `notification.created`（轻量事件，前端刷未读计数+重拉列表）、`message.sent`（完整 MessageDTO，前端直接插入会话）、`announcement.published`（完整 AnnouncementDTO，广播 `/topic/announcements`） |
| STOMP 端点 | `/ws` 原生 WebSocket（不引 SockJS）；simple broker `/topic`（广播）+`/queue`（点对点）；user destination 前缀 `/user`，用户队列 `/user/{userId}/queue/notifications|messages` |
| 握手鉴权 | `WebSocketAuthChannelInterceptor`（ChannelInterceptor）：CONNECT 帧 `Authorization: Bearer <accessToken>` 复用 JwtTokenProvider 校验，通过后 `setUser(StompPrincipal)`；失败回 ERROR 帧关闭连接；SecurityConfig HTTP 层仅 permitAll 放行升级 |
| 业务接入 | NotificationService 6 类通知（like/comment/follow/system/mention/reply）与 ConversationService.sendMessage 落库后同步发布事件（事务内直发，失败吞异常由轮询兜底） |
| 容错取舍 | MQ 发送失败静默降级：前端保留 15s 未读轮询 + 5s 会话增量轮询作兜底（幂等校准）；STOMP 断线 `reconnectDelay 3s` 自动重连 + 心跳 10s；`beforeConnect` 动态取最新 accessToken（401 过期不影响重连） |
| 系统公告 | `announcements`（title≤100/content≤2000/status 0草稿1已发布2已下线）；仅「草稿/新下线→已发布」状态迁移时广播，重复保存不重复推送；已下线公告 latest 不再返回；公开 `GET /announcements/latest` 无需登录 |
| 前端 | `services/realtime.ts` 单例（connect/disconnect/onNotification/onMessage/onAnnouncement）；全局挂载 RealtimeConnector（登录后连接、登出断开）+ AnnouncementBanner（latest 拉取 + 实时广播 + localStorage 关闭记忆 `buyer-show.closed-announcement`）；消息页 onMessage 直接插入当前会话；运营后台第 4 Tab 公告面板（CRUD/下线/删除确认/草稿/立即发布） |

> 设计取舍：事件总线选 RabbitMQ 而非直连 WebSocket 广播——通知写入与推送解耦（事务内不发 MQ 消息不阻塞业务）、为多实例部署铺路；推送 payload 通知仅发轻量事件（userId+notificationId），列表内容由前端按需重拉，避免推送通道承担 REST 序列化成本。

### 5.16 电商闭环·购买链接（G12）

**链路**：发布/编辑页商品卡「购买链接（可选）」→ 服务端白名单域名校验 → 详情页商品卡「去购买 ↗」新窗口跳转；V18 迁移 `posts.product_link`。

| 设计点 | 方案 |
|------|------|
| 数据模型 | `posts.product_link VARCHAR(500)`（V18）；可选字段，空白落库统一 null |
| 域名白名单 | `ProductLinkValidator`（common/util）：仅 http/https 且主机名命中 `taobao.com/tmall.com/jd.com/pinduoduo.com/yangkeduo.com`（含子域）；后缀匹配「完全相等或以 `.主域` 结尾」防 `nottaobao.com`/`taobao.com.evil.com` 前缀与尾随欺骗；`getHost()` 解析用户信息与端口；非法返回 8008 |
| 业务接入 | PostService 发布/编辑均先校验后落库（非法即拒绝，不产生待审残留）；productLink 纳入内容审核 evaluate 文本（防链接文本携带敏感内容） |
| 前端 | 发布页商品卡底部「购买链接（可选）」输入（type=url，placeholder 提示白名单平台，草稿随存随恢复）；详情页商品卡有链接时渲染珊瑚色「去购买 ↗」主按钮（`target=_blank rel=noopener noreferrer`），无链接保持「复制商品信息」既有交互 |
| 防钓鱼边界 | 链接仅能通过发布/编辑请求进入系统（无任何 URL 跳转代理）；详情页 `<a>` 直接跳转目标站，站内不做重定向（避免 open redirect 面） |

> 设计取舍：白名单在服务端强校验（前端 type=url 仅为体验优化，不构成防线）；v2 CPS 联盟返利（链接替换为带返利参数的跳转链）预留 productLink 长度 500 与白名单可扩展结构，不建独立跳转服务。

---

## 六、数据库设计（MySQL 8.0）

### 6.1 表清单

| 表名 | 用途 | 迁移 |
|------|------|------|
| users | 用户（昵称/头像/简介/统计/角色/状态；手机号/邮箱唯一） | V1 建表，V2 约束优化，V12 最后登录时间 |
| posts | 帖子（标题/正文/图片 JSON/标签 JSON/商品信息/计数；V16 精选标记 is_featured；V18 购买链接 product_link） | V1 建表，V2 Feed/热门/用户索引，V4 审核字段，V5 加固，V16 精选标记，V18 购买链接 |
| comments | 评论（一级嵌套 `parent_id` + `reply_count`；G8：`reply_to_id` 楼内回复对象） | V1 建表，V2 稳定排序索引，V4 审核字段，V5 加固，V15 回复对象字段 |
| likes | 帖子点赞（`UNIQUE user+post`） | V1 建表，V2 双向查询索引 |
| favorites | 帖子收藏（`UNIQUE user+post`；G7：`folder_id` 可空指向收藏夹） | V1 建表，V2 用户时间线索引，V14 收藏夹字段 |
| follows | 关注关系（follower/following） | V1 建表，V2 双向时间线索引 |
| tags | 标签字典（名称唯一 + post_count） | V1 建表，V2 校验/更新时间 |
| post_tag_rela | 帖子与标签多对多关系（高效筛选/统计） | V2 新增 |
| notifications | 通知（like/comment/follow/system/mention） | V1 建表，V2 时间线/未读索引 |
| favorite_comments | 评论收藏 | V1 建表 |
| favorite_tags | 标签收藏 | V1 建表 |
| search_history | 搜索历史（`UNIQUE user+keyword`） | V1 建表 |
| chat_conversation | 私信会话主表 | V2 新增 |
| chat_conversation_member_rela | 会话成员关系与未读计数 | V2 新增 |
| chat_message | 私信消息（文本/图片/商品卡片/系统） | V2 新增 |
| sensitive_words | 内容审核敏感词（action: 0=人工审核 1=直接拒绝） | V4 新增，V5 加固 |
| content_reports | 内容举报（`UNIQUE reporter+content_type+content_id`） | V4 新增，V5 加固 |
| user_blocks | 用户拉黑关系（`UNIQUE blocker+blocked`） | V13 新增 |
| favorite_folders | 收藏夹（`UNIQUE user+name`，收藏分类） | V14 新增 |
| site_banners | 站点 Banner 运营位（标题/图片/跳转类型/排序/启停） | V16 新增 |
| topics | 运营话题（名称唯一；与帖子标签同名关联聚合） | V16 新增 |
| announcements | 系统公告（标题/正文/状态 0草稿1已发布2已下线；idx_status_id） | V17 新增 |

> 完整 DDL 以 `server/src/main/resources/db/migration/` 迁移文件为准，本文档不重复粘贴。

### 6.2 迁移管理

使用 Flyway 管理数据库迁移：
- 文件位置：`server/src/main/resources/db/migration/`
- 命名规范：`V{N}__{description}.sql`；已执行迁移不可改写
- Spring Boot 启动时自动执行未跑过的迁移
- 新增 DROP 类迁移前必须核对库中实际状态（外键已由 V2 统一移除、审核索引由 V5 重建）；对不存在的对象执行 `DROP FOREIGN KEY` / `DROP INDEX` 会报 1091 并阻塞启动

| 版本 | 内容 |
|------|------|
| V1 | 初始化全部表结构（15 表） |
| V2 | 移除外键、重建索引、补充 `post_tag_rela` 与 `chat_*` 表 |
| V3 | Feed 主键游标优化（`(status, id DESC)`） |
| V4 | 内容审核：帖子/评论审核字段、`sensitive_words`、`content_reports` |
| V5 | 审核加固：默认待审、审核队列索引、举报唯一键收紧 |
| V6 | 埋点数据：`analytics_events` 事件表 |
| V7 | 申诉流程：`post_appeals` 帖子申诉表 |
| V8 | 评论点赞：`comment_likes` 表 |
| V9 | 评论编辑标记：`edited_at` 字段 |
| V10 | 审计日志：`admin_audit_log` 表（8 类管理员操作） |
| V11 | 私信会话：`conversations` + `messages` 表 |
| V12 | 用户表扩展（1.5）：`last_login_at` 最后登录时间 |
| V13 | 用户拉黑：`user_blocks` 表 |
| V14 | 收藏夹分类：`favorite_folders` 表 + `favorites.folder_id` 字段 |
| V15 | 评论盖楼：`comments.reply_to_id` 字段 |
| V16 | 运营位与话题：`site_banners` + `topics` 表 + `posts.is_featured` 精选标记（idx_featured） |
| V17 | 系统公告：`announcements` 表（status 状态机 + idx_status_id） |
| V18 | 电商闭环：`posts.product_link` 购买链接字段 |

V1-V18 已在 Docker 开发库顺序执行验证通过（Flyway 启动自动执行）。

### 6.3 当前有效结构与索引（V2/V3 治理结果）

| 表/模块 | 当前设计 | 核心索引或约束 |
|---------|---------|---------------|
| users | 用户名、昵称唯一；角色/状态/计数增加 CHECK；`last_login_at` 登录时更新（V12） | `username`、`uk_user_nickname` |
| posts | 图片和标签均为非空 JSON 数组；计数、价格、评分增加 CHECK；`is_featured` 精选标记（G10，默认 0） | `idx_post_feed(status, moderation_status, id DESC)`、`idx_post_hot`、`idx_post_user_feed`、`idx_featured(is_featured, id)` |
| comments | 一级嵌套由应用层校验；补 updated_at 与稳定排序；`reply_to_id` 指向楼内被回复评论（G8） | `idx_comment_post_feed`、`idx_comment_parent_feed`、`idx_comment_user_feed` |
| likes/favorites | `(user_id, post_id)` 唯一；补帖子反向查与用户时间线 | 唯一索引 + `idx_*_post` + `idx_*_user_feed` |
| follows | 防止自关注；双向列表按时间稳定排序 | `idx_follow_follower_feed`、`idx_follow_following_feed` |
| notifications | 同时支持全部时间线与未读列表 | `idx_notification_user_feed`、`idx_user_read` |
| post_tag_rela | JSON 标签用于展示，关系表负责高效筛选和统计 | `uk_post_tag`、`idx_tag_post`、`idx_tag_time_post` |
| chat_* | 会话、成员、消息三表支撑私信 | 会话键唯一、用户会话索引、会话消息索引 |
| user_blocks | 拉黑关系双向过滤（公开查询 `NOT EXISTS`）与私信限制 | `uk_blocker_blocked`、`idx_blocked` |
| favorite_folders | 收藏夹同用户重名唯一；按夹浏览用 `folder_id` 组合索引 | `uk_user_name`、`idx_user_folder_feed(user_id, folder_id, created_at)` |

**索引策略**：

1. Feed V3 使用 `(status, id DESC)`，以自增 `id` 作为不透明游标；它与发帖写入顺序一致，可做索引范围扫描且避免 filesort。
2. 登录用户 Feed 在单条 SQL 中通过 `EXISTS` 子查询返回点赞/收藏状态，均命中 `(user_id, post_id)` 唯一覆盖索引，消除应用层每页 `2N` 次查询。
3. Feed 列表不查询 `content` 大字段；详情页单独查询正文，降低网络传输和 InnoDB 回表成本。
4. 点赞、收藏、发帖数采用原子 SQL `SET count = GREATEST(count + delta, 0)`，避免并发读改写丢失。
5. 数据库不使用外键，按项目规范由 Service 事务和唯一约束维护一致性，降低高并发写入与迁移耦合。
6. `sys.schema_redundant_indexes` 审计结果为空，已移除 `users.idx_username` 等重复索引。

**当前 Feed 执行计划**：

- `posts` 使用 `idx_post_feed`，游标条件使用完整的 `status + id` key parts，无 filesort。
- `STRAIGHT_JOIN` 固定 posts 为驱动表，作者关联使用 users 主键。
- 点赞/收藏 `EXISTS` 使用 `(user_id, post_id)` 唯一覆盖索引单行查找。
- `using_filesort = false`。

---

## 七、部署与运行

### 冒烟测试

全栈启动后运行核心链路 API 冒烟（20 用例：健康/注册登录/发帖（含图片）/评论/点赞收藏/搜索/通知未读/私信全链路/管理端/权限边界）：

```bash
python3 scripts/smoke-test.py
```

可用 `SMOKE_API_BASE` 指向其他环境；退出码非 0 表示存在失败（可直接接入 CI）。

### 浏览器级 E2E（Playwright）

全栈启动后运行浏览器级 E2E（5 个 spec：登录态登出重登 / Feed 列表与详情浮层 / 评论点赞 / 发布→详情→删除全旅程 / 搜索）：

```bash
cd frontend && npm run test:e2e
```

- 目录：`frontend/e2e/`（`global-setup.ts` 注册专用测试账号、`specs/` 5 个用例、`fixtures/` 图片、`.auth/` 登录态产物，均已 gitignore）
- 自动拉起 5273 端口 vite dev（`--strictPort`），经 `/api` proxy 转发到 `API_PROXY_TARGET`（默认 8080，nginx 生产环境无此层）
- 登录态：setup project 将 token 写入 `storageState`，5 个 spec 共享一次注册的账号；注册图形验证码明文经 `redis-cli` 读 `captcha:{id}` 非侵入绕过（要求本机 Docker 运行中）
- CORS 白名单已含 5273（`cors.allowed-origins`，可用 `CORS_ALLOWED_ORIGINS` 覆盖）；新增 dev 端口时需同步白名单，否则 API 返回 403「Invalid CORS request」
- 串行执行（workers 1）避免测试账号数据交叉干扰；`npx playwright show-trace` 可解析失败用例的 trace

**E2E 落地过程中发现并修复的缺陷**：

1. dev 模式 API 404（既有缺陷）：vite 此前无 `/api` proxy，dev 页面 API 直连失败 → 补 server.proxy
2. CORS 白名单缺 5273 → 发布请求 403「Invalid CORS request」→ application.yml 白名单补齐
3. 未评分发布传 `productRating=0` 违反 DB 约束 `chk_post_rating`（仅允许 1-5 或 NULL）→ `PostService.normalizeRating` 归一化 0/null→NULL（createPost/updatePost 两处 + 单测）

### 7.1 环境要求

| 工具 | 最低版本 | 说明 |
|------|---------|------|
| Node.js | 20.x | 前端构建 |
| npm | 10.x | 包管理 |
| Java | 21 | 后端运行（Spring Boot 3 要求 17+） |
| Maven | 3.9 | 后端构建 |
| Docker | 24.x | 容器引擎（macOS 经 Colima 提供） |
| Docker Compose | 2.x | 编排 |
| Colima | 0.10+ | macOS Docker 运行时（替代 Docker Desktop） |

macOS 安装：

```bash
brew install node maven openjdk@21
sudo ln -sfn /opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-21.jdk
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
# Docker 运行时（macOS 使用 Colima，启动方式见 §7.2）
brew install colima docker docker-compose
```

### 7.2 启动 Docker 运行时（Colima，macOS）

macOS 使用 Colima 提供 Docker 运行时（轻量、无需 Docker Desktop）。**执行任何 `docker` / `docker compose` 命令前，必须先启动 Colima**：

```bash
colima start          # 启动（首次运行会创建虚拟机，默认 4 CPU / 8GiB 内存 / 60GiB 磁盘）
colima status         # 查看状态（Running 后 docker CLI 才可用）
colima stop           # 停止

# 自定义资源（本地同时跑 MySQL + ES 等多个容器，建议内存 ≥ 8GiB）
colima start --cpu 4 --memory 8 --disk 60

# 可选：随系统自启
brew services start colima

# 彻底删除虚拟机（危险：容器与数据卷全部丢失）
colima delete
```

- `colima start` 会自动将 docker context 切到 `colima`；未生效时执行 `docker context use colima`
- 运行校验：`docker info` 正常返回即运行时可用
- `start-dev.sh` 已内置检测：Docker 不可用时自动执行 `colima start`（未安装 Colima 则提示安装并退出）

### 7.3 方式一：Docker 一键启动（推荐）

```bash
cd /Users/qinghang/Documents/QoderWorkSpace/MyApp/buyer-show

docker compose up --build -d   # 构建并启动全部服务
docker compose ps              # 查看运行状态
docker compose logs -f         # 查看日志
```

> 镜像构建：API 采用多阶段构建并使用阿里云 Maven 镜像加速依赖（`server/.mvn/settings.xml`）；api / web 容器健康检查分别探测 `/api/v1/health` 与 `http://127.0.0.1/`（自定义 nginx 配置仅监听 IPv4，健康检查不可用 `localhost`）。

**启动后的服务清单**：

| 服务 | 地址 | 说明 |
|------|------|------|
| Web 前端 | http://localhost:3000 | Nginx 托管 React SPA |
| API 后端 | http://localhost:8080 | Spring Boot API |
| Swagger UI | http://localhost:8080/swagger-ui.html | API 文档 |
| MySQL | localhost:3306 | root/rootpass 或 app/devpassword |
| Redis | localhost:6379 | 缓存 |
| Elasticsearch | http://localhost:9200 | 全文搜索 |
| RabbitMQ | http://localhost:15672 | app/devpassword（管理界面） |
| MinIO | http://localhost:9001 | minioadmin/minioadmin（管理界面） |

**常用 Docker 命令**：

```bash
docker compose down                        # 停止全部
docker compose down -v                     # 停止并清除数据卷（重置数据库）
docker compose up -d mysql redis elasticsearch rabbitmq minio   # 只启动基础设施
docker compose up --build -d api           # 重新构建某个服务
docker compose exec mysql mysql -u app -pdevpassword buyershow  # 进入 MySQL
docker compose logs -f api                 # 查看 API 日志
```

**代码更新后重新部署**：

```bash
docker compose up -d --build api       # 仅后端改动（Maven 编译打包，约 1–3 分钟）
docker compose up -d --build web       # 仅前端改动（tsc + vite build，约 1 分钟）
docker compose up -d --build api web   # 前后端都改（或不确定时）
```

- `--build` 按最新代码重建镜像，`up -d` 自动替换旧容器；基础设施容器（MySQL/Redis/ES/RabbitMQ/MinIO）不受影响
- 判断规则：改动涉及 `server/`（Java / 迁移 / application.yml / Dockerfile）→ 必须重建 api（镜像内含编译产物，容器未挂载源码）；仅涉及 `frontend/` → 只重建 web；仅改 compose 的 `environment` → 无需 `--build`，`up -d` 自动重建容器
- 数据库迁移由 api 启动时 Flyway 自动执行，无需手动操作；新增 DROP 类迁移前先核对库中对象存在性（见 §6.2）
- 验证：`docker compose ps`（api/web 应为 healthy）+ `curl http://localhost:8080/api/v1/health`
- 提交前建议本地预验证：前端 `npm run build`、后端 `./mvnw test`（JDK 21），避免镜像构建中途失败
- `docker compose restart api` 仅重启进程、不会应用新代码

### 7.4 方式二：手动逐项启动

```bash
cd /Users/qinghang/Documents/QoderWorkSpace/MyApp/buyer-show

# 1. 启动基础设施
docker compose up -d mysql redis elasticsearch rabbitmq minio
docker compose wait mysql

# 2. 启动后端（自动执行 Flyway 迁移）
cd server && mvn spring-boot:run
# 或打包运行：
mvn clean package -DskipTests && java -jar target/buyer-show-server-1.0.0-SNAPSHOT.jar

# 3. 启动前端
cd ../frontend && npm install && npm run dev   # → http://localhost:5173
```

验证：

```bash
curl http://localhost:8080/api/v1/health
# → {"code":0,"message":"success","data":{"status":"UP",...}}

curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"test123","nickname":"测试用户","captchaId":"<先调 GET /auth/captcha>","captchaCode":"<图中4位>"}'
```

> 仓库提供 `start-dev.sh`（一键启动中间件 + 后端 + 前端）与 `stop-dev.sh`，脚本内容以文件为准。

### 7.5 数据库初始化与重置

Flyway 在 Spring Boot 启动时自动执行 `db/migration` 下未跑过的迁移。

```bash
# 手动查看
docker compose exec mysql mysql -u app -pdevpassword buyershow
SHOW TABLES;

# 重置数据库（危险：删除全部数据）
docker compose down -v && docker compose up -d mysql
```

### 7.6 前端代理与环境变量

前端通过 Vite 代理访问后端（`vite.config.ts`）：

```typescript
server: {
  port: 5173,
  proxy: { '/api': { target: 'http://localhost:8080', changeOrigin: true } },
}
```

**后端关键配置**（`server/src/main/resources/application.yml`）：

| 配置 | 默认值 | 说明 |
|------|--------|------|
| `server.port` | 8080 | API 端口 |
| `spring.datasource.url` | jdbc:mysql://localhost:3306/buyershow | MySQL 连接 |
| `jwt.secret` | (内置) | JWT 签名密钥（生产必须外置，见待办 P1.8） |
| `jwt.access-token-expiration` | 1800000 (30min) | access token 过期时间 |
| `jwt.refresh-token-expiration` | 604800000 (7天) | refresh token 过期时间 |
| `minio.endpoint` | http://localhost:9000 | MinIO 内网地址 |
| `minio.bucket-name` | buyershow-images | 图片存储桶 |

Docker Compose 通过环境变量覆盖（`SPRING_DATASOURCE_URL/USERNAME/PASSWORD` 等，容器间用服务名通信）。

### 7.7 常见问题

| 问题 | 处理 |
|------|------|
| docker 命令连接失败 | `colima status` 确认 Running；未运行执行 `colima start` |
| MySQL 启动失败 | `docker compose logs mysql`；端口占用用 `lsof -i :3306` 排查 |
| Flyway 迁移失败 | 查 `flyway_schema_history` 表；必要时重置数据库（见 7.5） |
| npm install 慢 | 已在 `frontend/.npmrc` 固定 `registry=https://registry.npmmirror.com`，无需手动设置 |
| Java 版本不对 | `export JAVA_HOME=$(/usr/libexec/java_home -v 21)` |
| Docker 内存不足 | 调大 Colima 内存：`colima stop && colima start --memory 8` |

### 7.8 依赖源约定（强制）

本仓库禁止使用任何内网 / 私有源，依赖必须来自公网。

| 项 | 约定 |
|---|---|
| npm 源 | `frontend/.npmrc` → `registry=https://registry.npmmirror.com`（公网镜像） |
| Maven 源 | `server/.mvn/settings.xml` → `maven.aliyun.com/repository/public`（公网镜像） |
| lockfile 校验 | `frontend/package-lock.json` 的 `resolved` 必须全部为公网地址，且包含 Linux 平台二进制，否则云端构建无法通过 `npm ci` |
| 变更依赖后 | 确认 lockfile 未被写入内网 registry；必要时删除 `node_modules` 与 lockfile 重新生成 |

完整规则与自检命令见工作区根目录 `.qoder/rules/no-intranet-resources.md`。

### 7.9 生产部署参考

**K8s 清单规划**：

```
infra/k8s/
├── namespace.yaml
├── api/        # deployment.yaml（HPA 2-20 副本）+ service.yaml + hpa.yaml
├── web/        # deployment.yaml（Nginx）+ service.yaml
├── mysql/      # statefulset.yaml（主从）+ service.yaml
├── redis/      # deployment.yaml + service.yaml
├── ingress.yaml / configmap.yaml / secrets.yaml
```

- API Deployment：3 副本起步，HPA 自动扩缩（CPU 65% / 内存 75%）
- MySQL：主从复制 + 读写分离；Redis：Sentinel 哨兵模式
- 静态前端：Nginx Deployment + CDN

**CI/CD**（规划）：GitHub Actions 构建镜像并推送阿里云镜像仓库（`registry.cn-hangzhou.aliyuncs.com/buyershow/{api,web}:${sha}`），ArgoCD 同步部署。

**域名规划**：

| 域名 | 用途 |
|------|------|
| `buyershow.com` | 主站（前端） |
| `api.buyershow.com` | API 接口 |
| `img.buyershow.com` | 图片 CDN |
| `admin.buyershow.com` | 管理后台 |

> 目标态部署架构（流量、缓存、消息与可观测链路）见《架构设计文档》。

---

## 八、旧版架构归档（Supabase 时期）

> 以下为 2026-06 至 2026-08 期间的 Supabase 单体架构历史记录，代码归档在 `buyer-show-backup/`（含 `supabase/` 迁移脚本），仅供参考。

### 8.1 旧技术栈
- Next.js 14 (App Router) + Supabase (PostgreSQL + Auth + Storage)
- 部署到 EdgeOne Pages 静态导出

### 8.2 迁移历史（v1-v23）
共 26 个迁移脚本，涵盖：
- v1-v5：基础 schema + 收藏 + 管理员 + 标签
- v6-v7：状态字段 SMALLINT 化
- v8：解封恢复帖子 bug 修复
- v9-v10：索引重建 + 审计
- v11：评论一级嵌套
- v12-v14：索引清理 + Feed RPC + Storage
- v15-v18：游标分页 + Toggle RPC + 性能优化
- v19-v20：标签收藏 RPC + 死代码清理
- v21-v23：active_posts 视图 + Feed JOIN 优化

### 8.3 已知教训（已在新架构中规避）
- Supabase SQL Editor 整段粘贴禁用 `CREATE INDEX CONCURRENTLY`
- RLS OR 条件阻止 partial index → 改用 SECURITY DEFINER 函数
- 状态字段用 SMALLINT + 代码常量，禁止 TEXT + CHECK
- `DROP FUNCTION` 前必须 DROP 所有引用它的 RLS 策略

---

## 九、P0 闭环实现（2026-09）

### 9.1 前端路由与服务层
- `react-router-dom` 管理首页、帖子详情、发布、登录、审核和消息路径；帖子详情使用 `:postId` 路由参数，支持刷新恢复。
- 帖子详情支持双模式（U23）：列表入口以 `navigate(`/posts/${id}`, { state: { modal: true } })` 打开浮层（遮罩 z-[60] + 居中卡片，关闭回列表并恢复滚动）；直链/刷新无 state 时渲染整页。
- 消息实时化（G11 v2）：STOMP over WebSocket（`/ws`）实时推送通知/私信/公告（RabbitMQ fanout 事件总线），15s/5s 轮询保留为降级兜底；断线 3s 自动重连 + 心跳 10s；页面隐藏时轮询暂停、恢复即补拉；在线状态由后端 Redis 活跃标记驱动（认证请求 touch，60s TTL，key: `activity:user:{id}`，会话列表批量判定）
- `frontend/src/services/` 按认证、帖子、评论、上传、举报和管理拆分请求；HTTP 客户端默认使用同源 `/api/v1`，主动检查 access token 过期时间，并在 401 时用 refresh token 合并刷新、重试一次。
- Feed 标签在后端过滤后再执行游标分页；前端通过请求版本号忽略过期响应，避免快速切换标签时旧数据覆盖新数据。
- 发布、消息和审核路由要求登录；登录后恢复原目标路径。帖子详情将加载错误与点赞/收藏/评论等操作错误分离，普通操作失败不再清空页面。

### 9.2 审核、举报与上传安全基线
- `V4__add_content_moderation.sql` 建立帖子/评论审核字段以及 `sensitive_words`、`content_reports` 表；`V5__harden_moderation_and_visibility.sql` 以增量方式加固默认值、索引和唯一约束，保持已发布迁移不可变。
- 既有数据在 V4 中回填为通过；V5 将数据库默认值切换为待审，避免滚动发布期间旧节点漏传审核状态而直接公开。
- 发布与评论逐字段执行 Unicode NFKC 标准化、空白/不可见字符清理及本地敏感词匹配；标题、正文、商品名、购物来源和标签均纳入审核。
- 管理端审核和举报处理使用带旧状态条件的 CAS 更新，同一内容只有一个并发请求能完成状态迁移并变更计数。
- 评论回复只允许挂在公开且审核通过的顶级评论下；删除或举报下架顶级评论时同步处理其一级回复并按实际通过数量调整帖子计数。
- 举报只允许指向公开且审核通过的帖子/评论；唯一键 `(reporter_id, content_type, content_id)` 限制同一用户重复举报同一内容。
- MinIO 上传先写入私有 `pending/{userId}/` 前缀；自动审核或人工审核通过后幂等复制到公开 `published/` 前缀，拒绝或发布失败时清理 pending 对象。
- JPEG/PNG 在服务端完整解码并重新编码以验证内容和清除元数据；WebP 校验 RIFF 长度和合法 VP8 chunk；Spring multipart 边界与业务限制统一为 10MB。
- MinIO Bucket 与「`published/*`、`thumbnails/*` 可读（pending 保持私有）」的策略在应用启动时一次性初始化；内部 Endpoint 与浏览器可访问的 public URL 分离。
- 管理端权限以后端实时读取的用户角色为准，JWT 内旧 role 不再直接决定授权。
- V4/V5 已通过单元契约测试，并在独立 MySQL 8.0 临时数据库完整执行 V1-V5 验证。

---

## 十、后续规划

> 完整待办台账以《产品设计文档》「代办功能」为唯一来源，本节仅列技术侧主线。

| 优先级 | 方向 | 说明 |
|--------|------|------|
| P1 | 图片处理流水线 | 多分辨率缩略图 + WebP 转码 + CDN 分发（P1.3） |
| P1 | 通知系统 | G11 已实现 WebSocket 实时推送（STOMP + RabbitMQ 事件总线）；离线消息可靠投递待推进（P1.4） |
| P1 | 搜索增强 | 热搜/联想 → 纠错 → 语义搜索，接入 Elasticsearch（P1.5） |
| P1 | 生产配置加固 | 密钥按环境注入、关闭生产 debug 日志（P1.8） |
| P2 | 推荐系统 | G3 v1 已实现详情页同标签召回 + 最新公开帖补足；后续引入热门排序、热度加权与个性化（P1.5/P2.2） |
| P2 | uniapp 移动端 | 微信小程序 + App（app/ 目录） |
| P2 | 数据平台 | 埋点 + 分析看板（P2.2） |
| P3 | 基础设施 | RabbitMQ 已接入（G11 事件总线）；ES 代码接入、K8s 编排与 CI/CD（Redis 已接入：缓存/限流/验证码/在线标记） |

### 开源项目借鉴（架构 & 进阶）

> 面向"架构演进 / 进阶能力"方向的开源对标清单：只借鉴**数据结构、状态机、算法与交互范式**，落到本项目 Spring Boot + React 栈重写；**不整体搬架构**（多数为 Laravel/Ruby/Rust/Node 栈，混栈会失控）。Java 系项目（Halo / eladmin）可连代码一起参考。

| 演进方向（关联待办） | 首选对标项目 | 技术栈 | 可借鉴的具体设计 |
|---|---|---|---|
| 图片处理流水线（P1.3） | **Pixelfed** | Laravel + React | 原图 + 多规格缩略图生成；"先落库后异步处理"队列（上传不阻塞）；CDN 化存储路径规划 |
| 搜索增强（P1.5） | **Meilisearch / Typesense**（→ ES） | Rust | 容错 / 分词 / 即搜即得 + 高亮；渐进式替换 MySQL `LIKE` |
| 通知系统（P1.4） | **Mastodon**、**Halo** | Ruby / Java | 通知表设计（类型枚举 + 已读游标）；SSE / WebSocket 实时推送骨架 |
| 时间线 / Feed 分页 | **Mastodon** | Ruby | 游标（雪花 ID）分页替代 offset；Feed 拉取缓存策略 |
| 推荐 / 热度排序（P2） | **Lemmy**、**NodeBB** | Rust / Node | Reddit/HN 式 hot ranking（score + 时间衰减），广场按热度而非纯时间排 |
| 内容审核 / 举报（P0.3 基线已做） | **Mastodon moderation**、**localmod** | Ruby / Go | 举报→审核队列→处置（通过/删除/封号）状态机与管理台；本地图片/文本自动审核信号做"先审后发"兜底 |
| 评论 / 盖楼（5.x 已做一级） | **Halo**、**Discourse** | Java / Ruby | 深层嵌套评论树、@提及、评论状态机（Halo 同 Java，可直接读源） |
| 管理后台 RBAC（UserRole） | **eladmin**、**RuoYi-Vue-Plus** | Spring Boot | 权限表结构 + 后台脚手架，避免从零搭 |

**若仅落地三个**：Pixelfed（图片晒物业务最贴）、Halo（同栈、评论/通知/状态机可直接读挪）、Meilisearch（一个二进制补齐"搜索是死控件"，投入产出比最高）。

> 目标态中间件设计（Redis Key 规划、RabbitMQ 拓扑、ES mapping）、可观测性方案与竞品对标详见《架构设计文档》。

---

*文档版本：v3.4 | 最后更新：2026-09-21*
