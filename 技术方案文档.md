# 买家说 — 技术方案文档

> 版本：v3.1 | 更新：2026-09-14 | 架构：前后端分离 Monorepo

**文档分工**（2026-09 文档合并后）：
- 本文档：技术栈、系统结构、数据库、API 契约、部署与运行手册
- 《产品设计文档》：产品定位、功能范围、UI 设计规范、代办事项
- 《架构设计文档》：目标态架构、中间件与可观测性设计、演进方向

---

## 一、项目概述

「买家说」是购物种草社区应用，采用**前后端分离 + Monorepo** 架构，支持 PC Web、手机浏览器、小程序多端访问。

**改造前**：Next.js 14 + Supabase 单体应用，部署到 EdgeOne Pages。
**改造后**：Vite + React 前端 + Spring Boot 后端 + MySQL 数据库，Docker + Kubernetes 部署。

---

## 二、技术栈

### 前端（frontend/）

| 层面 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 构建 | Vite | 8.x | 极速 HMR，esbuild 预构建 |
| 框架 | React | 19.x | 全球最主流前端框架 |
| 语言 | TypeScript | 6.x | 类型安全 |
| 组件库 | shadcn/ui | latest | Tailwind 原生组件，按需安装 |
| 样式 | Tailwind CSS | 4.x | 原子化 CSS，`@theme` Token |
| 路由 | react-router-dom | 7.x | SPA 路由 |
| 图标 | lucide-react | latest | 轻量 SVG 图标 |
| 状态 | 组件内 useState + `services/` 请求层 | - | 当前未引入全局状态库；Zustand 为规划项 |

### 后端（server/）

| 层面 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Spring Boot | 3.3 | Java 企业级标准 |
| 语言 | Java | 21 | LTS 版本 |
| ORM | MyBatis-Plus | 3.5 | 国内最主流 ORM |
| 数据库 | MySQL | 8.0 | 国内运维人才最多 |
| 缓存 | Redis | 7.x | 容器已就绪，代码未接入（规划） |
| 搜索 | Elasticsearch | 8.x | 容器已就绪，代码未接入（规划） |
| 消息队列 | RabbitMQ | 3.13 | 容器已就绪，代码未接入（规划） |
| 对象存储 | MinIO / 阿里云 OSS | 8.x 客户端 | S3 兼容协议，已接入 |
| 认证 | Spring Security + JWT | - | 双 token 机制 |
| 迁移 | Flyway | 10.x | SQL 版本管理 |
| API 文档 | SpringDoc OpenAPI | 2.x | 自动生成 Swagger |

### 部署

| 层面 | 选型 | 说明 |
|------|------|------|
| 容器 | Docker + Docker Compose | 本地开发环境（已可用） |
| 编排 | Kubernetes 1.28 + Helm 3 | 生产环境（规划） |
| CI/CD | GitHub Actions + ArgoCD | GitOps 持续部署（规划） |
| 可观测标准 | OpenTelemetry + W3C Trace Context | 详见《架构设计文档》可观测性章节 |
| 网关 | Nginx | 静态托管与反向代理（已可用）；Spring Cloud Gateway 为规划项 |

---

## 三、项目结构（Monorepo）

```
buyer-show/
├── frontend/                         # Web 前端（Vite + React 19）
│   ├── src/
│   │   ├── App.tsx                   # 入口 + 路由声明
│   │   ├── main.tsx                  # React 挂载
│   │   ├── index.css                 # Tailwind 4 + @theme 设计 Token
│   │   ├── components/ui/            # shadcn/ui 组件
│   │   ├── lib/utils.ts              # 工具函数
│   │   ├── services/                 # API 请求层（http/auth/posts/comments/uploads/reports/admin）
│   │   └── flows/                    # 业务 Flow
│   │       ├── shared/               # 共享类型 + 本地常量
│   │       ├── home-feed/            # 首页 Feed（标签筛选 + 游标分页）
│   │       ├── post-detail/          # 帖子详情（互动 + 评论 + 举报）
│   │       ├── publish-post/         # 发布（图片上传 + 商品信息）
│   │       ├── auth/                 # 登录/注册
│   │       ├── admin/                # 管理员审核台
│   │       └── messages/             # 私信通知（静态展示）
│   ├── vite.config.ts                # Vite + Tailwind 插件 + @ alias + /api 代理
│   ├── tsconfig.json / tsconfig.app.json
│   └── package.json
│
├── server/                           # Java 后端（Spring Boot 3.3，已实现核心 API）
│   ├── src/main/java/com/buyershow/
│   │   ├── BuyerShowApplication.java
│   │   ├── config/                   # SecurityConfig, MinioConfig, MinioBucketInitializer
│   │   ├── controller/               # Auth/Post/Comment/Upload/ContentReport/AdminModeration/Health
│   │   ├── service/                  # 业务逻辑层（认证、帖子、评论、审核、上传等）
│   │   ├── mapper/                   # MyBatis-Plus Mapper（含自定义 SQL）
│   │   ├── entity/                   # 数据库实体
│   │   ├── dto/                      # 请求/响应 DTO
│   │   └── common/                   # R<T>、ErrorCode、异常处理、JWT 安全
│   └── src/main/resources/
│       ├── application.yml
│       ├── mapper/*.xml              # MyBatis XML（如有）
│       └── db/migration/             # Flyway SQL（V1__init_schema.sql ... V5）
│
├── app/                              # 移动端 uniapp X（待实现）
│
├── docs/assets/                      # logo 等静态资源
├── ui-prototype.html                 # 移动端 UI 原型（15 屏）
├── docker-compose.yml                # 全栈本地编排
├── start-dev.sh / stop-dev.sh        # 一键启动/停止开发环境
│
├── AGENTS.md / CLAUDE.md / README.md # 规范与入口
├── 产品设计文档.md                    # 产品功能 + UI 规范 + 待办
├── 技术方案文档.md                    # 本文档
├── 架构设计文档.md                    # 目标态架构与演进
└── buyer-show-backup/                # 旧版 Next.js/Supabase 应用归档（独立目录）
```

---

## 四、前端架构

### 4.1 响应式布局

| 断点 | 宽度 | 导航 | Feed 列数 |
|------|------|------|----------|
| Mobile | < 768px | 底部 TabBar | 2 列 |
| Tablet | 768-1023px | 底部 TabBar | 3 列 |
| Desktop | 1024-1279px | 顶部 Nav + 侧边栏 | 3 列 |
| Wide | ≥ 1280px | 顶部 Nav + 侧边栏 | 4-5 列 |

### 4.2 Flow 清单（4 Flow · 已实现）

| Flow | 核心交互 |
|------|---------|
| 首页 Feed | 列表 + 标签筛选 + 搜索入口 |
| 帖子详情 | 图片 + 商品信息卡 + 评论树 + 互动 + 举报 |
| 发布分享 | 图片上传（MinIO）→ 商品信息（来源/价格/评分）→ 正文 + 审核反馈 |
| 私信通知 | 对话列表 + 聊天 + 互动通知（静态展示，待通知服务） |

> 移动端 15 屏完整定义见《产品设计文档》§四；设计 Token 与组件规范见《产品设计文档》§五。

---

## 五、后端架构

### 5.1 分层设计

```
Controller（参数校验 + 调用 Service + 返回 R<T>）
    ↓
Service（业务逻辑 + 事务管理）
    ↓
Mapper（MyBatis-Plus CRUD + 自定义 SQL）
    ↓
MySQL 8.0（Redis / ES / RabbitMQ 为规划中，代码未接入）
```

### 5.2 统一响应

```java
public class R<T> {
    private int code;       // 0=成功，其他=业务错误
    private String message; // 用户可见消息
    private T data;

    public static <T> R<T> ok(T data) { ... }
    public static <T> R<T> fail(int code, String message) { ... }
    public static <T> R<T> fail(ErrorCode error) { ... }
}
```

### 5.3 错误码规范

| 范围 | 模块 | 示例 |
|------|------|------|
| 1000-1999 | 认证 | TOKEN_EXPIRED, NO_PERMISSION, ACCOUNT_BANNED |
| 2000-2999 | 用户 | USER_NOT_FOUND, USERNAME_EXISTS, ACCOUNT_EXISTS |
| 3000-3999 | 帖子 | POST_NOT_FOUND, POST_NO_EDIT |
| 4000-4999 | 评论 | COMMENT_NOT_FOUND, COMMENT_REPLY_DEPTH |
| 5000-5999 | 上传 | FILE_TOO_LARGE, FILE_TYPE_INVALID, FILE_UPLOAD_FAILED |
| 6000-6999 | 校验 | PARAM_INVALID, PARAM_MISSING |
| 7000-7999 | 内容安全 | CONTENT_REJECTED, REPORT_DUPLICATED, CONTENT_NOT_PENDING |
| 9000-9999 | 系统 | SYSTEM_ERROR, RATE_LIMITED |

### 5.4 认证机制

```
注册 → BCrypt 哈希密码 → 写入 users（用户名唯一，手机号/邮箱可选绑定且唯一） → 签发 TokenPair
登录 → 按用户名/手机号/邮箱定位账号 → BCrypt 比对 → 签发 TokenPair

access_token:  JWT (HS256), 30 分钟过期, payload={userId, role}
refresh_token: JWT (HS256), 7 天过期, payload={userId, type=refresh}，刷新时重新签发 TokenPair
```

### 5.5 API 契约

**已实现（✅）**：

```
认证：POST /api/v1/auth/{register,login,refresh}
帖子：GET    /api/v1/posts             # 游标分页 + tag 过滤
      GET    /api/v1/posts/{id}
      POST   /api/v1/posts             # 发布（含内容审核）
      DELETE /api/v1/posts/{id}        # 作者删除
互动：POST   /api/v1/posts/{id}/like
      POST   /api/v1/posts/{id}/favorite
评论：GET    /api/v1/posts/{postId}/comments
      POST   /api/v1/posts/{postId}/comments
      DELETE /api/v1/comments/{commentId}
举报：POST   /api/v1/reports
上传：POST   /api/v1/upload/image
      DELETE /api/v1/upload/image?objectName=
用户：GET    /api/v1/users/me          # 当前登录用户资料
      PUT    /api/v1/users/me          # 更新当前用户资料
      GET    /api/v1/users/{userId}    # 指定用户公开资料
管理：GET    /api/v1/admin/moderation/{posts,comments}
      GET    /api/v1/admin/reports
      POST   /api/v1/admin/moderation/posts/{postId}
      POST   /api/v1/admin/moderation/comments/{commentId}
      POST   /api/v1/admin/reports/{reportId}
健康：GET    /api/v1/health
```

**规划（未实现）**：

```
关注：POST/DELETE /api/v1/users/{id}/follow
标签：GET /api/v1/tags、POST/DELETE /api/v1/tags/{name}/favorite
搜索：GET /api/v1/search、GET/DELETE /api/v1/search/history
认证扩展：POST /api/v1/auth/{logout,wechat,sms}
通知：GET /api/v1/notifications
私信：/api/v1/chat/*
管理扩展：/api/v1/admin/users/*、/api/v1/admin/tags/*
```

### 5.6 日志与可观测性架构

#### 5.6.1 主流方案对比

| 方案 | 组件 | 存储成本 | 运维复杂度 | 适用场景 |
|------|------|---------|-----------|---------|
| **ELK** | Logstash + Elasticsearch + Kibana | 高 | 高（分片/索引管理） | 大型企业、全文搜索需求 |
| **EFK** | Fluentd/Fluent Bit + ES + Kibana | 高 | 中高 | K8s + 需 ES 查询能力 |
| **PLG** | Promtail/Alloy + Loki + Grafana | 低 | 低 | K8s 环境、已有 Grafana 生态 |
| **OpenTelemetry** | OTel Collector + 混合后端 | 中 | 中 | 微服务、端到端链路追踪 |
| **SaaS** | Datadog/New Relic/Splunk | 极高（按量计费） | 极低 | 预算充足、团队小 |
| **云原生** | CloudWatch/Cloud Logging | 中 | 低 | 单一云平台 |

#### 5.6.2 选型决策

本项目采用 **PLG + OpenTelemetry 混合方案**：

```
Spring Boot App
├── Logback JSON + MDC (trace_id, request_id, user_id)
└── OTel Java Agent (自动注入 span)
         │
         ▼
   Grafana Alloy (采集层)
   ├── 日志：读取容器 stdout / 文件
   ├── 指标：抓取 /actuator/prometheus
   └── 链路：接收 OTLP traces
         │
    ┌────┼────┐
    ▼    ▼    ▼
  Loki  Prom  Tempo    ← 存储层
  (日志)(指标)(链路)
    └────┼────┘
         ▼
      Grafana          ← 统一查询面板
```

**选型理由**：
- **成本低**：Loki 只索引标签（不索引日志内容），存储比 ES 便宜 10-50 倍
- **运维轻**：单二进制部署，无需分片管理
- **一体化**：日志、指标、链路在一个 Grafana 面板查看，trace_id 串联全链路
- **扩展性**：OTel Agent 可随时接入更多后端，不锁定厂商

#### 5.6.3 应用层日志规范

**后端 Java**：

```
日志格式（dev）：  HH:mm:ss.SSS LEVEL [trace_id] [request_id] logger | message
日志格式（prod）： 单行 JSON（LogstashEncoder），含 timestamp/level/logger/message/trace_id/request_id/user_id

级别策略：
  com.buyershow.service     → INFO   业务操作（发帖/登录/审核/举报）
  com.buyershow.mapper      → WARN   仅记录慢 SQL 与异常
  org.springframework       → WARN   关闭框架噪音
  org.flywaydb              → INFO   迁移必须可见
  MyBatis StdOutImpl        → dev only，prod 用 NopLogging
```

**MDC 上下文**（每个 HTTP 请求自动注入）：

| 字段 | 来源 | 说明 |
|------|------|------|
| trace_id | W3C traceparent header 或自动生成 | 跨服务链路追踪 |
| request_id | UUID 生成 | 单次请求唯一标识 |
| user_id | Spring Security 上下文 | 当前认证用户（匿名不写入） |

**前端 React**：

```
HTTP 层：开发环境 console.log 自动输出 [HTTP] METHOD URL → STATUS (ms)
ErrorBoundary：捕获渲染崩溃，输出组件栈 + 错误信息
生产环境：HTTP 日志静默，ErrorBoundary 错误可上报 Sentry（规划中）
```

#### 5.6.4 分阶段落地计划

| 阶段 | 内容 | 复杂度 | 状态 |
|------|------|--------|------|
| Phase 1 | Logback JSON + MDC Filter + 前端 ErrorBoundary + HTTP 日志 | 低（纯代码） | 已实现 |
| Phase 2 | Loki + Alloy Docker Compose 部署 | 中（加 3 个容器） | 规划中 |
| Phase 3 | OTel Java Agent 自动注入 span | 中（加 JVM 参数） | 规划中 |
| Phase 4 | 前端 Sentry 错误上报 | 中（接入 SDK） | 规划中 |

#### 5.6.5 日志查询示例（Loki LogQL）

```logql
{app="buyer-show", level="ERROR"}                    # 所有错误日志
{app="buyer-show", trace_id="a1b2c3d4"}              # 某次请求全链路
{app="buyer-show"} |= "创建帖子" | json              # 搜索发帖操作
{app="buyer-show", user_id="42"}                     # 某用户所有操作
sum(rate({app="buyer-show", level="ERROR"}[5m]))     # 错误率趋势
```

### 5.7 状态与角色枚举

所有状态和角色字段统一使用 Java 枚举管理，消除魔法数字。数据库存储为 TINYINT，Java 层通过 `getValue()` 获取整数值。

| 枚举类 | 值 | 含义 | 用途 |
|--------|------|------|------|
| **PostStatus** | PUBLIC(0) | 公开 | 帖子可见状态 |
| | HIDDEN(1) | 隐藏 | 作者主动隐藏 |
| | DELETED(2) | 删除 | 软删除 |
| **CommentStatus** | ACTIVE(0) | 正常 | 评论可见状态 |
| | DELETED(1) | 删除 | 软删除 |
| **ModerationStatus** | APPROVED(0) | 审核通过 | 内容审核结果 |
| | PENDING(1) | 待人工审核 | 自动审核未通过，等待人工 |
| | REJECTED(2) | 审核驳回 | 内容违规 |
| **UserRole** | USER(0) | 普通用户 | JWT payload 与权限判断 |
| | ADMIN(1) | 管理员 | 管理端接口鉴权 |
| **UserStatus** | ACTIVE(0) | 正常 | 账号可用状态 |
| | BANNED(1) | 封禁 | 禁止登录 |
| | DELETED(2) | 注销 | 账号已注销 |

**使用规范**：
- Service 层一律使用枚举比较（`post.getStatus() == PostStatus.DELETED.getValue()`），禁止硬编码数字
- 新建状态字段必须定义枚举，`common/` 包下维护
- 枚举 `getValue()` 返回 TINYINT 值，与数据库 schema 对齐

---

## 六、数据库设计（MySQL 8.0）

### 6.1 表清单

| 表名 | 用途 | 迁移 |
|------|------|------|
| users | 用户（昵称/头像/简介/统计/角色/状态；手机号/邮箱唯一） | V1 建表，V2 约束优化 |
| posts | 帖子（标题/正文/图片 JSON/标签 JSON/商品信息/计数） | V1 建表，V2 Feed/热门/用户索引，V4 审核字段，V5 加固 |
| comments | 评论（一级嵌套 `parent_id` + `reply_count`） | V1 建表，V2 稳定排序索引，V4 审核字段，V5 加固 |
| likes | 帖子点赞（`UNIQUE user+post`） | V1 建表，V2 双向查询索引 |
| favorites | 帖子收藏（`UNIQUE user+post`） | V1 建表，V2 用户时间线索引 |
| follows | 关注关系（follower/following） | V1 建表，V2 双向时间线索引 |
| tags | 标签字典（名称唯一 + post_count） | V1 建表，V2 校验/更新时间 |
| post_tag_rela | 帖子与标签多对多关系（高效筛选/统计） | V2 新增 |
| notifications | 通知（like/comment/follow/system） | V1 建表，V2 时间线/未读索引 |
| favorite_comments | 评论收藏 | V1 建表 |
| favorite_tags | 标签收藏 | V1 建表 |
| search_history | 搜索历史（`UNIQUE user+keyword`） | V1 建表 |
| chat_conversation | 私信会话主表 | V2 新增 |
| chat_conversation_member_rela | 会话成员关系与未读计数 | V2 新增 |
| chat_message | 私信消息（文本/图片/商品卡片/系统） | V2 新增 |
| sensitive_words | 内容审核敏感词（action: 0=人工审核 1=直接拒绝） | V4 新增，V5 加固 |
| content_reports | 内容举报（`UNIQUE reporter+content_type+content_id`） | V4 新增，V5 加固 |

> 完整 DDL 以 `server/src/main/resources/db/migration/` 迁移文件为准，本文档不重复粘贴。

### 6.2 迁移管理

使用 Flyway 管理数据库迁移：
- 文件位置：`server/src/main/resources/db/migration/`
- 命名规范：`V{N}__{description}.sql`；已执行迁移不可改写
- Spring Boot 启动时自动执行未跑过的迁移

| 版本 | 内容 |
|------|------|
| V1 | 初始化全部表结构（15 表） |
| V2 | 移除外键、重建索引、补充 `post_tag_rela` 与 `chat_*` 表 |
| V3 | Feed 主键游标优化（`(status, id DESC)`） |
| V4 | 内容审核：帖子/评论审核字段、`sensitive_words`、`content_reports` |
| V5 | 审核加固：默认待审、审核队列索引、举报唯一键收紧 |

V1-V5 已在独立 MySQL 8.0 数据库顺序执行验证通过。

### 6.3 当前有效结构与索引（V2/V3 治理结果）

| 表/模块 | 当前设计 | 核心索引或约束 |
|---------|---------|---------------|
| users | 用户名、昵称唯一；角色/状态/计数增加 CHECK | `username`、`uk_user_nickname` |
| posts | 图片和标签均为非空 JSON 数组；计数、价格、评分增加 CHECK | `idx_post_feed(status, moderation_status, id DESC)`、`idx_post_hot`、`idx_post_user_feed` |
| comments | 一级嵌套由应用层校验；补 updated_at 与稳定排序 | `idx_comment_post_feed`、`idx_comment_parent_feed`、`idx_comment_user_feed` |
| likes/favorites | `(user_id, post_id)` 唯一；补帖子反向查与用户时间线 | 唯一索引 + `idx_*_post` + `idx_*_user_feed` |
| follows | 防止自关注；双向列表按时间稳定排序 | `idx_follow_follower_feed`、`idx_follow_following_feed` |
| notifications | 同时支持全部时间线与未读列表 | `idx_notification_user_feed`、`idx_user_read` |
| post_tag_rela | JSON 标签用于展示，关系表负责高效筛选和统计 | `uk_post_tag`、`idx_tag_post`、`idx_tag_time_post` |
| chat_* | 会话、成员、消息三表支撑私信 | 会话键唯一、用户会话索引、会话消息索引 |

**索引策略**：

1. Feed V3 使用 `(status, id DESC)`，以自增 `id` 作为不透明游标；它与发帖写入顺序一致，可做索引范围扫描且避免 filesort。
2. 登录用户 Feed 在单条 SQL 中通过 `EXISTS` 子查询返回点赞/收藏状态，均命中 `(user_id, post_id)` 唯一覆盖索引，消除应用层每页 `2N` 次查询。
3. Feed 列表不查询 `content` 大字段；详情页单独查询正文，降低网络传输和 InnoDB 回表成本。
4. 点赞、收藏、发帖数采用原子 SQL `SET count = GREATEST(count + delta, 0)`，避免并发读改写丢失。
5. 数据库不使用外键，按项目规范由 Service 事务和唯一约束维护一致性，降低高并发写入与迁移耦合。
6. `sys.schema_redundant_indexes` 审计结果为空，已移除 `users.idx_username` 等重复索引。

**当前 Feed 执行计划**：

- `posts` 使用 `idx_post_feed`，游标条件使用完整的 `status + id` key parts，无 filesort。
- `STRAIGHT_JOIN` 固定 posts 为驱动表，作者关联使用 users 主键。
- 点赞/收藏 `EXISTS` 使用 `(user_id, post_id)` 唯一覆盖索引单行查找。
- `using_filesort = false`。

---

## 七、部署与运行

### 7.1 环境要求

| 工具 | 最低版本 | 说明 |
|------|---------|------|
| Node.js | 20.x | 前端构建 |
| npm | 10.x | 包管理 |
| Java | 21 | 后端运行（Spring Boot 3 要求 17+） |
| Maven | 3.9 | 后端构建 |
| Docker | 24.x | 容器引擎（macOS 经 Colima 提供） |
| Docker Compose | 2.x | 编排 |
| Colima | 0.10+ | macOS Docker 运行时（替代 Docker Desktop） |

macOS 安装：

```bash
brew install node maven openjdk@21
sudo ln -sfn /opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-21.jdk
export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"
# Docker 运行时（macOS 使用 Colima，启动方式见 §7.2）
brew install colima docker docker-compose
```

### 7.2 启动 Docker 运行时（Colima，macOS）

macOS 使用 Colima 提供 Docker 运行时（轻量、无需 Docker Desktop）。**执行任何 `docker` / `docker compose` 命令前，必须先启动 Colima**：

```bash
colima start          # 启动（首次运行会创建虚拟机，默认 4 CPU / 8GiB 内存 / 60GiB 磁盘）
colima status         # 查看状态（Running 后 docker CLI 才可用）
colima stop           # 停止

# 自定义资源（本地同时跑 MySQL + ES 等多个容器，建议内存 ≥ 8GiB）
colima start --cpu 4 --memory 8 --disk 60

# 可选：随系统自启
brew services start colima

# 彻底删除虚拟机（危险：容器与数据卷全部丢失）
colima delete
```

- `colima start` 会自动将 docker context 切到 `colima`；未生效时执行 `docker context use colima`
- 运行校验：`docker info` 正常返回即运行时可用
- `start-dev.sh` 已内置检测：Docker 不可用时自动执行 `colima start`（未安装 Colima 则提示安装并退出）

### 7.3 方式一：Docker 一键启动（推荐）

```bash
cd /Users/qinghang/Documents/QoderWorkSpace/MyApp/buyer-show

docker compose up --build -d   # 构建并启动全部服务
docker compose ps              # 查看运行状态
docker compose logs -f         # 查看日志
```

**启动后的服务清单**：

| 服务 | 地址 | 说明 |
|------|------|------|
| Web 前端 | http://localhost:3000 | Nginx 托管 React SPA |
| API 后端 | http://localhost:8080 | Spring Boot API |
| Swagger UI | http://localhost:8080/swagger-ui.html | API 文档 |
| MySQL | localhost:3306 | root/rootpass 或 app/devpassword |
| Redis | localhost:6379 | 缓存 |
| Elasticsearch | http://localhost:9200 | 全文搜索 |
| RabbitMQ | http://localhost:15672 | app/devpassword（管理界面） |
| MinIO | http://localhost:9001 | minioadmin/minioadmin（管理界面） |

**常用 Docker 命令**：

```bash
docker compose down                        # 停止全部
docker compose down -v                     # 停止并清除数据卷（重置数据库）
docker compose up -d mysql redis elasticsearch rabbitmq minio   # 只启动基础设施
docker compose up --build -d api           # 重新构建某个服务
docker compose exec mysql mysql -u app -pdevpassword buyershow  # 进入 MySQL
docker compose logs -f api                 # 查看 API 日志
```

### 7.4 方式二：手动逐项启动

```bash
cd /Users/qinghang/Documents/QoderWorkSpace/MyApp/buyer-show

# 1. 启动基础设施
docker compose up -d mysql redis elasticsearch rabbitmq minio
docker compose wait mysql

# 2. 启动后端（自动执行 Flyway 迁移）
cd server && mvn spring-boot:run
# 或打包运行：
mvn clean package -DskipTests && java -jar target/buyer-show-server-1.0.0-SNAPSHOT.jar

# 3. 启动前端
cd ../frontend && npm install && npm run dev   # → http://localhost:5173
```

验证：

```bash
curl http://localhost:8080/api/v1/health
# → {"code":0,"message":"success","data":{"status":"UP",...}}

curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"test123","nickname":"测试用户"}'
```

> 仓库提供 `start-dev.sh`（一键启动中间件 + 后端 + 前端）与 `stop-dev.sh`，脚本内容以文件为准。

### 7.5 数据库初始化与重置

Flyway 在 Spring Boot 启动时自动执行 `db/migration` 下未跑过的迁移。

```bash
# 手动查看
docker compose exec mysql mysql -u app -pdevpassword buyershow
SHOW TABLES;

# 重置数据库（危险：删除全部数据）
docker compose down -v && docker compose up -d mysql
```

### 7.6 前端代理与环境变量

前端通过 Vite 代理访问后端（`vite.config.ts`）：

```typescript
server: {
  port: 5173,
  proxy: { '/api': { target: 'http://localhost:8080', changeOrigin: true } },
}
```

**后端关键配置**（`server/src/main/resources/application.yml`）：

| 配置 | 默认值 | 说明 |
|------|--------|------|
| `server.port` | 8080 | API 端口 |
| `spring.datasource.url` | jdbc:mysql://localhost:3306/buyershow | MySQL 连接 |
| `jwt.secret` | (内置) | JWT 签名密钥（生产必须外置，见待办 P1.8） |
| `jwt.access-token-expiration` | 1800000 (30min) | access token 过期时间 |
| `jwt.refresh-token-expiration` | 604800000 (7天) | refresh token 过期时间 |
| `minio.endpoint` | http://localhost:9000 | MinIO 内网地址 |
| `minio.bucket-name` | buyershow-images | 图片存储桶 |

Docker Compose 通过环境变量覆盖（`SPRING_DATASOURCE_URL/USERNAME/PASSWORD` 等，容器间用服务名通信）。

### 7.7 常见问题

| 问题 | 处理 |
|------|------|
| docker 命令连接失败 | `colima status` 确认 Running；未运行执行 `colima start` |
| MySQL 启动失败 | `docker compose logs mysql`；端口占用用 `lsof -i :3306` 排查 |
| Flyway 迁移失败 | 查 `flyway_schema_history` 表；必要时重置数据库（见 7.5） |
| npm install 慢 | `npm config set registry https://registry.npmmirror.com` |
| Java 版本不对 | `export JAVA_HOME=$(/usr/libexec/java_home -v 21)` |
| Docker 内存不足 | 调大 Colima 内存：`colima stop && colima start --memory 8` |

### 7.8 生产部署参考

**K8s 清单规划**：

```
infra/k8s/
├── namespace.yaml
├── api/        # deployment.yaml（HPA 2-20 副本）+ service.yaml + hpa.yaml
├── web/        # deployment.yaml（Nginx）+ service.yaml
├── mysql/      # statefulset.yaml（主从）+ service.yaml
├── redis/      # deployment.yaml + service.yaml
├── ingress.yaml / configmap.yaml / secrets.yaml
```

- API Deployment：3 副本起步，HPA 自动扩缩（CPU 65% / 内存 75%）
- MySQL：主从复制 + 读写分离；Redis：Sentinel 哨兵模式
- 静态前端：Nginx Deployment + CDN

**CI/CD**（规划）：GitHub Actions 构建镜像并推送阿里云镜像仓库（`registry.cn-hangzhou.aliyuncs.com/buyershow/{api,web}:${sha}`），ArgoCD 同步部署。

**域名规划**：

| 域名 | 用途 |
|------|------|
| `buyershow.com` | 主站（前端） |
| `api.buyershow.com` | API 接口 |
| `img.buyershow.com` | 图片 CDN |
| `admin.buyershow.com` | 管理后台 |

> 目标态部署架构（流量、缓存、消息与可观测链路）见《架构设计文档》。

---

## 八、旧版架构归档（Supabase 时期）

> 以下为 2026-06 至 2026-08 期间的 Supabase 单体架构历史记录，代码归档在 `buyer-show-backup/`（含 `supabase/` 迁移脚本），仅供参考。

### 8.1 旧技术栈
- Next.js 14 (App Router) + Supabase (PostgreSQL + Auth + Storage)
- 部署到 EdgeOne Pages 静态导出

### 8.2 迁移历史（v1-v23）
共 26 个迁移脚本，涵盖：
- v1-v5：基础 schema + 收藏 + 管理员 + 标签
- v6-v7：状态字段 SMALLINT 化
- v8：解封恢复帖子 bug 修复
- v9-v10：索引重建 + 审计
- v11：评论一级嵌套
- v12-v14：索引清理 + Feed RPC + Storage
- v15-v18：游标分页 + Toggle RPC + 性能优化
- v19-v20：标签收藏 RPC + 死代码清理
- v21-v23：active_posts 视图 + Feed JOIN 优化

### 8.3 已知教训（已在新架构中规避）
- Supabase SQL Editor 整段粘贴禁用 `CREATE INDEX CONCURRENTLY`
- RLS OR 条件阻止 partial index → 改用 SECURITY DEFINER 函数
- 状态字段用 SMALLINT + 代码常量，禁止 TEXT + CHECK
- `DROP FUNCTION` 前必须 DROP 所有引用它的 RLS 策略

---

## 九、P0 闭环实现（2026-09）

### 9.1 前端路由与服务层
- `react-router-dom` 管理首页、帖子详情、发布、登录、审核和消息路径；帖子详情使用 `:postId` 路由参数，支持刷新恢复。
- `frontend/src/services/` 按认证、帖子、评论、上传、举报和管理拆分请求；HTTP 客户端默认使用同源 `/api/v1`，主动检查 access token 过期时间，并在 401 时用 refresh token 合并刷新、重试一次。
- Feed 标签在后端过滤后再执行游标分页；前端通过请求版本号忽略过期响应，避免快速切换标签时旧数据覆盖新数据。
- 发布、消息和审核路由要求登录；登录后恢复原目标路径。帖子详情将加载错误与点赞/收藏/评论等操作错误分离，普通操作失败不再清空页面。

### 9.2 审核、举报与上传安全基线
- `V4__add_content_moderation.sql` 建立帖子/评论审核字段以及 `sensitive_words`、`content_reports` 表；`V5__harden_moderation_and_visibility.sql` 以增量方式加固默认值、索引和唯一约束，保持已发布迁移不可变。
- 既有数据在 V4 中回填为通过；V5 将数据库默认值切换为待审，避免滚动发布期间旧节点漏传审核状态而直接公开。
- 发布与评论逐字段执行 Unicode NFKC 标准化、空白/不可见字符清理及本地敏感词匹配；标题、正文、商品名、购物来源和标签均纳入审核。
- 管理端审核和举报处理使用带旧状态条件的 CAS 更新，同一内容只有一个并发请求能完成状态迁移并变更计数。
- 评论回复只允许挂在公开且审核通过的顶级评论下；删除或举报下架顶级评论时同步处理其一级回复并按实际通过数量调整帖子计数。
- 举报只允许指向公开且审核通过的帖子/评论；唯一键 `(reporter_id, content_type, content_id)` 限制同一用户重复举报同一内容。
- MinIO 上传先写入私有 `pending/{userId}/` 前缀；自动审核或人工审核通过后幂等复制到公开 `published/` 前缀，拒绝或发布失败时清理 pending 对象。
- JPEG/PNG 在服务端完整解码并重新编码以验证内容和清除元数据；WebP 校验 RIFF 长度和合法 VP8 chunk；Spring multipart 边界与业务限制统一为 10MB。
- MinIO Bucket 与仅 `published/*` 可读的策略在应用启动时一次性初始化；内部 Endpoint 与浏览器可访问的 public URL 分离。
- 管理端权限以后端实时读取的用户角色为准，JWT 内旧 role 不再直接决定授权。
- V4/V5 已通过单元契约测试，并在独立 MySQL 8.0 临时数据库完整执行 V1-V5 验证。

---

## 十、后续规划

> 完整待办台账以《产品设计文档》「代办功能」为唯一来源，本节仅列技术侧主线。

| 优先级 | 方向 | 说明 |
|--------|------|------|
| P1 | 图片处理流水线 | 多分辨率缩略图 + WebP 转码 + CDN 分发（P1.3） |
| P1 | 通知系统 | WebSocket 实时推送 + 离线消息可靠投递（P1.4） |
| P1 | 搜索增强 | 热搜/联想 → 纠错 → 语义搜索，接入 Elasticsearch（P1.5） |
| P1 | 生产配置加固 | 密钥按环境注入、关闭生产 debug 日志（P1.8） |
| P2 | 推荐系统 | 热门排序 → 标签匹配 → 个性化（P1.5/P2.2） |
| P2 | uniapp 移动端 | 微信小程序 + App（app/ 目录） |
| P2 | 数据平台 | 埋点 + 分析看板（P2.2） |
| P3 | 基础设施 | Redis/ES/RabbitMQ 代码接入、K8s 编排与 CI/CD |

> 目标态中间件设计（Redis Key 规划、RabbitMQ 拓扑、ES mapping）、可观测性方案与竞品对标详见《架构设计文档》。

---

*文档版本：v3.1 | 最后更新：2026-09-14*
