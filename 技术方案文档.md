# 买家说 — 技术方案文档

> 版本：v3.0 | 更新：2026-09-07 | 架构：前后端分离 Monorepo

---

## 一、项目概述

「买家说」是购物种草社区应用，采用**前后端分离 + Monorepo** 架构，支持 PC Web、手机浏览器、小程序多端访问。

**改造前**：Next.js 14 + Supabase 单体应用，部署到 EdgeOne Pages。
**改造后**：Vite + React 前端 + Spring Boot 后端 + MySQL 数据库，Docker + Kubernetes 部署。

---

## 二、技术栈

### 前端（frontend/）

| 层面 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 构建 | Vite | 8.x | 极速 HMR，esbuild 预构建 |
| 框架 | React | 19.x | 全球最主流前端框架 |
| 语言 | TypeScript | 6.x | 类型安全 |
| 组件库 | shadcn/ui | latest | Tailwind 原生组件，按需安装 |
| 样式 | Tailwind CSS | 4.x | 原子化 CSS，`@theme` Token |
| 路由 | react-router-dom | 7.x | SPA 路由 |
| 图标 | lucide-react | latest | 轻量 SVG 图标 |
| 状态 | Zustand | 5.x | 轻量状态管理 |

### 后端（server/）

| 层面 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Spring Boot | 3.3 | Java 企业级标准 |
| 语言 | Java | 21 | LTS 版本 |
| ORM | MyBatis-Plus | 3.5 | 国内最主流 ORM |
| 数据库 | MySQL | 8.0 | 国内运维人才最多 |
| 缓存 | Redis | 7.x | 最主流缓存 |
| 搜索 | Elasticsearch | 8.x | 全文搜索标准 |
| 消息队列 | RabbitMQ | 3.13 | 最主流消息中间件 |
| 对象存储 | MinIO / 阿里云 OSS | - | S3 兼容协议 |
| 认证 | Spring Security + JWT | - | 双 token 机制 |
| 迁移 | Flyway | 10.x | SQL 版本管理 |
| API 文档 | SpringDoc OpenAPI | 2.x | 自动生成 Swagger |

### 部署

| 层面 | 选型 | 说明 |
|------|------|------|
| 容器 | Docker + Docker Compose | 本地开发环境 |
| 编排 | Kubernetes 1.28 + Helm 3 | 生产环境 |
| CI/CD | GitHub Actions + ArgoCD | GitOps 持续部署 |
| 监控 | Prometheus + Grafana + ELK | 指标 + 日志 |
| 网关 | Nginx + Spring Cloud Gateway | 反向代理 + 限流 |

---

## 三、项目结构（Monorepo）

```
buyer-show/
├── frontend/                         # Web 前端
│   ├── src/
│   │   ├── App.tsx                   # 应用入口 + 屏幕切换
│   │   ├── main.tsx                  # React 挂载
│   │   ├── index.css                 # Tailwind + 珊瑚红主题 Token
│   │   ├── components/ui/            # shadcn/ui 组件（15 个）
│   │   ├── lib/utils.ts              # 工具函数
│   │   └── flows/                    # 业务 Flow
│   │       ├── shared/               # 共享类型 + mock 数据
│   │       ├── home-feed/            # 首页 Feed（瀑布流 + 标签 + 搜索）
│   │       ├── post-detail/          # 帖子详情（左图右文 + 评论 + 商品卡）
│   │       ├── publish-post/         # 发布分享（3 步表单）
│   │       └── messages/             # 私信通知（分栏布局）
│   ├── vite.config.ts                # Vite + Tailwind 插件 + @ alias
│   ├── tsconfig.json / tsconfig.app.json
│   └── package.json
│
├── server/                           # Java 后端（待实现）
│   ├── src/main/java/com/buyershow/
│   │   ├── BuyerShowApplication.java
│   │   ├── config/                   # SecurityConfig, RedisConfig, ESConfig, MinIOConfig
│   │   ├── controller/               # AuthController, PostController, CommentController...
│   │   ├── service/                  # 业务逻辑层
│   │   ├── mapper/                   # MyBatis-Plus Mapper
│   │   ├── entity/                   # 数据库实体
│   │   ├── dto/                      # 请求/响应 DTO
│   │   ├── common/                   # 异常处理、安全、工具类
│   │   └── consumer/                 # RabbitMQ 消费者
│   └── src/main/resources/
│       ├── application.yml / application-dev.yml / application-prod.yml
│       ├── mapper/*.xml              # MyBatis XML
│       └── db/migration/            # Flyway SQL（V1__init_schema.sql...）
│
├── app/                              # 移动端 uniapp X（待实现）
│
├── docs/assets/                      # logo 等静态资源
├── supabase/                         # 旧版 Supabase 迁移脚本（归档）
│
├── AGENTS.md / CLAUDE.md             # 编码规范
├── 架构设计文档.md                    # 完整架构方案
├── 架构改进分析.md                    # 竞品对标分析
├── 产品设计文档.md                    # 产品功能定义
├── UI设计规范文档.md                  # UI 设计规范
├── 技术开发规范.md                    # 编码规范
├── ui-prototype.html                  # 移动端 UI 原型
└── README.md
```

---

## 四、前端架构

### 4.1 响应式布局

| 断点 | 宽度 | 导航 | Feed 列数 |
|------|------|------|----------|
| Mobile | < 768px | 底部 TabBar | 2 列瀑布流 |
| Tablet | 768-1023px | 底部 TabBar | 3 列瀑布流 |
| Desktop | 1024-1279px | 顶部 Nav + 侧边栏 | 3 列 |
| Wide | ≥ 1280px | 顶部 Nav + 侧边栏 | 4-5 列 |

### 4.2 Flow 清单（4 Flow · 已实现）

| Flow | 屏幕数 | 核心交互 |
|------|--------|---------|
| 首页 Feed | 3 | 瀑布流 + 标签筛选 + 搜索 + 侧边栏热门话题 |
| 帖子详情 | 3 | 图片轮播 + 商品信息卡 + 评论树 + 作者侧栏 |
| 发布分享 | 3 | 图片上传 → 商品信息（来源/价格/评分）→ 正文 + 预览 |
| 私信通知 | 3 | 对话列表 + 聊天分栏 + 互动通知 |

### 4.3 设计系统 Token

```css
@theme inline {
  --color-coral: #FF6B35;        /* 品牌主色 */
  --color-coral-light: #FFF0E8;  /* 高亮背景 */
  --color-coral-dark: #E55A2B;   /* 悬停加深 */
  --color-warm-bg: #FAF7F5;      /* 页面背景 */
  --color-warm-100: #F5EDE8;     /* 卡片分隔 */
  --color-warm-200: #E8DDD5;     /* 边框 */
}
```

---

## 五、后端架构

### 5.1 分层设计

```
Controller（参数校验 + 调用 Service + 返回 R<T>）
    ↓
Service（业务逻辑 + 事务管理）
    ↓
Mapper（MyBatis-Plus CRUD + 自定义 XML SQL）
    ↓
MySQL 8.0 + Redis 7 + Elasticsearch 8
```

### 5.2 统一响应

```java
public class R<T> {
    private int code;       // 0=成功，1xxx=业务错误
    private String message; // 用户可见消息
    private T data;

    public static <T> R<T> ok(T data) { ... }
    public static <T> R<T> fail(int code, String message) { ... }
    public static <T> R<T> fail(ErrorCode error) { ... }
}
```

### 5.3 错误码规范

| 范围 | 模块 | 示例 |
|------|------|------|
| 1000-1999 | 认证 | TOKEN_EXPIRED, NO_PERMISSION, ACCOUNT_BANNED |
| 2000-2999 | 用户 | USER_NOT_FOUND, USERNAME_EXISTS |
| 3000-3999 | 帖子 | POST_NOT_FOUND, POST_NO_EDIT |
| 4000-4999 | 评论 | COMMENT_NOT_FOUND |
| 5000-5999 | 上传 | FILE_TOO_LARGE, FILE_TYPE_INVALID |
| 9000-9999 | 系统 | SYSTEM_ERROR, RATE_LIMITED |

### 5.4 认证机制

```
注册 → BCrypt 哈希密码 → 写入 users → 签发 TokenPair
登录 → BCrypt 比对 → 签发 TokenPair

access_token:  JWT (HS256), 30 分钟过期, payload={userId, role}
refresh_token: UUID 随机串, 7 天过期, 存 Redis
```

### 5.5 API 路由设计

```
认证：POST /api/v1/auth/{register,login,refresh,logout,wechat}
用户：GET/PUT /api/v1/users/me, GET /api/v1/users/:id
帖子：GET /api/v1/posts, GET/POST/PUT/DELETE /api/v1/posts/:id
互动：POST/DELETE /api/v1/posts/:id/{like,favorite}
评论：GET/POST /api/v1/posts/:id/comments, DELETE /api/v1/comments/:id
关注：POST/DELETE /api/v1/users/:id/follow
标签：GET /api/v1/tags, POST/DELETE /api/v1/tags/:name/favorite
搜索：GET /api/v1/search, GET/DELETE /api/v1/search/history
上传：POST /api/v1/upload/{image,images}
管理：GET/PUT /api/v1/admin/users/:id/{ban,unban,role}
```

---

## 六、数据库设计（MySQL 8.0）

### 6.1 V1 初始结构（历史基线）

> 下方为首版基线 DDL 摘要。生产有效结构必须按 `V1__init_schema.sql` + `V2__optimize_schema_and_indexes.sql` 顺序执行；V2 会移除外键、重建索引并补充标签关系和私信表。

```sql
-- 用户
CREATE TABLE users (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(255) DEFAULT NULL UNIQUE,
    phone VARCHAR(20) DEFAULT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    nickname VARCHAR(50) NOT NULL,
    avatar_url VARCHAR(500) DEFAULT NULL,
    bio VARCHAR(500) DEFAULT NULL,
    post_count INT NOT NULL DEFAULT 0,
    follower_count INT NOT NULL DEFAULT 0,
    following_count INT NOT NULL DEFAULT 0,
    role TINYINT NOT NULL DEFAULT 0 COMMENT '0=用户 1=管理员',
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0=正常 1=封禁 2=注销',
    openid VARCHAR(100) DEFAULT NULL UNIQUE COMMENT '微信openid',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 帖子
CREATE TABLE posts (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NOT NULL,
    title VARCHAR(200) NOT NULL,
    content TEXT NOT NULL,
    images JSON NOT NULL COMMENT '图片URL数组',
    tags JSON DEFAULT NULL COMMENT '标签数组',
    product_name VARCHAR(200) DEFAULT NULL,
    product_price DECIMAL(10,2) DEFAULT NULL,
    product_source VARCHAR(50) DEFAULT NULL,
    product_rating TINYINT DEFAULT NULL COMMENT '1-5',
    like_count INT NOT NULL DEFAULT 0,
    comment_count INT NOT NULL DEFAULT 0,
    favorite_count INT NOT NULL DEFAULT 0,
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0=公开 1=隐藏 2=删除',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_created (user_id, created_at DESC),
    INDEX idx_status_created (status, created_at DESC),
    FULLTEXT INDEX ft_title_content (title, content) WITH PARSER ngram,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 评论（支持一级嵌套）
CREATE TABLE comments (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    post_id BIGINT UNSIGNED NOT NULL,
    user_id BIGINT UNSIGNED NOT NULL,
    parent_id BIGINT UNSIGNED DEFAULT NULL COMMENT '父评论ID，NULL=顶级',
    content TEXT NOT NULL,
    reply_count INT NOT NULL DEFAULT 0,
    like_count INT NOT NULL DEFAULT 0,
    status TINYINT NOT NULL DEFAULT 0,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_post_created (post_id, created_at),
    INDEX idx_parent (parent_id),
    FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (parent_id) REFERENCES comments(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 点赞
CREATE TABLE likes (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NOT NULL,
    post_id BIGINT UNSIGNED NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_post (user_id, post_id),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 收藏
CREATE TABLE favorites (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NOT NULL,
    post_id BIGINT UNSIGNED NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_post (user_id, post_id),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 关注
CREATE TABLE follows (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    follower_id BIGINT UNSIGNED NOT NULL,
    following_id BIGINT UNSIGNED NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_follower_following (follower_id, following_id),
    FOREIGN KEY (follower_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (following_id) REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 标签字典
CREATE TABLE tags (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL UNIQUE,
    post_count INT NOT NULL DEFAULT 0,
    status TINYINT NOT NULL DEFAULT 0 COMMENT '0=正常 1=禁用',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 通知
CREATE TABLE notifications (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NOT NULL COMMENT '接收者',
    type VARCHAR(30) NOT NULL COMMENT 'like/comment/follow/system',
    actor_id BIGINT UNSIGNED DEFAULT NULL COMMENT '触发者',
    target_type VARCHAR(20) DEFAULT NULL,
    target_id BIGINT UNSIGNED DEFAULT NULL,
    content VARCHAR(500) DEFAULT NULL,
    is_read TINYINT NOT NULL DEFAULT 0,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_read (user_id, is_read, created_at DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 6.2 迁移管理

使用 Flyway 管理数据库迁移：
- 文件位置：`server/src/main/resources/db/migration/`
- 命名规范：`V{N}__{description}.sql`（如 `V1__init_schema.sql`）
- Spring Boot 启动时自动执行未跑过的迁移
- 已执行：V1 初始化结构、V2 表结构与索引优化、V3 Feed 主键游标优化

### 6.3 V2/V3 当前有效结构与索引

`V2__optimize_schema_and_indexes.sql` 完成表结构、关系模型和索引治理；`V3__stabilize_feed_cursor.sql` 将主 Feed 收敛为高效的主键游标：

| 表/模块 | 当前设计 | 核心索引或约束 |
|---------|---------|---------------|
| users | 用户名、昵称唯一；角色/状态/计数增加 CHECK | `username`、`uk_user_nickname` |
| posts | 图片和标签均为非空 JSON 数组；计数、价格、评分增加 CHECK | `idx_post_feed(status, id DESC)`、`idx_post_hot(status, like_count DESC, id DESC)`、`idx_post_user_feed(user_id, status, created_at DESC, id DESC)` |
| comments | 一级嵌套由应用层校验；补 updated_at 与稳定排序 | `idx_comment_post_feed`、`idx_comment_parent_feed`、`idx_comment_user_feed` |
| likes/favorites | `(user_id, post_id)` 唯一；补帖子反向查与用户时间线 | 唯一索引 + `idx_*_post` + `idx_*_user_feed` |
| follows | 防止自关注；双向列表按时间稳定排序 | `idx_follow_follower_feed`、`idx_follow_following_feed` |
| notifications | 同时支持全部时间线与未读列表 | `idx_notification_user_feed`、`idx_user_read` |
| post_tag_rela | JSON 标签用于展示，关系表负责高效筛选和统计 | `uk_post_tag`、`idx_tag_post`、`idx_tag_time_post` |
| chat_* | 会话、成员、消息三表支撑私信 | 会话键唯一、用户会话索引、会话消息索引 |

**索引策略**：

1. Feed V3 使用 `(status, id DESC)`，以自增 `id` 作为不透明游标；它与发帖写入顺序一致，可做索引范围扫描且避免 filesort。
2. 登录用户 Feed 在单条 SQL 中通过 `EXISTS` 子查询返回点赞/收藏状态，均命中 `(user_id, post_id)` 唯一覆盖索引，消除应用层每页 `2N` 次查询。
3. Feed 列表不查询 `content` 大字段；详情页单独查询正文，降低网络传输和 InnoDB 回表成本。
4. 点赞、收藏、发帖数采用原子 SQL `SET count = GREATEST(count + delta, 0)`，避免并发读改写丢失。
5. 数据库不使用外键，按项目规范由 Service 事务和唯一约束维护一致性，降低高并发写入与迁移耦合。
6. `sys.schema_redundant_indexes` 审计结果为空，已移除 `users.idx_username` 等重复索引。

**当前 Feed 执行计划**：
- `posts` 使用 `idx_post_feed`，游标条件使用完整的 `status + id` key parts，无 filesort。
- `STRAIGHT_JOIN` 固定 posts 为驱动表，作者关联使用 users 主键。
- 点赞/收藏 `EXISTS` 使用 `(user_id, post_id)` 唯一覆盖索引单行查找。
- `using_filesort = false`。

---

## 七、部署架构

### 7.1 本地开发（Docker Compose）

```yaml
services:
  mysql:    # MySQL 8.0
  redis:    # Redis 7
  elasticsearch:  # ES 8
  rabbitmq: # RabbitMQ 3.13
  minio:    # 对象存储
  api:      # Spring Boot API
  web:      # Nginx 静态托管
```

### 7.2 生产部署（Kubernetes）

```
Ingress (Nginx) → Spring Cloud Gateway → API Pods (HPA 2-20)
                                              ↓
                                    MySQL (主从) + Redis + ES + RabbitMQ
```

- API Deployment：3 副本起步，HPA 自动扩缩（CPU 65% / 内存 75%）
- MySQL：主从复制 + 读写分离
- Redis：Sentinel 哨兵模式
- 静态前端：Nginx Deployment + CDN

---

## 八、旧版架构归档（Supabase 时期）

> 以下为 2026-06 至 2026-08 期间的 Supabase 单体架构历史记录，仅供参考。

### 8.1 旧技术栈
- Next.js 14 (App Router) + Supabase (PostgreSQL + Auth + Storage)
- 部署到 EdgeOne Pages 静态导出

### 8.2 迁移历史（v1-v23）
共 26 个迁移脚本保存在 `supabase/` 目录，涵盖：
- v1-v5：基础 schema + 收藏 + 管理员 + 标签
- v6-v7：状态字段 SMALLINT 化
- v8：解封恢复帖子 bug 修复
- v9-v10：索引重建 + 审计
- v11：评论一级嵌套
- v12-v14：索引清理 + Feed RPC + Storage
- v15-v18：游标分页 + Toggle RPC + 性能优化
- v19-v20：标签收藏 RPC + 死代码清理
- v21-v23：active_posts 视图 + Feed JOIN 优化

### 8.3 已知教训（已在新架构中规避）
- Supabase SQL Editor 整段粘贴禁用 `CREATE INDEX CONCURRENTLY`
- RLS OR 条件阻止 partial index → 改用 SECURITY DEFINER 函数
- 状态字段用 SMALLINT + 代码常量，禁止 TEXT + CHECK
- `DROP FUNCTION` 前必须 DROP 所有引用它的 RLS 策略

---

## 九、后续规划

| 优先级 | 功能 | 说明 |
|--------|------|------|
| P0 | Spring Boot 后端实现 | 用户认证 + 帖子 CRUD + 评论 + 互动 |
| P0 | MySQL 数据库初始化 | Flyway 迁移 + 索引优化 |
| P0 | 前后端联调 | API 对接 + JWT 认证集成 |
| P1 | 内容审核系统 | AI 文本/图片审核 + 人工审核工作台 |
| P1 | 图片处理流水线 | 多分辨率缩略图 + CDN 分发 |
| P1 | 通知系统 | WebSocket 实时推送 + 离线消息 |
| P2 | 推荐系统 | 热门排序 → 标签匹配 → 深度学习 |
| P2 | 搜索增强 | 查询建议 + 纠错 + 语义搜索 |
| P2 | uniapp 移动端 | 微信小程序 + App |
| P3 | 数据平台 | 埋点 + ClickHouse + 分析看板 |
