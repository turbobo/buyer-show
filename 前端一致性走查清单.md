# 前端一致性走查清单（U36）

> **用途**：把「一致」从靠自觉变为**可执行机制**。新功能提交前、每周例行、发版前按本清单走查。
> **配套**：`frontend/npm run check:ui`（静态检查，error 将阻断）；`buyer-show/scripts/smoke-test.py`（接口冒烟）；`frontend/node_modules/.bin/oxlint`（代码 lint）。
> **基线**：`UI设计规范文档.md`（v1.3）+ `frontend/DESIGN_THEME.md`（前端契约）。

---

## 一、自动检查（每次提交前，≤1 分钟）

| # | 命令 | 期望 |
|---|------|------|
| 1 | `cd frontend && npm run check:ui` | **error = 0**（裸色值 0；z-index 越界 0） |
| 2 | `cd frontend && npx tsc -b` | 无类型错误 |
| 3 | `cd buyer-show && python3 scripts/smoke-test.py` | 23/23 通过（接口回归） |

## 二、人工走查（新功能 / 发版前）

### 1. 三态与反馈（U34）
- [ ] Loading：>300ms 的操作有反馈（页面级用 `LoadingState`，列表骨架屏）
- [ ] Empty：空列表使用 `EmptyState`（图标 + 标题 + 描述 + 可选 CTA），不手写
- [ ] Error：就近展示 + 重试（`ErrorState`），页面级错误不白屏
- [ ] 提交类按钮：loading 态 + 防连点；成功/失败有 toast

### 2. 弹窗与确认（U33/U34）
- [ ] 确认类弹窗统一 `ConfirmDialog`（autoFocus 在「取消」；破坏性按钮 destructive）
- [ ] 表单类弹层统一 `AppDialog`（Esc/遮罩关闭/焦点陷阱内置）
- [ ] 不新增自撸 `fixed inset-0` 弹窗（特殊复合弹层需在评审中说明）

### 3. 导航（U35）
- [ ] 频道/菜单变更只改 `src/lib/navigation.ts`（DesktopHeader / AppTabBar / Feed 导航同源）
- [ ] PC 菜单顺序：[logo][首页][消息][搜索][主题][审核台][发布][用户区]，发布在身份区左侧
- [ ] 移动：Stack 页有「← 返回 + ⌂ 首页」；TabBar 隐藏规则生效（/、/posts/*、/publish、/login、/admin/*）

### 4. 视觉与 Token（U32）
- [ ] 颜色/间距/圆角/阴影用 token 与 4px 刻度（裸色值会被 check:ui 拦截）
- [ ] 浅珊瑚底文字用 `text-coral-contrast`（不是 text-coral）
- [ ] 层叠取值对照梯度：50 < 55 < 60（弹层）< 9999（图片预览）
- [ ] 暗色模式过一遍（token 化自动生效；无硬编码白底）

### 5. 可访问性与触控（U28-U31 基线）
- [ ] 图标按钮有 `aria-label`；图片有 `alt`；每页可见唯一 `h1`
- [ ] 主操作触控 ≥44（TabBar/导航/CTA/弹窗按钮）；次级 ≥36–40
- [ ] Esc 可关闭弹层；Tab 焦点环可见；破坏性操作二次确认

### 6. 动效与性能
- [ ] 微交互 150–300ms；仅 transform/opacity；页面容器无 transform（红线）
- [ ] 列表图 `loading="lazy"` + `alt` + 失败兜底
- [ ] 长列表分页/虚拟化符合现状策略

---

## 三、周例行走查（15 分钟）

1. 跑「一、自动检查」三项
2. 浏览器（390 / 1440 双尺寸）抽查：首页、详情、消息、个人主页、发布、管理后台
3. `check:ui` 输出的 warn 项逐条判断：修复 or 记录豁免理由（豁免写入本节下方）
4. 更新 `产品设计文档.md` 与本文档（如新增约定）

### 豁免记录
| 位置 | 原因 | 复核时间 |
|------|------|---------|
| `flows/post-detail/detail.tsx` U23 浮层（3 处 z-[60] 容器） | 页面级浮层（非对话框），内部含页面导航 | 2026-09 建立 |
| `flows/admin/users.tsx` 帖子预览弹窗 | 复合结构（图片头 + 滚动内容 + 操作行），不适合标准 Dialog API | 2026-09 建立 |
| `flows/auth/login.tsx` 验证码图 `w-[120px]` / 骨架 `h-[68px]` 等 4 处任意像素 | 图片/骨架固有尺寸 | 2026-09 建立 |

---

*版本 v1.0 | 2026-09-17 | 来源：G 组前端一致性体系（U32-U36）*
