# 买家说 — 产品设计文档

> 购物种草社区多端应用（PC Web + 手机浏览器 + 小程序），让用户分享真实购物体验，发现好物推荐。
> 版本：v2.0 | 更新：2026-09-07 | 架构：前后端分离 Monorepo

---

## 一、产品定位

**一句话描述**：真实买家的购物分享社区，帮你买到真正值得的好物。

**目标用户**：
- 18-35 岁年轻消费者，习惯网购
- 喜欢看真实用户评价再做购买决策
- 乐于分享自己的购物心得和使用体验

**核心价值**：
- 对读者：看到真实、可信的购物评价，避免踩坑
- 对作者：记录购物体验，获得社区认同
- 对品牌：获取真实用户反馈和口碑传播

---

## 二、功能全景图

```
买家说
├── 🏠 发现（首页）
│   ├── 瀑布流帖子列表
│   ├── 标签分类筛选
│   ├── 下拉刷新
│   ├── 触底加载更多
│   └── 骨架屏加载
│
├── 📄 帖子详情
│   ├── 图片轮播
│   ├── 商品信息（名称/价格/评分）
│   ├── 点赞互动
│   ├── 评论列表
│   └── 发表评论
│
├── ✏️ 发布
│   ├── 多图上传
│   ├── 标题/正文编辑
│   ├── 商品信息（选填）
│   ├── 星级评分
│   └── 标签选择
│
├── 🔍 搜索
│   ├── 关键词搜索
│   ├── 热门标签
│   └── 搜索历史
│
├── 👤 个人中心
│   ├── 用户信息卡片
│   ├── 数据统计（发布/粉丝/关注）
│   ├── 菜单分组（内容管理/社交互动/账号设置）
│   ├── 关注与粉丝列表
│   ├── 他人主页（/user/[id]）
│   └── 退出登录/注销账号
│
└── 🔐 认证体系
    ├── 匿名快捷登录
    ├── 手机号登录（待开发）
    └── 微信登录（待开发）
```

---

## 三、已完成功能

### 3.1 基础设施 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| Next.js 14 + React 18 | ✅ 完成 | App Router，静态导出 |
| TypeScript strict 模式 | ✅ 完成 | 全量类型覆盖 |
| Tailwind CSS 设计系统 | ✅ 完成 | 珊瑚红主题色、字体层级、阴影体系 |
| Framer Motion 动效 | ✅ 完成 | 集中式 variants，列表淡入、心跳、骨架屏 |
| Zustand 状态管理 | ✅ 完成 | user store + UI store |
| Supabase 数据库 | ✅ 完成 | 7 张表 + RLS + 触发器 + 索引 |
| Supabase Auth | ✅ 完成 | 匿名登录 + 会话持久化（localStorage），刷新/重开不掉登录 |
| 响应式布局 | ✅ 完成 | 手机/平板/桌面/大屏四档适配 |

### 3.2 页面功能

#### 首页 / 发现 ✅

| 功能 | 状态 | 数据来源 |
|------|------|----------|
| 瀑布流帖子列表 | ✅ 完成 | Supabase posts 表 |
| 标签分类筛选 | ✅ 完成 | Supabase GIN 索引查询 |
| 骨架屏加载 | ✅ 完成 | shimmer 动画 |
| 下拉刷新 | ✅ 完成 | 重新拉取第 1 页 |
| 触底加载更多 | ✅ 完成 | 分页查询，每页 20 条 |
| 帖子卡片 hover 预加载 | ✅ 完成 | router.prefetch |
| 首屏图片 eager 加载 | ✅ 完成 | 前 2 张 eager，其余 lazy |
| 空状态提示 | ✅ 完成 | 统一 EmptyState 组件 |
| 回到顶部按钮 | ✅ 完成 | 滚动超过 400px 显示 |

#### 帖子详情页 ✅

| 功能 | 状态 | 数据来源 |
|------|------|----------|
| 图片轮播 | ✅ 完成 | Supabase posts.images |
| 商品信息展示 | ✅ 完成 | Supabase posts 表 |
| 星级评分展示 | ✅ 完成 | Supabase posts.rating |
| 标签展示 | ✅ 完成 | Supabase posts.tags |
| 点赞按钮 | ✅ 完成 | Supabase likes 表 + 触发器更新 like_count |
| 收藏按钮 | ✅ 完成 | Supabase favorites 表 + 触发器更新 favorite_count |
| 评论列表 | ✅ 完成 | Supabase comments 表 |
| 发表评论 | ✅ 完成 | Supabase comments 表，触发器更新 comment_count |
| 评论回复（一级嵌套） | ✅ 完成 | `comments.parent_id` 外键（`ON DELETE SET NULL`）+ `reply_count` 触发器 + `trg_check_one_level_reply` 强制一级嵌套 + `comments_insert` RLS（当前用户未封禁 + 父评论作者未封禁，详见 `migration-v11-comment-replies.sql`） |
| 评论删除 | ✅ 完成 | 作者可见删除按钮，二次确认弹窗，`comments_delete_own` RLS；删父评论时回复保留（`parent_id` 置 NULL，前端显示「原评论已删除」占位，参考小红书/抖音） |
| 桌面端评论输入 | ✅ 完成 | CommentSection 内联输入框（`hidden md:block`），移动端保留底部固定栏 |
| 评论收藏 | ✅ 完成 | CommentItem 右侧⭐按钮，`favorite_comments` 表触发 favorite_count |
| 登录守卫 | ✅ 完成 | useAuthGuard + LoginSheet |
| SEO Meta 标签 | ✅ 完成 | generateMetadata 动态生成 |
| JSON-LD 结构化数据 | ✅ 完成 | Schema.org Review |
| PC 端左图右文分栏 | ✅ 完成 | 响应式 flex 布局 |

#### 发布页 ✅

| 功能 | 状态 | 数据来源 |
|------|------|----------|
| 多图选择 | ✅ UI 完成 | 本地预览（Storage 上传未接入） |
| 标题/正文输入 | ✅ 完成 | 前端校验 2-50 字 / 10-2000 字（与数据库 CHECK 约束一致） |
| 商品名称/价格 | ✅ 完成 | 选填字段 |
| 星级评分 | ✅ 完成 | 1-5 星交互组件 |
| 标签选择 | ✅ 完成 | 最多 3 个，HOT_TAGS |
| 发布提交 | ✅ 完成 | Supabase createPost |
| 登录检查 | ✅ 完成 | 未登录弹出 LoginSheet |
| 成功反馈 | ✅ 完成 | Toast + 动画 + 跳转首页 |

#### 搜索页 ✅

| 功能 | 状态 | 数据来源 |
|------|------|----------|
| 搜索输入框 | ✅ UI 完成 | — |
| 热门标签展示 | ✅ UI 完成 | 本地常量 |
| 搜索历史 | ✅ 完成 | Supabase search_history 表 |
| 搜索结果列表 | ✅ 完成 | Supabase searchPosts，匹配 title/content/product_name/tags |
| 标签直搜（点击热门标签命中 tags 数组） | ✅ 完成 | PostgREST cs 操作符 |
| 热门标签收藏 | ✅ 完成 | 每个 HOT_TAG 右侧⭐按钮，`favorite_tags` 表持久化 |
| 搜索防抖 300ms | ✅ 完成 | — |

#### 个人中心 ✅

| 功能 | 状态 | 数据来源 |
|------|------|----------|
| 用户信息卡片 | ✅ 完成 | Supabase profiles 表 |
| 数据统计 | ✅ 完成 | profiles 表计数器 |
| 我的发布 | ✅ 完成 | PostCard 右上角三点菜单（编辑/删除），符合主流 App 习惯；删除走二次确认弹窗 |
| 帖子编辑 | ✅ 完成 | `/post/[id]/edit` 路由，作者归属双重校验（前端 + 服务端） |
| 帖子删除 | ✅ 完成 | 二次确认弹窗，service 层归属校验 |
| 我的收藏 | ✅ 完成 | `/profile/favorites`，Tab 切换：帖子 / 评论 / 标签 |
| 我的评论 | ✅ 完成 | `/profile/comments`，Tab 切换：按时间 / 按帖子聚合，支持删除评论 |
| 我的点赞 | ✅ 完成 | `/profile/likes`，展示用户点赞过的帖子列表 |
| 编辑资料 | ✅ 完成 | `/profile/edit` 路由，昵称去重 + 实时校验 + 头像 base64 兜底（Storage 待接入） |
| 注销账号 | ✅ 完成 | 二次确认 → 帖子置 deleted + 昵称匿名占位 + 登出（auth.users 真删除待 Edge Function） |
| 管理后台入口 | ✅ 完成 | 个人中心菜单顶部「管理后台」条目（仅 `role='admin'` 可见） |
| 关注与粉丝 | ✅ 完成 | `/profile/follow`：关注/粉丝双 Tab 列表；`FollowButton` 可复用组件（sm/md 尺寸 + 登录守卫 + 加载态） |
| 他人主页 | ✅ 完成 | `/user/[id]`：用户卡片 + 帖子网格 + FollowButton + 粉丝计数实时同步 |
| 返回按钮优化 | ✅ 完成 | `smartBack` 统一返回逻辑（history + referrer 双重判断 + fallback 兜底）；PC TopNav 扩展覆盖 `/profile/*` 子页；全站 7 处页面统一接入 `smartBack`；`/auth/reset-password` 新增返回按钮 |
| 登录/退出 | ✅ 完成 | Supabase Auth |
| 未登录卡片 | ✅ 完成 | 快捷登录引导 |
| 菜单项 | ✅ 完成 | 三组分类（内容管理 / 社交互动 / 账号设置），退出登录与注销账号整合进菜单 |

#### 管理后台 ✅（仅管理员可见）

| 功能 | 状态 | 数据来源 |
|------|------|----------|
| 权限守卫 `useAdminGuard` | ✅ 完成 | 登录 + `role='admin'` + `status≠'banned'` 三层校验 |
| 独立布局（隐藏前台 TabBar/TopNav） | ✅ 完成 | `ConditionalNav` 按 `/admin/*` 路径切换 |
| 总览首页 `/admin` | ✅ 完成 | 用户总数卡片 + 快捷入口 |
| 用户列表 `/admin/users` | ✅ 完成 | 分页 + 昵称/ID 搜索（防抖 300ms） |
| 封禁 / 解封 / 提权 | ✅ 完成 | Supabase RPC（`ban_user` / `unban_user` / `promote_to_admin`）；封禁连带软删 active 帖子，解封按 `posts.original_status` 精确还原（保留管理员主动 HIDDEN 语义） |
| 自举提权 | ✅ 完成 | 系统无管理员时允许任意登录用户自提为 admin |
| 默认管理员账号 | ✅ 完成 | `admin@buyer-show.local` / `admin123`，由 `migration-v5.2-admin-init.sql` 通过 GoTrue signup 创建（`auth.users` + `profiles` 一致性由 GoTrue 维护） |
| 标签管理 | ✅ 完成 | `/admin/tags`：新建/重命名/合并/删除，`tags` 表 + 4 个 RPC 同步更新 `posts.tags` 数组 |

### 3.3 全局组件 ✅

| 组件 | 状态 | 说明 |
|------|------|------|
| Toast 通知 | ✅ 完成 | 全局单例，顶部滑入，自动消失 |
| Modal 确认弹窗 | ✅ 完成 | 遮罩 + 双按钮 + 危险操作红色 |
| LoginSheet 登录面板 | ✅ 完成 | 底部弹出，Supabase 匿名登录，登录态自动持久化 |
| AuthProvider 全局认证 | ✅ 完成 | 挂载于 RootLayout，启动时恢复 session 并订阅 onAuthStateChange |
| EmptyState 空状态 | ✅ 完成 | 图标 + 标题 + 描述 + 操作按钮 |
| Skeleton 骨架屏 | ✅ 完成 | shimmer 动画，固定高度避免 hydration mismatch |
| SmartImage 智能图片 | ✅ 完成 | 加载中骨架屏 + 错误占位图 |
| PostCard 帖子卡片 | ✅ 完成 | 封面图 + 标题 + 价格 + 评分 + 标签 + 用户；可选 `ownerActions` 开启右上角三点菜单（编辑/删除） |
| TabBar 底部导航 | ✅ 完成 | 4 tab + 发布按钮凸起 |
| TopNav 顶部导航 | ✅ 完成 | PC 端 Logo + 导航 + 详情页返回按钮 |

### 3.4 安全与性能 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| RLS 行级安全策略 | ✅ 完成 | 7 张表全部配置 |
| 无障碍 aria-label | ✅ 完成 | 所有图标按钮、导航、交互元素 |
| focus-visible 焦点环 | ✅ 完成 | 珊瑚红 outline |
| prefers-reduced-motion | ✅ 完成 | 动画降级 |
| 数据库索引重建 | ✅ 完成 | `migration-v9-rebuild-posts-indexes.sql`：posts/comments/likes/favorites/follows/search_history 补建 partial/复合索引，首页从 1.5s+ → 50-150ms |
| 404 页面 | ✅ 完成 | not-found.tsx |
| 全局 ErrorBoundary | ✅ 完成 | error.tsx + 重试按钮 |
| SEO Open Graph | ✅ 完成 | 每帖独立 meta 标签 |

---

## 四、待开发功能

### 4.1 P0 — 核心功能闭环（已完成）

当前迭代已全部完成：详情页/评论/点赞/搜索/搜索历史均已接入 Supabase，对应功能状态见第三章「页面功能」表格。

### 4.2 P1 — 图片与内容

| # | 功能 | 说明 | 预计工作量 |
|---|------|------|-----------|
| 6 | **Storage 图片上传** | 解决 storage.objects 权限问题后，接入 uploadImage | 2h |
| 7 | **图片压缩** | 上传前压缩到 500KB 以内，转 WebP | 1h |
| 8 | **全屏图片预览** | 点击封面图全屏查看，左右滑动，双击放大 | 1.5h |
| 9 | **内容安全审核** | 接入 Supabase Edge Function + 敏感词过滤 | 2h |

### 4.3 P2 — 社交与互动

| # | 功能 | 说明 | 预计工作量 |
|---|------|------|-----------|
| 11 | **关注/粉丝** ✅ | 关注按钮 + follows 表 + 双向计数 + `/profile/follow` 列表 | 已完成 |
| 12 | **他人主页** ✅ | /user/[id] 路由，查看别人的帖子和资料 + FollowButton | 已完成 |
| 13 | **评论点赞** | 评论可点赞，独立计数 | 1h |
| 14 | **回复评论** ✅ | 一级嵌套回复，`parent_id` + `reply_count` 触发器 | 已完成 |
| 15 | **通知系统** | 点赞/评论/关注通知，红点提醒 | 3h |
| 16 | **分享功能** | 微信分享卡片 + 复制链接 + 生成海报 | 2h |

### 4.4 P2 — 认证升级

| # | 功能 | 说明 | 预计工作量 |
|---|------|------|-----------|
| 17 | **手机号 + 验证码登录** | 接入短信服务（阿里云/腾讯云短信） | 2h |
| 18 | **微信 OAuth 登录** | 微信开放平台 H5 授权 | 3h |
| 19 | **邮箱 + 密码登录** | Supabase 内置 Auth | 1h |
| 21 | **密码找回** | Supabase 邮件重置 | 0.5h |

### 4.5 P3 — 增长与体验

| # | 功能 | 说明 | 预计工作量 |
|---|------|------|-----------|
| 22 | **PWA 离线支持** | Service Worker 缓存已浏览帖子 | 2h |
| 23 | **虚拟滚动** | 帖子超过 50 条时使用，减少 DOM 节点 | 1.5h |
| 24 | **暗色模式** | Tailwind dark mode + 手动切换 | 3h |
| 25 | **推荐算法** | 基于标签偏好 + 热门排序 | 3h |
| 26 | **举报功能** | 举报帖子/评论，后台审核 | 2h |

### 4.6 P3 — 部署与运维

| # | 功能 | 说明 | 预计工作量 |
|---|------|------|-----------|
| 28 | **EdgeOne Pages 部署** | 推送代码，自动构建部署 | 1h |
| 29 | **自定义域名** | 绑定域名 + SSL | 1h |
| 30 | **EdgeOne Analytics** | 接入访问统计 | 0.5h |
| 31 | **错误监控** | 全局 ErrorBoundary 上报错误日志 | 1h |
| 32 | **sitemap.xml** | 自动生成帖子 sitemap 提交搜索引擎 | 1h |

---

## 五、功能完成度总览

```
前端 Web       ████████████████████ 100%  (Vite + React + shadcn/ui，真实 API、路由、审核台与认证页已接入)
后端 API       ██████████████████░░  90%  (认证、帖子、评论、审核、举报与 MinIO 上传 API 已实现)
移动端 App     ░░░░░░░░░░░░░░░░░░░░   0%  (uniapp X 待实现)
数据库         ████████████████████ 100%  (MySQL 8.0，Flyway V1-V5 已执行并验证)
内容审核       ████████████████░░░░  80%  (本地敏感词、审核台、举报处置已实现；第三方图片审核待接入)
通知系统       ████░░░░░░░░░░░░░░░░  20%  (通知与私信表结构完成，服务层待实现)
推荐系统       ░░░░░░░░░░░░░░░░░░░░   0%
部署上线       ████████░░░░░░░░░░░░  40%  (Docker Compose 可用，K8s 待配置)
```

**整体完成度：约 35%（Web UI、核心 API 与数据库已可运行；移动端、审核、推荐和 K8s 待实现）**

---

## 六、数据库表结构（MySQL 8.0）

> 旧版 Supabase/PostgreSQL 迁移脚本已归档在 `supabase/` 目录。新版使用 MySQL 8.0 + Flyway 迁移管理。
> 完整 DDL 见 `技术方案文档.md` §六。

| 表名 | 用途 | 状态 |
|------|------|------|
| users | 用户资料（昵称/头像/简介/统计/角色/状态） | ✅ V1 建表 + V2 唯一约束/校验优化 |
| posts | 帖子（标题/内容/图片JSON/标签JSON/商品信息/计数） | ✅ V1 建表 + V2 Feed/热门/用户索引 |
| comments | 评论（支持一级嵌套 parent_id + reply_count） | ✅ V1 建表 + V2 稳定排序索引 |
| likes | 帖子点赞（UNIQUE user+post） | ✅ V1 建表 + V2 双向查询索引 |
| favorites | 帖子收藏（UNIQUE user+post） | ✅ V1 建表 + V2 用户时间线索引 |
| follows | 关注关系（follower/following 双向） | ✅ V1 建表 + V2 双向时间线索引 |
| tags | 标签字典（名称唯一 + post_count） | ✅ V1 建表 + V2 校验/更新时间 |
| post_tag_rela | 帖子与标签多对多关系（高效筛选/统计） | ✅ V2 新增 |
| notifications | 通知（点赞/评论/关注/系统） | ✅ V1 建表 + V2 时间线/未读索引 |
| favorite_comments | 评论收藏 | ✅ V1 建表 + V2 用户时间线索引 |
| favorite_tags | 标签收藏 | ✅ V1 建表 + V2 稳定排序索引 |
| search_history | 搜索历史（UNIQUE user+keyword） | ✅ V1 建表 + V2 稳定排序索引 |
| chat_conversation | 私信会话主表 | ✅ V2 新增 |
| chat_conversation_member_rela | 会话成员关系与未读计数 | ✅ V2 新增 |
| chat_message | 私信消息（文本/图片/商品卡片/系统） | ✅ V2 新增 |

---

## 七、技术架构

```
┌───────────────┐    ┌──────────────────────┐    ┌─────────────────────┐
│  客户端        │───▶│  API Gateway (Nginx) │───▶│  Spring Boot API    │
│  PC/手机浏览器 │    │  SSL + 限流 + 路由    │    │  Java 21            │
│  小程序        │    └──────────────────────┘    └────────┬────────────┘
└───────────────┘                                         │
                                           ┌──────────────┼──────────────┐
                                           ▼              ▼              ▼
                                      MySQL 8.0      Redis 7      Elasticsearch 8
                                      (主从读写分离)  (缓存+会话)   (全文搜索)
```

- **前端**：Vite 8 + React 19 + shadcn/ui + Tailwind CSS 4 + Zustand
- **后端**：Spring Boot 3.3 + MyBatis-Plus + Spring Security + JWT
- **数据库**：MySQL 8.0 + Redis 7 + Elasticsearch 8
- **消息队列**：RabbitMQ 3.13（图片处理、通知推送）
- **对象存储**：MinIO（自托管）/ 阿里云 OSS
- **部署**：Docker + Kubernetes + Helm 3
- **CI/CD**：GitHub Actions + ArgoCD
- **监控**：Prometheus + Grafana + ELK
- **域名**：待绑定

---

## 八、移动端 UI 设计（v1.0 新增）

### 8.1 全局导航架构

**导航模式**：底部 TabBar（4 Tab）+ Stack 导航

```
TabBar:
  ├── 🏠 首页 — Feed 瀑布流 + 标签筛选
  ├── 🔍 搜索 — 关键词 + 标签搜索
  ├── ➕ 发布 — 购物分享（中央凸起珊瑚色按钮）
  ├── 💬 消息 — 私信 + 互动通知（Tab 切换）
  └── 👤 我的 — 个人中心
```

### 8.2 Flow 清单（4 Flow · 15 Screen）

**UI 原型文件**：`ui-prototype.html`（双击浏览器打开查看全部 15 个手机屏幕）

#### Flow 1: 首页 Feed 浏览（3 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 首页 Feed | 浏览购物分享瀑布流 | Logo + 搜索入口、标签横向滚动（全部/美妆/数码/服饰/食品/家居/母婴）、双列瀑布流卡片（封面图+标题+价格标签+来源平台图标+点赞数）、下拉刷新+触底加载 |
| Screen 2 | 搜索发现 | 关键词+标签搜索 | 搜索输入框（自动聚焦）、搜索历史、热门搜索标签、猜你想搜 |
| Screen 3 | 标签详情 | 浏览特定标签下的帖子 | 标签名+帖子数量、排序切换（最新/最热/精华）、帖子瀑布流 |

**瀑布流卡片信息架构**：
- 封面图（高度自适应，120-160px）
- 标题（最多 2 行，截断）
- 价格（珊瑚色大字）
- 购物来源（绿点 + 平台名）
- 用户头像+昵称
- 点赞数（❤️ 图标 + 计数）

#### Flow 2: 发布购物分享（4 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 选图页 | 选择 1-9 张图片 | 已选预览条、相册 3 列网格、选中角标、添加按钮、相机入口 |
| Screen 2 | 商品信息 | 填写商品元数据 | 图片缩略图横滑、商品名称输入、购物来源选择器（天猫/京东/拼多多/线下/海淘/其他）、价格+数量、星级评分 1-5、购买链接（可选） |
| Screen 3 | 正文编辑 | 撰写分享正文 | 富文本区域、标签选择（热门标签+自定义+添加）、底部工具栏（图片/表情/#话题/@提及） |
| Screen 4 | 预览发布 | 检查并发布 | 帖子预览卡片（图片拼贴+标题+商品信息卡+正文摘要+标签）、存草稿按钮、发布按钮 |

**商品信息卡片设计**（发布预览 + 帖子详情中展示）：
- 商品缩略图（48px 圆角方块）
- 商品名称（单行截断）
- 来源平台（绿色 ✓ 标识）
- 星级评分
- 价格（珊瑚色大字）
- 购买链接按钮（"去购买 ›"）

#### Flow 3: 帖子详情与互动（4 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 帖子详情 | 浏览完整内容 | 全屏图片轮播（滑动切换+圆点指示器）、用户信息行+关注按钮、商品信息卡片（来源平台+价格+评分+购买链接）、正文、标签、互动统计、评论区、底部操作栏（评论输入+点赞+收藏+分享） |
| Screen 2 | 评论区 | 浏览和发表评论 | 底部弹出模态（85% 高度）、评论列表（1 级嵌套回复）、评论输入框+表情+发送、拖拽关闭 |
| Screen 3 | 商品来源 | 查看购买渠道 | 底部半屏弹出、商品大图+品牌+来源平台验证标识、价格+评分、"前往购买"按钮、复制链接按钮 |
| Screen 4 | 用户主页 | 查看作者信息 | 头像+昵称+简介、统计（分享/粉丝/关注）、关注+私信按钮、Tab 切换（分享/收藏/赞过）、帖子网格 |

#### Flow 4: 私信聊天（4 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 消息列表 | 查看所有对话和通知 | Tab 切换（私信/互动通知）、对话列表（头像+昵称+最后消息+时间+未读角标） |
| Screen 2 | 聊天页 | 实时对话 | 顶部用户信息+在线状态、消息气泡（收=白/发=珊瑚色）、商品分享卡片、底部输入栏（语音+文字+图片+发送） |
| Screen 3 | 新建对话 | 选择聊天对象 | 搜索用户、互关好友列表（在线状态）、最近联系、发消息按钮 |
| Screen 4 | 通知中心 | 系统+互动通知 | 互动通知列表（❤️点赞/💬评论/👤关注/📢系统）、帖子缩略图、一键已读 |

### 8.3 设计系统

| 维度 | 规范 |
|------|------|
| 品牌主色 | 珊瑚红 `#FF6B35`（CTA、选中态、品牌标识） |
| 辅助色 | 暖白 `#FAF7F5`（页面背景）、珊瑚浅 `#FFF0E8`（高亮背景） |
| 字体 | Noto Sans SC（中文优先系统字体栈） |
| 卡片圆角 | 16px（大卡片）、12px（按钮/输入框）、9999px（标签/药丸） |
| 阴影 | 卡片 `0 2px 8px rgba(0,0,0,0.06)`、浮层 `0 4px 16px rgba(0,0,0,0.1)` |
| 间距基数 | 4px，所有间距为 4 的倍数 |
| 触控目标 | 最小 44×44px |

---

## 代办功能

### 已完成

- [x] 2.1 显示用户发布的帖子列表（`/profile/posts`，含编辑/删除入口）
- [x] 2.2 显示用户发布的评论列表（`/profile/comments`）
- [x] 3.1 显示用户收藏的帖子列表（`/profile/favorites`）
- [x] 3.2 显示用户收藏的评论列表（`/profile/favorites` Tab = 评论，`favorite_comments` 表）
- [x] 3.4 显示用户收藏的标签列表（`/profile/favorites` Tab = 标签，`favorite_tags` 表）
- [x] 4.1 显示用户评论的帖子列表（`/profile/comments`，按帖聚合 Tab）
- [x] 4.2 显示用户评论的评论列表（`/profile/comments` Tab = 按时间，平铺列表）
- [x] 1.2 用户资料编辑 + 头像（`/profile/edit`，头像 Storage 上线后切链接）
- [x] 1.4 用户注销（软注销：帖子置 deleted + 昵称匿名占位 + 登出）
- [x] 1.6 昵称唯一性校验（前端实时去重 + DB 唯一索引兜底，见 `migration-v3-account.sql`）
- [x] 5.1 profiles.role 字段 + `profiles_admin_all` 策略（`migration-v5-admin.sql`）
- [x] 5.2 用户管理（`/admin/users`：列表/搜索/封禁/解封/提权；封禁自动隐藏 active 帖子）
- [x] 5.3 标签管理（`/admin/tags`：新建/重命名/合并/删除，`tags` 表 + 4 RPC；`migration-v5.3-tags.sql`）
- [x] 5.4 独立 admin 布局（ConditionalNav 按路径隐藏 TopNav/TabBar；AdminLayout 带侧栏与权限守卫）
- [x] 5.5 解封恢复帖子 bug 修复（`migration-v8-fix-unban-restore-posts.sql`：`posts.original_status` 暂存列 + `ban_user`/`unban_user` 重写为对称操作）
- [x] 6.1 去 RPC 化（v13–v20 未部署 RPC 全量回退 PostgREST 直查，消除线上 `function not found` 报错）

### 待办

- [ ] 1.1 邮箱/手机号注册登录（依赖短信服务接入或 Supabase 邮件认证）
- [x] 1.3 密码修改 / 忘记密码（`/profile/change-password` 修改密码 + `/auth/forgot-password` 忘记密码 + `/auth/reset-password` 邮件重置回调）
- [ ] 1.5 用户表扩展冗余字段（待业务驱动，目前 profiles 字段已覆盖核心场景）

#### 架构补齐与风险治理

- [x] P0.1 实现最小可用业务闭环：登录注册、帖子列表/详情、发布、评论、点赞与收藏均切换至真实 API；消息 Flow 保持静态展示
- [x] P0.2 完成 Flyway V4/V5 部署验证：V1-V5 已在独立 MySQL 8.0 数据库顺序执行通过，审核默认值、唯一键与索引已核验
- [x] P0.3 建立内容安全基线：本地敏感词、待人工审核状态、用户举报、管理员审核工作台与并发状态迁移已完成；第三方图片审核待接入
- [x] P1.1 完善前端应用路由：已使用 `react-router-dom` 支持帖子详情深链接、刷新恢复与路由参数
- [ ] P1.2 建立前端数据访问层：按领域拆分 `services/`，统一 HTTP 错误处理与 JWT 主动刷新；服务端数据缓存待接入
- [ ] P1.3 完善图片上传与处理：MinIO 私有 pending → 审核通过后 published 流程、JPEG/PNG 完整解码重编码、WebP 容器校验、失败清理已完成；缩略图与 CDN 待接入
- [ ] P1.4 上线互动通知：点赞、评论、关注的站内通知；支持实时推送与离线消息可靠投递
- [ ] P1.5 优化 Feed 与搜索：先实现热门排序、热搜/联想，再逐步引入标签个性化推荐与搜索纠错
- [ ] P1.6 建立基础反作弊与风控：频率限制、验证码、设备/行为识别、内容去重与防刷策略
- [ ] P1.7 改善内容页可发现性：补齐页面 meta、Sitemap、结构化数据，并评估预渲染或 SSR/SSG 方案
- [ ] P1.8 加固生产配置：移除配置文件中的硬编码密钥与开发凭据，按环境注入密钥并关闭生产 debug 日志
- [ ] P2.1 建立质量保障：补充后端单元/集成测试与前端组件/E2E 测试，覆盖核心发布和互动链路
- [ ] P2.2 建立数据分析能力：采集浏览、发布、互动与转化事件，提供基础运营分析看板

- [x] 3.3 收藏用户列表（已完全并入「关注与粉丝」`/profile/follow`，`/profile/favorites` 页移除用户 Tab 避免功能重复）

- [x] 4.1 优化页面的返回按钮（全站统一 `smartBack` 返回逻辑 + TopNav 扩展覆盖 `/profile/*` 子页 + `/auth/reset-password` 新增返回按钮）

- [x] 4.2 优化头像不超过 500KB（客户端 canvas 压缩：自动缩放至 400px + JPEG 逐级降质量，支持任意尺寸图片上传）
  
- [x] 4.3 完善封禁功能（`BannedScreen` 全屏封禁页面 + `useAuthGuard` 兜底检查，被封用户登录后看到封禁提示页，无法使用任何功能）
- 
- [x] 5.1 帖子评论功能增强（评论删除 + 一级嵌套回复 + 桌面端评论输入；`migration-v11-comment-replies.sql` 含 RLS 封禁校验）
- [x] 5.2 评论功能完善（评论回复：`parent_id`（`ON DELETE SET NULL`）+ `reply_count` 触发器 + `trg_check_one_level_reply` 一级嵌套约束 + `comments_insert` RLS（封禁用户禁评 + 父评论作者封禁禁回复）；评论删除：作者二次确认 + 删父评论时回复保留并显示「原评论已删除」占位；桌面端内联输入）
- [x] 5.3 我的评论列表（`/profile/comments`：按时间/按帖子双 Tab + 删除按钮）
- [x] 5.4 评论功能设计建议（见下方）
- [x] 5.5 评论支持关联显示，如回复某评论则显示在该评论下（嵌套显示；`fetchPostDetail` 改用 `fetchPostComments` 树组装）

### 评论功能设计建议（5.4）

**已实现**：
- 评论删除（作者可删，二次确认，级联删除子回复）
- 一级嵌套回复（`parent_id` 外键，只允许回复顶级评论，不支持回复的回复）
- 桌面端评论输入（评论区底部内联输入框，移动端保留固定底栏）
- 评论收藏（收藏按钮 + `favorite_comments` 表）

**推荐后续迭代**：
- 评论点赞：独立 `comment_likes` 表 + 触发器维护 `like_count`，与收藏区分
- 评论分页：评论超过 20 条时分页加载（"查看更多评论"按钮）
- 评论排序：支持「最新」/「最热」切换（按 `created_at` / `favorite_count` 排序）
- 评论举报：举报按钮 + `comment_reports` 表 + 管理后台审核
- 评论编辑：作者可编辑（发布后 5 分钟内），标记「已编辑」
        
*文档版本：v2.0 | 最后更新：2026-09-07*
