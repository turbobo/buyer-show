# 买家说 — 产品设计文档

> 购物种草社区多端应用（PC Web + 手机浏览器 + 小程序），让用户分享真实购物体验，发现好物推荐。
> 版本：v3.0 | 更新：2026-09-14 | 架构：前后端分离 Monorepo

**文档分工**（2026-09 文档合并后）：
- 本文档：产品定位、功能范围、实现现状、UI 设计规范、代办事项
- 《技术方案文档》：技术栈、系统结构、数据库、API 契约、部署手册
- 《架构设计文档》：目标态架构、中间件与可观测性设计、演进方向

---

## 一、产品定位

**一句话描述**：真实买家的购物分享社区，帮你买到真正值得的好物。

**目标用户**：
- 18-35 岁年轻消费者，习惯网购
- 喜欢看真实用户评价再做购买决策
- 乐于分享自己的购物心得和使用体验

**核心价值**：
- 对读者：看到真实、可信的购物评价，避免踩坑
- 对作者：记录购物体验，获得社区认同
- 对品牌：获取真实用户反馈和口碑传播

---

## 二、功能全景图

```
买家说
├── 🏠 发现（首页）
│   ├── 帖子列表（卡片流）
│   ├── 标签分类筛选
│   ├── 下拉刷新
│   ├── 触底加载更多
│   └── 骨架屏加载
│
├── 📄 帖子详情
│   ├── 图片轮播
│   ├── 商品信息（名称/价格/评分）
│   ├── 点赞互动
│   ├── 评论列表（一级嵌套回复）
│   ├── 发表评论
│   ├── 举报内容
│   ├── @提及 + #话题# 高亮跳转
│   └── 猜你喜欢相关推荐
│
├── ✏️ 发布
│   ├── 图片上传（MinIO，JPEG/PNG/WebP ≤10MB）
│   ├── 标题/正文编辑
│   ├── 商品信息（选填）
│   ├── 星级评分
│   ├── 标签选择
│   ├── #话题# / @提及语法
│   └── 内容审核反馈（通过/待审）
│
├── 🔍 搜索
│   ├── 关键词搜索
│   ├── 热门标签
│   └── 搜索历史
│
├── 👤 个人中心
│   ├── 用户信息卡片
│   ├── 数据统计（发布/粉丝/关注）
│   ├── 菜单分组（内容管理/社交互动/账号设置）
│   ├── 关注与粉丝列表
│   ├── 他人主页（/user/[id]）
│   └── 退出登录（注销入口在编辑资料页底部危险区）
│
├── 🛡️ 审核台（仅管理员）
│   ├── 待审帖子 / 待审评论
│   ├── 待处理举报
│   └── 通过 / 驳回（含说明）
│
└── 🔐 认证体系
    ├── 用户名/手机号/邮箱 + 密码（✅ 已实现）
    ├── 短信验证码登录（待接入短信服务）
    └── 微信登录（待开发）
```

---

## 三、实现现状（前后端分离版）

**已跑通的核心闭环**：注册登录 → 浏览 Feed → 帖子详情 → 发布（图片上传 + 内容审核）→ 点赞/收藏/评论/举报 → 管理员审核处置。

| 模块 | 状态 | 说明 |
|------|------|------|
| 认证 | ✅ 完成 | 用户名/手机号/邮箱 + 密码注册登录；JWT 双 token，401 自动刷新 |
| Feed | ✅ 完成 | 游标分页、标签筛选（后端过滤后再分页）；标签来源暂用本地常量，待接入标签接口 |
| 帖子详情 | ✅ 完成 | 图片、商品信息、标签、点赞、收藏、评论、举报、猜你喜欢相关推荐（同标签优先）；正文 @提及 + #话题# 高亮（提及跳用户主页、话题跳标签 Feed，可分享链接） |
| 发布 | ✅ 完成 | MinIO 图片上传（JPEG/PNG/WebP ≤10MB，1-9 张）、商品信息、星级、标签、正文 #话题#（自动并入标签）/@提及、审核结果反馈 |
| 评论 | ✅ 完成 | 顶级评论 + 楼内回复（G8 二级嵌套：回复的回复、回复 @昵称 前缀、楼中楼折叠、被回复人 reply 通知）；作者删除；待审/驳回内容不公开 |
| 互动 | ✅ 完成 | 点赞/收藏，计数原子更新（GREATEST 防负数） |
| 举报 | ✅ 完成 | 帖子/评论举报，唯一索引防重复；管理员采纳后下架内容 |
| 审核台 | ✅ 完成 | `/admin/moderation`：待审帖子/评论/举报三 Tab，通过/驳回/采纳/驳回举报 |
| 消息 | ✅ 完成 | 私信（会话列表/聊天/拉黑）+ 互动通知；进入消息中心即自动已读通知（红点即时清零，PC 导航/移动端 TabBar 徽标同步），单条点击已读 + 一键全部已读，未读徽标 = 通知 + 私信之和 |
| 搜索 | ✅ 完成 | 关键词搜索（G5 接入 ES：标题/正文/标签/商品名相关性排序 + 高亮片段，ES 不可用自动降级 MySQL LIKE）；前端搜索页结果卡片标题/摘要 <em> 高亮渲染（拆分渲染防注入） |
| 拉黑屏蔽 | ✅ 完成 | G6：`user_blocks` 表 + 双向内容互不可见 + 禁私信 + 自动双向取关；入口在主页/评论/会话内 |
| 收藏夹分类 | ✅ 完成 | G7：收藏到指定夹/默认夹、创建/重命名/删除、按夹浏览；详情页选择器 + 主页夹 chips 管理 |
| 创作数据 | ✅ 完成 | G9：个人主页「创作数据」页（仅本人）——帖子数/阅读/获赞/被收藏/评论概览、近 7/30 天互动趋势柱状图（指标切换）、Top5 热门帖；复用埋点基建 |
| 运营位与话题 | ✅ 完成 | G10：首页 Banner 横滑（帖子/话题/外链三跳转）+ Feed「精选」档 + 话题广场/话题聚合页；管理后台「运营管理」三 Tab（Banner CRUD/精选打标/话题 CRUD） |
| 实时推送与公告 | ✅ 完成 | G11：STOMP over WebSocket（`/ws`）+ RabbitMQ fanout 事件总线实时推送通知/私信/公告；全局公告横幅（关闭记忆）+ 运营后台「系统公告」Tab；轮询保留为降级兜底 |
| 电商闭环 | ✅ 完成 | G12：商品卡购买链接（可选，仅淘宝/天猫/京东/拼多多白名单域名）+ 详情页「去购买 ↗」新窗口跳转；非法链接发布/编辑被拒（8008） |
| 个性化推荐 | ✅ 完成 | G13：Feed「推荐」档（登录后基于近 90 天发帖/收藏/点赞标签偏好重排，命中偏好 +50 分加成叠加热度分；冷启动回退热门；排除自己发的帖） |
| 关注粉丝 / 标签管理 | ✅ 完成 | 粉丝/关注列表（`/user/:id/followers|following`，互关标识 + 游标分页）+ 拉黑管理 Tab；标签管理已由 `/admin/tags` 运营后台承接（统计/重命名/合并/删除） |
| 个人中心 | ✅ 完成 | 资料查看/编辑（`/profile` + `/profile/edit`）、帖子/收藏/赞过/评论/拉黑五 Tab、创作数据、收藏夹管理；补全「账号注销」（软注销，二次确认，不可逆） |
| 账号注销 | ✅ 完成 | 入口为编辑资料页底部「账号注销」危险区（低频操作深层化，不入主页）；destructive + ConfirmDialog 不可逆提示；软删全部帖子 + 置注销状态，之后登录被拒（1006）、旧 token 失效（1003）、他人查资料 2001 |
| 移动端 App | ❌ 未开始 | uniapp X 待实现 |

**旧版归档**：Next.js 14 + Supabase 版应用（含搜索、个人中心、关注粉丝、标签管理、管理后台等完整功能）已归档至 `buyer-show-backup/`，实施明细不再维护；产品能力按「代办功能」迁移到前后端分离版。

**完成度总览**：

```
前端 Web       ███████████████████░  95%  (核心 Flow 全部接入真实 API + 个人中心/搜索/关注页齐备；余量：移动端适配细化与长期体验增强)
后端 API       ███████████████████░  95%  (认证、帖子、评论、审核、举报、上传、运营、推送、推荐、注销 API 已实现)
移动端 App     ░░░░░░░░░░░░░░░░░░░░   0%  (uniapp X 待实现)
数据库         ████████████████████ 100%  (MySQL 8.0，Flyway V1-V18 已执行并验证)
内容审核       ████████████████░░░░  80%  (本地敏感词、审核台、举报处置已实现；第三方图片审核待接入)
通知系统       ████████████████████ 100%  (G11 STOMP 实时推送 + RabbitMQ 事件总线 + 公告，轮询保留为降级)
推荐系统       ████████░░░░░░░░░░░░  40%  (G13 v1 标签偏好重排已上线；独立推荐服务/向量召回待扩展)
部署上线       ████████░░░░░░░░░░░░  40%  (Docker Compose 可用，K8s 待配置)
```

**整体完成度：约 85%（Web/后端核心闭环全部完成；移动端 0%、K8s 与部分外部依赖能力待实现）**

---

## 四、移动端 UI 设计（Flow 清单）

**导航模式**：底部 TabBar（4 Tab）+ Stack 导航。

```
TabBar:
  ├── 🏠 首页 — Feed 列表 + 标签筛选
  ├── 🔍 搜索 — 关键词 + 标签搜索
  ├── ➕ 发布 — 购物分享（中央凸起珊瑚色按钮）
  ├── 💬 消息 — 私信 + 互动通知（Tab 切换）
  └── 👤 我的 — 个人中心
```

**UI 原型文件**：`ui-prototype.html`（浏览器打开查看全部 15 个手机屏幕）。

#### Flow 1: 首页 Feed 浏览（3 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 首页 Feed | 浏览购物分享流 | Logo + 搜索入口、标签横向滚动（全部/美妆/数码/服饰/食品/家居/母婴）、帖子卡片（封面图+标题+价格标签+来源平台图标+点赞数）、下拉刷新+触底加载 |
| Screen 2 | 搜索发现 | 关键词+标签搜索 | 搜索输入框（自动聚焦）、搜索历史、热门搜索标签、猜你想搜 |
| Screen 3 | 标签详情 | 浏览特定标签下的帖子 | 标签名+帖子数量、排序切换（最新/最热/精华）、帖子列表 |

**卡片信息架构**：封面图（高度自适应，120-160px）、标题（最多 2 行截断）、价格（珊瑚色大字）、购物来源（绿点 + 平台名）、用户头像+昵称、点赞数（❤️ 图标 + 计数）。

#### Flow 2: 发布购物分享（4 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 选图页 | 选择图片（当前实现单图） | 已选预览条、相册网格、选中角标、添加按钮、相机入口 |
| Screen 2 | 商品信息 | 填写商品元数据 | 图片缩略图横滑、商品名称输入、购物来源选择器（天猫/京东/拼多多/线下/海淘/其他）、价格+数量、星级评分 1-5、购买链接（可选） |
| Screen 3 | 正文编辑 | 撰写分享正文 | 富文本区域、标签选择（热门标签+自定义+添加）、底部工具栏（图片/表情/#话题/@提及） |
| Screen 4 | 预览发布 | 检查并发布 | 帖子预览卡片（图片拼贴+标题+商品信息卡+正文摘要+标签）、存草稿按钮、发布按钮 |

#### Flow 3: 帖子详情与互动（4 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 帖子详情 | 浏览完整内容 | 全屏图片轮播（滑动切换+圆点指示器）、用户信息行+关注按钮、商品信息卡片（来源平台+价格+评分+购买链接）、正文（@提及高亮可点跳用户主页、#话题# 跳话题 Feed）、标签（点击直达话题 Feed）、互动统计、评论区、猜你喜欢相关推荐区（同标签优先、不足补最新公开帖，卡片点击复用详情浮层）、底部操作栏（评论输入+点赞+收藏+分享） |
| Screen 2 | 评论区 | 浏览和发表评论 | 底部弹出模态（85% 高度）、评论列表（1 级嵌套回复）、评论输入框+表情+发送、拖拽关闭 |
| Screen 3 | 商品来源 | 查看购买渠道 | 底部半屏弹出、商品大图+品牌+来源平台验证标识、价格+评分、"前往购买"按钮、复制链接按钮 |
| Screen 4 | 用户主页 | 查看作者信息 | 头像+昵称+简介、统计（分享/粉丝/关注）、关注+私信按钮、Tab 切换（分享/收藏/赞过）、帖子网格 |

#### Flow 4: 私信聊天（4 屏）

| 屏幕 | 名称 | 核心交互 | 关键组件 |
|------|------|---------|---------|
| Screen 1 | 消息列表 | 查看所有对话和通知 | Tab 切换（私信/互动通知）、对话列表（头像+昵称+最后消息+时间+未读角标）；进入即自动已读通知、红点即时清零 |
| Screen 2 | 聊天页 | 实时对话 | 顶部用户信息+在线状态、消息气泡（收=白/发=珊瑚色）、商品分享卡片、底部输入栏（语音+文字+图片+发送） |
| Screen 3 | 新建对话 | 选择聊天对象 | 搜索用户、互关好友列表（在线状态）、最近联系、发消息按钮 |
| Screen 4 | 通知中心 | 系统+互动通知 | 互动通知列表（❤️点赞/💬评论/👤关注/@提及/📢系统）、单条点击已读跳转、一键全部已读；未读项背景高亮 + 右侧红点 |

#### Flow 5: 用户中心（5 屏，2026-09-16 设计，按 ui-ux-pro-max 准则）

> 设计原则：沿用现有珊瑚色设计系统（Token 见 §5.1），不引入新风格；交互遵循 44px 触控、loading/空态/错误就近反馈、icon 按钮带 aria-label（§5.8/§5.12）；容器宽度：个人中心系列（主页 / 资料编辑 / 关注与粉丝）统一 `max-w-5xl` 导航与内容容器，保证 PC 端返回按钮与内容左边缘跨页稳定。

| 屏幕 | 路由 | 名称 | 核心交互 | 关键组件 |
|------|------|------|---------|---------|
| Screen 1 | `/user/:id` | 用户主页（他人） | 查看作者 | 资料卡（头像/昵称/@用户名/简介）、统计三列（分享/粉丝/关注，点击跳对应列表或网格）、关注按钮（未登录引导登录）、Tab 切换（分享/收藏/赞过）、帖子网格（触底加载） |
| Screen 2 | `/profile` | 用户主页（我的） | 管理自己 | 同 Screen 1 + 「编辑资料」按钮（跳 /profile/edit）、帖子操作（编辑/删除/审核状态徽标，见 §6.4） |
| Screen 3 | `/profile/edit` | 资料编辑 | 修改个人资料 | 头像（点击更换，上传中态，即时预览）、昵称（必填 2-20 字符，唯一校验）、简介（≤100 字符）、用户名（只读）、保存（PC 表单卡底部右对齐 + 分隔线；移动端置顶导航）、取消；有未保存变更时离开需确认 |
| Screen 4 | `/user/:id/followers` | 粉丝列表 | 浏览粉丝 | 用户行（头像+昵称+简介截断+关注/已关注/互相关注按钮）、触底加载、空态「还没有粉丝」 |
| Screen 5 | `/user/:id/following` | 关注列表 | 浏览关注中的人 | 同 Screen 4，空态「还没有关注的人」 |

**页面规格**：

1. **主页 Tab（分享/收藏/赞过）**：分段控件，等宽三格；分享=公开帖（他人）/全部帖（自己，含待审）；收藏=该用户收藏的公开帖（按收藏时间倒序）；赞过=该用户点赞的公开帖（按点赞时间倒序）；各自独立游标分页（20/页）+ 空态（「还没有收藏」/「还没有点赞」）+ 骨架屏。
2. **资料编辑**：保存成功 toast + 返回主页并刷新；昵称重复就地报错（`role="alert"`）；头像上传复用 `/upload/image`（进度/失败可重试，失败回滚原头像）；离页确认用现有确认弹窗模式。
3. **关注/粉丝列表**：按关注时间倒序；自己出现在列表中时显示为「你」且不显示关注按钮；互关显示「互相关注」；他人页统计「粉丝/关注」数字可点击直达对应列表；自己的列表入口在「我的主页」统计列。
4. **私信入口**：主页「私信」按钮暂复用 `/messages`，会话创建能力待消息模块迭代（登记待办）。

**后端契约**（新增项已标注）：

```
GET  /api/v1/users/{id}                # 资料（已有，含统计与 isFollowing）
GET  /api/v1/users/{id}/posts          # 公开帖（已有）
GET  /api/v1/users/me/posts            # 我的全部帖（已有）
GET  /api/v1/users/{id}/favorites      # 【新增】收藏的公开帖（游标）
GET  /api/v1/users/{id}/likes          # 【新增】赞过的公开帖（游标）
GET  /api/v1/users/{id}/followers      # 【新增】粉丝列表（游标）
GET  /api/v1/users/{id}/following      # 【新增】关注列表（游标）
PUT  /api/v1/users/me                  # 资料更新（已有）
```

---

## 五、UI 设计规范

> 本章为全局设计决策，所有页面和组件开发必须遵循。参考基准：小红书、Instagram、Pinterest、什么值得买。
> 色值以 `frontend/src/index.css` 的 `@theme` 注册为准（coral、coral-light、coral-dark、warm-bg、warm-100/200/300）。

### 5.1 设计系统 Design Tokens

**品牌图标**：珊瑚渐变圆角方块 + 白色购物袋 + 对话点；唯一源文件 `frontend/public/favicon.svg`（浏览器标签图标、导航左上角与登录页均引用同一文件），导出资产见 `docs/assets/logo.svg` 与 `logo.png`。

**色彩体系**（品牌扩展色，注册于 `index.css`）：

| Token | 色值 | 用途 |
|-------|------|------|
| `coral` | `#FF6B35` | 品牌主色、CTA、选中态、强调 |
| `coral-light` | `#FFF0E8` | 高亮背景、选中底色 |
| `coral-dark` | `#E55A2B` | 按压态、hover 加深 |
| `warm-bg` | `#FAF7F5` | 页面背景 |
| `warm-100` | `#F5EDE8` | 卡片分隔、浅底 |
| `warm-200` | `#E8DDD5` | 分隔线加深 |
| `warm-300` | `#D4C5B9` | 边框、占位 |

语义色（shadcn/ui 变量）：`background` / `foreground`（正文）/ `muted-foreground`（辅助）/ `destructive`（错误）。状态色：成功 `#10B981`、警告 `#F59E0B`、错误/点赞红 `#EF4444`。

**字体层级**：

| 层级 | 字号 | 行高 | 字重 | 用途 |
|------|------|------|------|------|
| H1 | 20px | 28px | 700 | 页面标题、详情页标题 |
| H2 | 16px | 24px | 700 | 卡片标题、区块标题 |
| Body | 14px | 22px | 400 | 正文内容、评论 |
| Caption | 12px | 18px | 400 | 时间、辅助说明 |
| Tiny | 10px | 14px | 500 | 标签文字、TabBar 文字 |

- 字体栈：`-apple-system, BlinkMacSystemFont, 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif`
- 数字字体：`DIN Alternate, Avenir Next`（价格、统计数字）

**间距系统**：基准单位 **4px**，所有间距为 4 的倍数。

| Token | 值 | Tailwind | 用途 |
|-------|-----|----------|------|
| xs | 4px | `p-1` | 图标与文字间距 |
| sm | 8px | `p-2` / `gap-2` | 紧凑元素间距 |
| md | 12px | `p-3` / `gap-3` | 卡片内间距、列表间距 |
| lg | 16px | `p-4` / `gap-4` | 区块间距 |
| xl | 20px | `p-5` | 页面边距 |
| 2xl | 24px | `p-6` | 大区块间距 |
| 3xl | 32px | `p-8` | 桌面端页面边距 |

**圆角**：

| Token | 值 | 用途 |
|-------|-----|------|
| sm | 8px | 输入框、小标签 |
| md | 12px | 按钮、小卡片 |
| lg | 16px | 帖子卡片、内容卡片 |
| xl | 24px | 弹窗、底部 Sheet |
| full | 9999px | 头像、胶囊按钮、标签 |

**阴影层级**：

| 层级 | 值 | 用途 |
|------|-----|------|
| shadow-sm | `0 1px 3px rgba(0,0,0,0.06)` | 卡片默认 |
| shadow-md | `0 4px 12px rgba(0,0,0,0.08)` | 悬浮按钮、导航栏 |
| shadow-lg | `0 8px 24px rgba(0,0,0,0.12)` | 弹窗、下拉菜单 |
| shadow-coral | `0 4px 12px rgba(255,107,53,0.25)` | 主色按钮 |

### 5.2 导航架构

**页面层级**（当前实现 + 规划）：

```
Tab 页面（一级，规划）
├── /              发现（首页）
├── /search        搜索
├── /publish       发布
└── /profile       个人中心

当前已实现路由（Vite SPA）
├── /                   首页 Feed
├── /posts/:postId      帖子详情
├── /publish            发布（需登录）
├── /login              登录/注册
├── /messages           消息（需登录，静态）
└── /admin/moderation   审核台（需登录 + ADMIN 角色）
```

**导航栏规则**：

| 场景 | 移动端 | PC 端 |
|------|--------|-------|
| Tab 页面 | 底部 TabBar（4 tab + 发布凸起按钮） | 顶部导航栏 |
| 二级页面 | 顶部返回箭头 + 页面标题 | 顶部返回箭头 + 面包屑 |
| 详情页 | 无导航栏，浮动返回按钮 | 顶部导航栏 + 浮动返回按钮 |
| 发布页 | 顶部标题 + 关闭按钮 | 顶部导航栏 + 居中表单 |

**TabBar 设计**：高度 56px + safe-area-inset-bottom；背景 `white/95` + `backdrop-blur-lg`；选中态珊瑚色图标+文字+底部指示条；发布按钮为珊瑚红圆形凸起（28px）；选中动画 spring scale。

**全局导航统一规范（2026-09-16 实装；所有 Stack 页面强制一致）**：

*① Stack 页顶部导航（含返回语义的页面）统一结构*：

```
<nav sticky top-0 z-50 border-b border-border bg-card/95 backdrop-blur-xl>
  └─ <div mx-auto flex h-14 max-w-5xl items-center gap-3 px-4>
       [返回 icon 按钮]   [标题（flex-1 左对齐）]   [右侧操作区]
```

| 元素 | 规格 |
|------|------|
| 返回按钮 | `variant=ghost size=icon`（44px 触达）+ `ArrowLeft h-5 w-5` + `aria-label="返回上一页"` + `smartBack()`（外链兜底回首页）；**加 `-ml-3` 负偏移**：44px 热区中 20px 图标居中会内缩 12px，负偏移后**图标左边缘与正文左边缘精确对齐** |
| 标题 | `flex-1 truncate text-lg font-bold text-foreground`，**左对齐紧跟返回按钮**（不使用居中标题） |
| 右侧操作 | icon 按钮统一 `size="icon"`（间距 `gap-1`）；主操作（发布/保存/全部已读）用文字按钮或 coral 实心按钮 |
| 容器宽度 | **统一 `max-w-5xl`**（返回按钮左边缘跨页固定，不随内容宽度跳动）；表单类页面内容可在 5xl 容器内居中收窄（`max-w-3xl`） |

*② 明确例外（独立规范，不参与 Stack 一致性对比）*：

- Tab 级页面（首页 Feed）：无返回按钮，导航容器跟随内容宽度（7xl）
- 登录页：无业务返回，仅「返回首页」icon（`aria-label="返回首页"`），几何与 Stack 页返回按钮完全一致（同容器/同位置）
- 404 页：居中状态页，保留「回到首页」主按钮
- 管理后台：AdminLayout 独立体系（侧栏 + 顶栏，见 §七）

*③ 一致性问题修复记录（2026-09-16）*：发布/编辑资料页由「文字取消/返回 + 居中标题 + 3xl 容器」改为标准 Stack 结构（5xl）；登录页返回按钮由视口 absolute 文字按钮改为标准容器内 icon；消息页导航容器 7xl→5xl 并补 aria-label；通知页补齐缺失的顶部导航与返回入口。

**全站框架统一规范 v2（2026-09-16 设计，待实现）**：

*① 框架宽度全站统一为 `max-w-5xl`（消除"显示范围"跳动）*

| 页面族 | 现状 | v2 目标 |
|--------|------|---------|
| 首页 Feed | 导航与内容 **7xl**（1280px） | 统一 **5xl**（1024px） |
| 其余全部页面（详情/主页/列表/表单/消息/通知） | 5xl | 5xl（不变） |
| 表单类页内容（发布/编辑资料） | 5xl 内居中收窄 3xl | 不变 |
| 管理后台 | 侧栏 240px + 内容 5xl 居中 | 不变（独立体系） |

- **根因说明**：首页（7xl）跳转个人主页（5xl）时，内容左右边界内缩约 128px，产生"页面显示范围变化"的跳动感；全站同框架后，导航左边缘与内容左边缘**跨页恒定**
- **Feed 瀑布流配套调整**：列数档位由「≥1280: 4 列 / ≥768: 3 列 / <768: 2 列」改为「≥768: 3 列 / <768: 2 列」；单列宽度基本不变（约 303px → 322px），信息密度损失可忽略
- **取舍说明**：PC 超大屏内容两侧留白增加——对齐稳定性与阅读宽度优先（有意为之）；移动端无感知变化

*② 全站导航入口：每个 Stack 页可一键回首页*

- 导航结构升级（在 §5.2 统一结构上增加首页按钮）：

```
[← 返回]  [⌂ 首页]  [标题 flex-1 左对齐]        [右侧操作区]
```

  - 首页按钮：`House` 图标、`aria-label="返回首页"`、44px 触达、`navigate('/')`
  - 负偏移规则：返回按钮保持 `-ml-3`（图标对齐正文左边缘）；返回与首页两按钮组内 `gap-1` 紧凑排列
- **覆盖页面**：帖子详情、他人/我的主页、粉丝/关注列表、编辑资料、发布/编辑帖（含加载态）、消息、通知（"返回上一页"逻辑全部保留，新增直达首页）
- **已有直达入口不重复**：Feed（TabBar/顶部导航）、登录页（返回首页）、404（回到首页）、管理后台（返回前台）
- **深层路径收益**：如「详情 → 他人主页 → 粉丝列表 → 用户主页」链路中，任一页均可 1 次点击回首页，无需连续返回

*③ 验收标准*

1. PC 1440 视口下逐页切换「首页 → 详情 → 他人主页 → 粉丝 → 编辑资料 → 发布 → 消息 → 通知」：**导航左边缘与内容左边缘 x 坐标全程恒定（无跳动）**
2. 每个页面均可**一键回首页**（≤1 次点击）；深层路径无需连续点击返回
3. 移动端（390px 视口）：导航「返回 + 首页」两按钮不与标题挤压，标题按 `truncate` 正常截断
4. 管理后台与登录/404 页保持各自独立规范（例外清单见 ②/§5.2 例外）

*④ 实施清单（✅ P0-1/P0-2 已于 2026-09-16 实施为 U13/U14；TabBar 全局化、PC Header 等演进项见下方 v3）*
- P1（设计保留，本期不做）：评估 PC 端「全局顶部导航」（全站统一 Header：首页/搜索/发布/消息/个人中心 + 用户区），进一步将 PC 导航收敛为单一条；移动端维持现状（Stack 返回导航 + 首页 TabBar）

**框架与导航体验 v3（2026-09-16 设计；从"返回按钮依赖"转向多维导航）**：

*设计理念*：返回按钮是「兜底」而非「主通道」。用户通过 ①系统手势 ②常驻导航（移动端 TabBar / PC 全局 Header）③就近入口 ④位置恢复 完成导航，「返回」仅作辅助。

**现状痛点（代码核实）**：底部 TabBar 仅在首页（`safe-bottom` 仅 feed.tsx）；`ScrollToTop` 对所有路由变化 `scrollTo(0,0)`——**从详情返回 Feed 必回顶部，浏览位置丢失**，用户因此"不敢深入"；Stack 页无频道级导航，去消息/个人中心必须经返回。

*A. 移动端*

- **A1 底部 TabBar 全局化**：Stack 页（主页/粉丝列表/编辑资料/消息）保留底部 TabBar → 任意页 **1 次点击**切换一级频道（首页/搜索/发布/消息/我的）
  - 沉浸页例外：**帖子详情**（底部为互动操作栏）与全屏预览不显示 TabBar
  - Tab 点击语义：回到该频道根页（清空该频道浏览栈，行业惯例）；当前频道高亮保持
  - 内容底部安全间距：TabBar 高约 64px + `safe-area`，页面内容 `padding-bottom` 同步抬升防遮挡
- **A2 系统手势零成本启用**：SPA 基于 History API，**iOS 边缘右滑返回 / Android 系统返回键原生生效**；设计上不拦截屏幕边缘 touch 事件、不自定义边缘手势（避免与系统冲突）
- **A3 双击首页 Tab 回顶**（长列表快速归位惯例）；下拉刷新维持现状（Feed 已有）
- **A4 列表滚动位置恢复**：前进（PUSH）时回顶；**后退（POP，如详情 → 返回 Feed）时恢复离开前位置**（内存缓存 + 标记位）

*B. PC 端*

- **B1 全局顶部 Header**（取代原 v2-U15 评估项，升级为正式实施）：PC 全站常驻 Header（logo + 首页/搜索/发布/消息/个人中心 + 用户区）→ 任意页 **1 击切频道**；Stack 页「返回」保留（层级返回语义），但频道切换不再依赖返回
- **B2 导航路径提示**：PC Stack 页标题采用「**首页 / 当前页名**」双级路径（"首页"可点击）——比纯图标直观
- **B3 键盘导航**：`Esc` = 返回上一页（PC 惯例）；焦点环全站可见（已有）

*C. 通用*

- **C1 就近入口原则**（固化已有实践）：详情页作者区 → 主页；主页统计列 → 关注/粉丝/收藏/赞过；自己帖子 → 编辑/申诉——入口与内容就近出现，避免"先返回再找"
- **C2 返回语义统一**（保持）：`smartBack`（外链兜底回首页）
- **C3 层级精简**：能 Tab 化的不建子页（Flow 5 已在主页内实践）

*效果量化（典型路径点击次数）*

| 任务 | 现状 | v3 设计 |
|------|------|---------|
| 详情 → 消息 | 返回首页再点消息（2 次，且位置丢失） | **1 次**（TabBar） |
| 粉丝列表 → 首页 | 连续返回 2-3 次 | **1 次**（Tab 首页） |
| 逛 Feed 深入一帖 → 回来继续逛 | **回顶，位置丢失** | **位置原样恢复** |
| PC 任意页 → 个人中心 | 返回首页再点头像（2 次） | **1 次**（Header） |
| 返回上一页（辅助路径） | 按钮 | 按钮 / **系统手势（原生）** / PC `Esc` |

*实施清单（✅ 已于 2026-09-16 全部实施）*：U13/U14 框架统一、U16 TabBar 全局化、U17 滚动位置恢复、U18 PC 全局 Header、U19 手势与键盘（Esc 返回 / 同 Tab 回顶）；同期落地 U9 图片性能与 U20 下拉刷新。

*修订（2026-09-16 优化，导航去重）*：移动端 Stack 导航保留「⌂ 首页」；**PC 端隐藏该按钮**（首页入口由全局 Header 承担，消除双首页）；Feed 顶部导航统一 `h-14` 并补「首页（高亮）/ 消息」频道按钮组，与 Header 结构及 logo 规格完全对齐。

**框架与导航体验 v4（2026-09-16 设计；参考小红书交互，去除二级标题栏）**：

*参考基准（小红书 Web/App 交互拆解）*：
- PC 全站**单行**固定导航（logo + 搜索 + 发布 + 消息 + 头像），**无第二行标题栏**；个人主页直接以资料卡开始
- 个人主页 Tab（笔记/收藏/赞过）**滚动吸顶**（标志性交互）
- 笔记详情以**浮层弹窗**打开（背景暗化，关闭即回列表，天然保留滚动位置）——本产品列为 P2 探索
- 移动端个人主页无标题栏，返回依赖系统手势/浮动返回箭头

*① 去除二级导航条（仅 PC，md+）*
- 移除范围：`/profile`（我的主页）、`/user/:id`（他人主页）、粉丝/关注列表、编辑资料、消息、通知——内容从全局 Header 下**直接开始**（"我的主页"标题由资料卡（昵称+编辑资料按钮）自然承载，不再有文字标题栏）
- 编辑资料页的「保存」按钮（原在第二行右侧）PC 下迁移至**表单底部主按钮**
- 保留范围：帖子详情（沉浸阅读，含返回/分享/编辑）、发布页（表单含取消/发布）——不在本次改动内
- 移动端：**保留**现有「返回 + ⌂ + 标题」第二行（返回能力依赖）；后续可选演进为浮动返回（见 ④）

*② 顶部固定菜单导航（✅ 2026-09-16 三页菜单一致性已实施）*
- **首页 / 消息 / 个人主页 三页菜单完全一致**：logo（可点击回首页） · 🏠首页 · 💬消息 · 搜索框 · 主题 · 审核台（管理员） · 用户区（头像+昵称+退出图标） · +发布（最右）
- 页面态高亮：消息/通知页高亮「消息」、首页 Feed 高亮「首页」；用户区与登出确认弹窗在 Feed 与 Header 同构复用
- Feed 导航（navigate 自带）与 DesktopHeader 为同构实现（h-14 容器 / gap-4 / 同尺寸居中搜索框 / 同款用户区）；搜索浮层化（U24）后续两边同步

*③ 个人主页 Tab 吸顶（小红书核心体感）*
- 分享/收藏/赞过分段控件滚动吸顶：PC `top-14`（紧贴 Header）、移动 `top-0`
- 资料卡随滚动移出，Tab 常驻可视，网格从其下继续滚动

*④ 移动端（本次不变）*
- 保留「返回 + ⌂ + 标题」导航（返回与首页入口依赖）
- 可选演进（后续）：透明渐变标题栏（初始无底、滚动后渐显）或浮动返回箭头

*实施状态（2026-09-16）*：U21/U22 ✅ 已实施并经浏览器验证；U23/U24 待排期。

**导航按钮位置规范 v5（2026-09-16 设计；按钮分区与主流布局对齐）**：

*① 位置分区（从左到右）*

```
登录态（PC）：
┌────────────────────────────────────────────────────────────────────────────────┐
│ [logo买家说] [🏠首页] [💬消息] │ [····搜索框····] │ [🌙] [审核台*] [+发布] [头像+昵称] │
└────────────────────────────────────────────────────────────────────────────────┘
          品牌区   频道区          搜索区（自适应居中）   工具区     操作区   身份区·最右锚点

未登录（PC）：
┌────────────────────────────────────────────────────────────────────────────────┐
│ [logo买家说] [🏠首页] [💬消息] │ [····搜索框····] │ [🌙] [+发布] [登录]           │
└────────────────────────────────────────────────────────────────────────────────┘
                                                                     身份区=登录按钮

移动端（<768）：不使用顶部按钮组——底部 TabBar [首页|搜索|+发布|消息|我的] + 页面级返回导航（见 §4.3）
```

> `*` 审核台仅管理员可见（ghost 次级样式）；身份区内部顺序固定：头像+昵称（登录态）/ 登录按钮（未登录态）。

| 区 | 内容 | 说明 |
|----|------|------|
| 品牌区 | logo（点击回首页） | 紧靠左侧 |
| 频道区 | 🏠 首页 · 💬 消息 | gap-1 紧邻品牌区 |
| 搜索区 | 搜索框（`flex-1 max-w-xl mx-auto` 自适应居中） | 首页真输入 / 其余页浮层化（U24） |
| 工具区 | 🌙 夜景模式 · 审核台（仅管理员，ghost 次级样式） | 靠右起点 |
| 操作区 | **+ 发布**（coral 主 CTA） | 紧邻身份区左侧 |
| 身份区（**最右锚点**） | 已登录：头像+昵称（点击→个人主页）；未登录：**登录**按钮（点击→`/login?redirect=当前路径`） | 导航栏最右端 |

*② 与当前实现的差异（调整项）*

1. **发布与身份区换位**：当前 `[头像+昵称][退出][发布]` → v5 `[发布][头像+昵称]`——头像作为导航**最右锚点**（小红书/微博/B 站惯例），发布紧邻其左
2. **退出登录收进头像下拉菜单**：点击头像展开（我的主页 / 编辑资料 / 退出登录；退出为 destructive + 二次确认）；导航栏不再放裸退出图标 → 更简洁
3. **未登录**：登录按钮占据最右身份位（ghost 样式，不与发布 CTA 抢焦点）；登录成功后回跳来源页
4. 审核台保持管理员专属（ghost 次级）

*③ 交互细节*

- 头像下拉：点击展开（昵称头 + 分隔线 + 3 项），点击外部 / `Esc` 关闭，键盘可达
- 登录按钮与用户区随路由自动同步（df38caf 已修复常驻 Header 的登录态刷新）
- Feed 导航与 DesktopHeader 同步调整（菜单一致性规范，§5.2 ②）

*④ 实施状态*：✅ 已于 2026-09-16 实施（U25）：发布前移至身份区左侧、头像为最右锚点；新增 UserMenu 头像下拉菜单（我的主页/编辑资料/退出）；Feed 与 Header 同步，浏览器 3/3 项验证通过。

*验收标准*
1. PC `/profile` 首屏为**资料卡**（无"我的主页"标题行）；滚动后 Tab 吸顶于 Header 下方
2. PC `/messages`、`/notifications` 首屏为列表内容（无标题行）
3. PC 编辑资料页保存按钮在表单底部，保存流程正常
4. 移动端返回/首页入口全链路不受影响
5. 各页返回首页：PC = Header「首页」；移动 = ⌂ / TabBar（不变）

### 5.3 全局交互状态组件

**Toast 通知**（顶部滑入）：

| 类型 | 样式 | 时长 | 示例 |
|------|------|------|------|
| success | 绿色 ✓ + 文字 | 2s | "发布成功" |
| error | 红色 ✕ + 文字 | 3s | "网络异常，请重试" |
| info | 灰色 ℹ + 文字 | 2s | "已复制到剪贴板" |
| loading | 旋转圈 + 文字 | 手动关闭 | "上传中..." |

样式：圆角胶囊，深色背景 `gray-800/90`，白色文字，居中固定顶部 60px

**确认弹窗 Modal**（危险操作二次确认）：遮罩 `black/50` + `backdrop-blur-sm`；白色卡片圆角 24px；标题 H2 居中；描述 Caption 灰色；取消 + 确认双按钮（危险操作用红色）；动画 scale 0.9→1 + opacity 0→1。

**空状态** `<EmptyState icon title description action />`：

| 场景 | 图标 | 标题 | 描述 | 操作按钮 |
|------|------|------|------|----------|
| 首页无内容 | 📦 | 还没有内容 | 来发第一条买家秀吧 | 去发布 |
| 搜索无结果 | 🔍 | 没找到相关内容 | 换个关键词试试 | 无 |
| 评论为空 | 💬 | 暂无评论 | 快来抢沙发！ | 无 |
| 帖子不存在 | 📭 | 帖子不存在 | 可能已被删除或链接无效 | 返回首页 |
| 未登录 | 👤 | 登录后查看 | 登录后可以点赞评论 | 去登录 |
| 网络异常 | 📡 | 网络连接失败 | 请检查网络后重试 | 重试 |

**骨架屏 Skeleton**：圆角与目标元素一致；shimmer 动画 `linear-gradient(90deg, #f0ebe6 25%, #e8e0d8 50%, #f0ebe6 75%)`，背景尺寸 200%，1.5s 循环；首页还原完整布局（标签栏 + 4 张卡片），详情页为图片区 + 标题行 + 内容行。

**下拉刷新**：触发阈值 60px；珊瑚红旋转圈；文案"下拉 → 释放刷新 → 刷新中... → 完成"；超时 10s 自动恢复。

### 5.4 图片策略

**上传规范**（当前实现 + 规划）：

| 环节 | 规则 |
|------|------|
| 选择 | 当前单图（≤10MB）；规划最多 9 张，支持相册 + 拍照 |
| 格式 | JPEG / PNG / WebP；服务端以文件签名 + MIME + 扩展名三重校验 |
| 存储 | MinIO 私有 `pending/{userId}/` 前缀，审核通过后转 `published/` 前缀公开访问 |
| 压缩 | 规划：长边 ≤ 2048px，质量 85%，目标 < 500KB/张 |
| 缩略图 | 规划：封面图生成 400px 宽缩略图 |

**展示规范**：

| 场景 | 尺寸 | 模式 | 说明 |
|------|------|------|------|
| 列表封面 | 宽度自适应，高度按比例 | `object-cover` | 随机高度 180~260px |
| 详情页轮播 | 宽度 100%，4:3 | `object-cover` | snap 滚动 |
| 全屏预览 | 最大宽度 90vw，最大高度 80vh | `object-contain` | 深色背景 |
| 用户头像 | 固定尺寸 | `object-cover` + `rounded-full` | 5/8/9/72px |

**加载策略**：列表 `loading="lazy"`；详情页首图预加载 `loading="eager"`；占位图统一灰色背景 + 居中图标；加载失败显示占位图 + "图片加载失败"；图片地址由服务端生成（当前 MinIO 公开 URL，CDN 分发给规划项）。

**图片预览**（规划）：全屏深色背景 `black/95`；左右滑动切换（snap）；底部圆点指示器；右上角关闭按钮；双击放大（移动端）、滚轮放大（PC 端）。

### 5.5 登录与权限体系

**登录方式**：

| 方式 | 状态 | 说明 |
|------|------|------|
| 用户名/手机号/邮箱 + 密码 | ✅ 已实现 | 注册可绑定手机号/邮箱（唯一校验） |
| 游客浏览 | ✅ 已实现 | 不登录可浏览 Feed 与详情，互动需登录 |
| 手机号 + 验证码 | 待接入 | 国内用户主要方式，依赖短信服务 |
| 微信登录 | 待开发 | 社交账号快捷登录 |

**未登录权限矩阵**：

| 操作 | 未登录 | 已登录 |
|------|--------|--------|
| 浏览帖子列表 | ✅ | ✅ |
| 查看帖子详情 | ✅ | ✅ |
| 搜索 | ✅ | ✅ |
| 点赞 | ❌ 弹出登录引导 | ✅ |
| 评论 | ❌ 弹出登录引导 | ✅ |
| 发布帖子 | ❌ 跳转登录页 | ✅ |
| 查看个人中心 | 显示登录卡片 | ✅ |
| 关注用户 | ❌ 弹出登录引导 | ✅ |

**登录引导交互**：点赞/评论时底部 Sheet 弹出（logo + 提示 + 登录按钮）；发布时跳转独立登录页；登录后恢复原目标路径（`/login?redirect=...`）。

**Token 管理**：JWT 双 token（access 30 分钟 / refresh 7 天）存储于 `localStorage`；HTTP 客户端在 401 时自动用 refresh token 刷新并重试一次；刷新失败清除登录态并提示"登录已过期，请重新登录"。

**登录页与登出交互补充**（2026-09-16 设计，已实现）：

*① 登录页「返回首页」入口*

- 现状问题：`/login` 为独立全屏页且无退出入口，从外部链接或登出后进入时无法回首页（游客本可自由浏览 Feed/详情/搜索/他人主页）
- 设计：登录页**左上角固定「← 返回首页」**（ghost 文字按钮，与其它页面导航左上角几何一致；44px 触达，`aria-label="返回首页"`，Tab 顺序位于表单之前）
- 行为：点击**直接跳转 `/`**（不用 `smartBack`——外部直达时 history 可能为空，回首页更确定）；登录/注册模式切换时入口保持可见
- 定位：登录页不承担"拦截"职责，仅是可选入口页；顶部一处入口即可，卡片内不重复放置

*② 登出交互（二次确认 + 反馈 + 跳转）*

- 现状问题：首页导航「退出登录」一键即登出，无确认、无提示、无跳转，易误触且状态切换生硬
- 交互流：
  1. 点击「退出登录」→ 弹**确认弹窗**（`role="dialog"` `aria-modal="true"`）：标题「退出登录？」，副文案「退出后将以游客身份浏览，随时可以重新登录。」；按钮 [取消]（outline）/ [退出登录]（destructive，置于右侧）
  2. 键盘与焦点：Esc = 取消；打开时焦点落「取消」；关闭后焦点回到触发按钮
  3. 确认后：按钮 **loading（"退出中..."）+ disabled 防连点** → 清理 token 与本地会话态 → **Toast「已退出登录」**（aria-live polite，3 秒自动消失）
  4. 跳转策略：当前处于受保护路由（`/profile`、`/publish` 等）→ 跳首页 `/`；否则**原地切换为游客态**（首页场景为主）；不强制跳登录页（与"游客可自由浏览"理念一致）
  5. 登出后界面：导航区头像按钮替换为「登录」入口（现状已有该分支，保持不变）
- 实现提示：确认弹窗复用站内 dialog 模式（参考 `reports.tsx` 确认弹窗结构）；纯前端登出，无后端改动；未来个人中心设置页的登出复用同一确认组件

### 5.6 分享与 SEO

**分享策略**：

| 平台 | 分享形式 | 内容 |
|------|----------|------|
| 微信好友 | 小程序卡片 / H5 链接卡片 | 封面图 + 标题 + "来自买家说" |
| 朋友圈 | H5 链接 | 封面图 + 标题 |
| 复制链接 | URL | `https://buyer-show.com/post/{id}` |
| 生成海报 | 图片 | 封面图 + 标题 + 二维码 + 评分 |

**SEO Meta 标签**（每个帖子页独立生成）：

```html
<title>{post.title} - 买家说</title>
<meta name="description" content="{post.content 前 120 字}" />
<meta property="og:title" content="{post.title}" />
<meta property="og:description" content="{post.content 前 80 字}" />
<meta property="og:image" content="{post.images[0]}" />
<meta property="og:url" content="https://buyer-show.com/post/{id}" />
<meta property="og:type" content="article" />
<meta name="twitter:card" content="summary_large_image" />
```

**结构化数据 (Schema.org)**：帖子页输出 `Review` 类型 JSON-LD（name、reviewBody、itemReviewed、reviewRating、author、datePublished、aggregateRating）。

**Sitemap**（规划，见待办 P1.7）：自动生成 `sitemap.xml`（active 帖子 URL），提交至百度站长平台 + Google Search Console。

### 5.7 性能基线

**核心指标目标**：

| 指标 | 目标值 | 测量工具 |
|------|--------|----------|
| LCP (最大内容绘制) | < 2.0s | Lighthouse |
| FID (首次输入延迟) | < 100ms | Lighthouse |
| CLS (累积布局偏移) | < 0.1 | Lighthouse |
| TTI (可交互时间) | < 3.0s | Lighthouse |
| 首屏 JS 体积 | < 150KB | Bundle analyzer |

**优化策略**：Vite 路由级懒加载；图片 `loading="lazy"` + 占位骨架屏；字体 `font-display: swap` + 系统字体优先；静态资源长缓存 `Cache-Control: max-age=31536000, immutable`；gzip / brotli 压缩；CDN 与 PWA 为规划项。

**监控**（规划，见待办 P2.2）：接入访问统计；全局 ErrorBoundary 上报错误；`web-vitals` 采集 LCP/FID/CLS。

### 5.8 无障碍 (Accessibility)

**语义化要求**：每页唯一 `<h1>`；图标按钮必须 `aria-label`；图片必须 `alt`；表单 `<label>` 关联 `<input>`；`prefers-reduced-motion` 降级动画。

**交互要求**：所有交互元素支持 Tab 键导航；`:focus-visible` 显示珊瑚色 outline；文字与背景对比度 ≥ 4.5:1（WCAG AA）；触控目标最小 44×44px。

**需补全的 aria-label**：

| 元素 | aria-label |
|------|------------|
| 返回按钮 | "返回上一页" |
| 刷新按钮 | "刷新内容" |
| 点赞按钮 | "点赞" / "取消点赞" |
| 评论发送 | "发送评论" |
| 关闭预览 | "关闭图片预览" |
| 删除图片 | "删除第 N 张图片" |
| 搜索按钮 | "搜索" |
| 清除搜索 | "清除搜索内容" |

### 5.9 动效规范

**动画原则**：微交互 150~300ms，页面转场 300~500ms；交互用 `ease-out`，弹跳用 `spring`；同一屏幕不超过 2 种同时进行的动画；`prefers-reduced-motion: reduce` 时关闭非必要动画。

**动效清单**：

| 动效 | 参数 | 触发场景 |
|------|------|----------|
| fadeInUp | 0.45s, delay 0.08s×index | 列表项错落淡入 |
| heartbeat | 0.6s, scale [1,1.4,0.9,1.2,1] | 点赞 |
| shimmer | 1.5s infinite | 骨架屏 |
| slideUp | 0.3s ease-out | 底部 Sheet 弹出 |
| spring scale | stiffness 300, damping 25 | TabBar 选中、按钮点击 |
| rotate 360 | 0.8s linear infinite | 刷新图标旋转 |
| staggerChildren | 0.06s interval | 卡片入场 |

> 当前前端未引入动效库，动效以 CSS/Tailwind 过渡实现；上表为后续统一的参数基线。

### 5.10 响应式断点

| 名称 | 范围 | Tailwind | 布局 |
|------|------|----------|------|
| 手机 | < 768px | 默认 | 单列/双列，底部 TabBar，430px 居中 |
| 平板 | 768~1023px | `md:` | 三列，顶部导航 |
| 桌面 | 1024~1279px | `lg:` | 四列 |
| 大屏 | ≥ 1280px | `xl:` | 五列 |

### 5.11 组件与文件命名

| 类型 | 命名规则 | 示例 |
|------|----------|------|
| 页面 Flow | `flows/<领域>/<名称>.tsx`（小写文件名） | `flows/post-detail/detail.tsx` |
| UI 组件 | `components/ui/*.tsx`（小写文件名，组件名 PascalCase） | `components/ui/button.tsx` |
| Hook | `use` 前缀 | `usePosts.ts` |
| 工具函数 | camelCase | `formatTime.ts` |
| 服务层 | `services/<领域>.ts` | `services/posts.ts` |

### 5.12 错误处理规范

| 错误场景 | 用户提示 | 处理方式 |
|----------|----------|----------|
| 网络超时 | "网络不太给力，点重试看看" | 显示重试按钮 |
| 401 | "登录已过期，请重新登录" | 自动刷新 token，失败则清除登录态 |
| 403 | "该内容已被删除或隐藏" | 显示空状态 |
| 404 | "页面不存在" | 显示 404 页面 |
| 429 | "操作太频繁，稍后再试" | Toast 提示 |
| 500 | "服务器开小差了" | 显示重试按钮 |
| 图片上传失败 | "图片上传失败，请重试" | 展示重试入口，保留选择状态 |
| 内容违规 | "内容包含违规信息，请修改" | Modal 提示 |

**全局兜底**：页面级 ErrorBoundary 捕获渲染错误；关键组件（帖子列表、评论区）独立 ErrorBoundary；兜底 UI 为错误提示 + "重新加载" 按钮。

### 5.13 图标系统

- 统一使用 `lucide-react`，禁止在业务组件中内联自绘 SVG
- 尺寸：`h-4 w-4`（行内）/ `h-5 w-5`（按钮）/ `h-6 w-6`（强调），与文字基线对齐
- 颜色跟随 `currentColor`，品牌色场景用 `text-coral`

### 5.14 暗色模式（规划）

**策略**：Tailwind `darkMode: 'class'`（`index.css` 已注册 `@custom-variant dark`），通过 `<html class="dark">` 切换。

**暗色色板**：

| Token | 色值 | 用途 |
|-------|------|------|
| `dark-50` | `#0a0a0a` | 最深背景 |
| `dark-100` | `#141414` | 页面背景 |
| `dark-200` | `#1e1e1e` | 卡片背景 |
| `dark-300` | `#282828` | 凸起表面 |
| `dark-400` | `#333333` | 输入框背景 |
| `dark-500` | `#444444` | 静默表面 |

**编码规范**：所有新增/修改的组件必须同时提供 `dark:` 变体（背景、文字、边框、分割线）；品牌色 coral 在深浅模式通用，无需额外 dark 变体。

**切换入口**：PC 全局 Header 主题按钮（浅色/深色两态切换）；移动端「我的」页导航右上角三态循环按钮（跟随系统→浅色→深色，图标 Monitor/Sun/Moon，aria-label 注明当前态）；偏好持久化于 localStorage（`buyer-show.theme`），跟随系统时响应系统主题变化。

---

## 六、代办功能

### 已完成

- [x] 2.1 显示用户发布的帖子列表（`/profile/posts`，含编辑/删除入口）
- [x] 2.2 显示用户发布的评论列表（`/profile/comments`）
- [x] 3.1 显示用户收藏的帖子列表（`/profile/favorites`）
- [x] 3.2 显示用户收藏的评论列表（`/profile/favorites` Tab = 评论，`favorite_comments` 表）
- [x] 3.4 显示用户收藏的标签列表（`/profile/favorites` Tab = 标签，`favorite_tags` 表）
- [x] 4.1 显示用户评论的帖子列表（`/profile/comments`，按帖聚合 Tab）
- [x] 4.2 显示用户评论的评论列表（`/profile/comments` Tab = 按时间，平铺列表）
- [x] 1.2 用户资料编辑 + 头像（`/profile/edit`，头像 Storage 上线后切链接）
- [x] 1.4 用户注销（软注销：帖子置 deleted + 昵称匿名占位 + 登出）
- [x] 1.6 昵称唯一性校验（前端实时去重 + DB 唯一索引兜底，见 `migration-v3-account.sql`）
- [x] 5.1 profiles.role 字段 + `profiles_admin_all` 策略（`migration-v5-admin.sql`）
- [x] 5.2 用户管理（`/admin/users`：列表/搜索/封禁/解封/提权；封禁自动隐藏 active 帖子）
- [x] 5.3 标签管理（`/admin/tags`：新建/重命名/合并/删除，`tags` 表 + 4 RPC；`migration-v5.3-tags.sql`）
- [x] 5.4 独立 admin 布局（ConditionalNav 按路径隐藏 TopNav/TabBar；AdminLayout 带侧栏与权限守卫）
- [x] 5.5 解封恢复帖子 bug 修复（`migration-v8-fix-unban-restore-posts.sql`：`posts.original_status` 暂存列 + `ban_user`/`unban_user` 重写为对称操作）
- [x] 6.1 去 RPC 化（v13–v20 未部署 RPC 全量回退 PostgREST 直查，消除线上 `function not found` 报错）

> 以上条目为旧版 Next.js/Supabase 应用的历史完成记录（实现位于 `buyer-show-backup/`），当前前后端分离版按下列 P0–P3 迁移。

### 待办

> 编号约定：数字 = 旧版功能迁移项；`P` = 架构补齐与风险治理；`U` = UI 体验与页面框架；`7.x` = 消息中心/管理后台等模块。**未完成项集中在下方 Open 区**；已完成项收进文末归档（可追溯）。

#### 一、待排期（Open）

**B. 内容与体验增强**

- [x] E 组前体验优化批次（2026-09-21）：登录/注册密码可见性切换（Eye/EyeOff 切换 + 44px 热区 + aria-label 双向同步）；移动端主题切换入口（「我的」页导航三态循环：跟随系统/浅色/深色，补齐 P3.2 暗色模式移动端缺口）；图片全屏预览双击缩放（双击放大 2x/复位，cursor zoom-in/out 联动，draggable=false）——均为零外部依赖体验补全，不阻塞 E 组（短信/邮件验证码登录、第三方图片审核）落地
- [ ] P1.6-补充 设备/行为识别（P3，待业务驱动）：图形验证码与登录限流已落地

**C. 功能建设**

- [x] 7.4c WebSocket 推送与系统公告：G11 已落地（STOMP 实时推送 + 系统公告，轻量实时化 v1 在线状态 + 5s 轮询保留为降级兜底）
- [ ] 1.3 忘记密码（中）：依赖邮件/短信服务接入（E 组外部依赖后启动）；登录态「密码修改」已实现
- [ ] P1.3-补充 CDN 分发（低）：缩略图已生成；CDN 加速待接入

**D. 评论迭代（待排期）**

（✅ 5.6-5.10 已全部完成，见归档）

**E. 外部依赖（服务接入后启动）**

- 短信 / 邮件验证码登录
- 第三方图片审核

**F. 前端架构与 UI 演进（2026-09-18 三维分析：应用架构 / UI / 前端框架）**

架构（P4）：

- [x] P4.1-P4.6 全部完成（见归档）

UI（U37-U39）：

- [x] U37-U39 全部完成（见归档）

**G. 社区生态补全（2026-09-20 竞品对标分析：小红书/什么值得买/得物）**

> 对标结论：当前核心缺口为内容形态（单图无视频）、内容发现（无关注流/无推荐）、电商闭环（无购买链路）、创作激励（无数据反馈）。以下按分期登记，实施时复用既有基建（游标分页/通知/ES 容器/JSON 列/审计）。

**P0（社区留存与内容形态，高价值低成本）**：

- [x] G1 关注流 Feed：新增 `GET /posts?scope=following`（复用 follows 表 + 游标分页）；Feed 排序档加「关注」（最新/热门/关注三档）；未登录点击登录引导；空态「关注一些感兴趣的买家吧」——当前关注仅有通知价值无内容消费场景，闭环断裂
- [x] G2 多图发布（9 图）：posts.images 已是 JSON 列，后端补 `@Size(max=9)` 上限校验（发布/编辑共用 CreatePostRequest，恶意传 10 张被拒 code 6001）；上传区九宫格（首图封面标记/删除/多选/上限）与发布页多图上传链路（逐张上传 + 保序组装 + 失败回滚删除）均已具备；拖拽排序为后续可选增强
- [x] G3 详情页相关推荐（「猜你喜欢」区块）：v1 已实现 `GET /posts/{id}/related?limit=6`；同标签最新公开帖优先召回，不足时补最新公开帖（排除当前帖与重复项，limit 默认 6、最大 12）；详情页底部 2/3 列卡片区，点击复用详情浮层；v2 热度加权待后续推进
- [x] G4 帖子 @提及 + #话题#：正文解析 `@昵称`（2-20 字符，负向 lookbehind 防邮箱误匹配）与 `#话题#`（1-20 字符）；@提及上限 10、话题自动并入标签（手选在前去重，总上限 8，审核按合并后标签）；详情页正文高亮（@跳用户主页、#跳 `/?tag=` 话题 Feed）、标签 chips 点击直达话题 Feed；@提及生成 mention 通知（复用 notifications 新增 type，@自己过滤）；Feed 支持 `/?tag=` 话题直达（可分享链接，冷门话题自动追加进标签栏）
- [x] G5 搜索增强（ES 接入）：`posts` 索引（标题/正文/标签/商品名，自定义 ngram 中文分词支持子串匹配）+ 发帖/编辑/删除/审核/封禁等写点事务提交后异步同步（AFTER_COMMIT 事件防竞态）；搜索相关性排序（同分新帖优先）+ 标题/摘要高亮片段（`<em>` 标记，前端拆分渲染防注入）；ES 不可用自动降级 MySQL LIKE（降级同样带高亮）；「你是不是想找」纠错为后续
- [x] G6 拉黑/屏蔽用户：`user_blocks` 表（blocker/blocked 唯一键）；内容互不可见（Feed/关注流/热门/搜索/相关推荐/主页帖子/收藏/赞过/详情/评论/粉丝/关注列表/ES 回查共 13 处公开查询统一双向 NOT EXISTS 过滤）+ 不可私信（发起/发送均校验，历史保留）+ 自动双向取关（计数同步）；主页（拉黑/解除+拉黑管理 Tab）、评论（拉黑作者即时移除其全部评论）、会话内（头部拉黑）三处入口——私信防骚扰规则（关注后可发起）的配套治理

**P1（生态与运营，中期）**：

- [x] G7 收藏夹分类：`favorite_folders` 表（同用户重名唯一）+ favorites.folder_id（NULL=默认收藏夹不落库）；详情页未收藏时点击弹收藏夹选择器（选夹/新建，默认夹兜底），已收藏点击直接取消；本人主页收藏 Tab 夹 chips（全部/默认收藏夹/各夹+收藏数）+「管理」弹窗（新建/内联重命名/删除，删除后夹内收藏移回默认夹）；移动收藏（详情页选夹/跨夹移动）
- [x] G8 评论盖楼（二级嵌套）：`comments.reply_to_id` 指向被回复评论，树保持两级（顶级评论+扁平楼内回复）；回复任意评论自动推导楼根（越楼/回复已删除拒绝）；楼内回复展示「回复 @昵称」高亮前缀 + 被回复人收到 `reply` 通知（帖子作者不重复收）；楼中楼默认展示前 3 条、超出折叠「展开 N 条回复」；所有层级均可点击「回复」
- [x] G9 创作者中心（创作数据反馈）：个人主页「创作数据」页（仅本人）——帖子数/阅读量（近 N 天埋点）/获赞/被收藏/评论（累计）概览卡片，近 7/30 天互动趋势柱状图（阅读/点赞/收藏/评论指标切换、逐日补零），Top5 热门帖（近 N 天浏览，点击跳详情）；复用 analytics_events 埋点基建，无新表；入口在个人主页「编辑资料」旁
- [x] G10 运营位与话题运营：V16 `site_banners`/`topics` 表 + `posts.is_featured` 精选标记；首页 Banner 横滑（post/topic/url 三跳转、仅「最新+全部」档展示）、Feed「精选」档（运营打标优质内容流）、话题广场 + 话题聚合页（同名标签复用 Feed 查询）；管理后台「运营管理」三 Tab（Banner CRUD、精选打标/取消、话题 CRUD）；重名 Banner/话题拒绝（8002/8004）、仅审核通过帖可打标（8005）、停用话题即下架话题页
- [x] G11 WebSocket 实时推送（与 Open 7.4c 联动）：通知/私信从轮询升级 STOMP 推送（RabbitMQ fanout 事件总线 + `/ws` 握手鉴权）+ 系统公告（V17 `announcements`，公开 latest + 广播 + 运营后台管理）；轮询保留为降级兜底

**P2（增长与商业，高价值高成本/依赖外部）**：

- [x] G12 电商闭环（种草→拔草）：商品卡可选填购买链接（淘宝/天猫/京东/拼多多白名单域名校验防钓鱼，V18 `posts.product_link`）+ 详情页「去购买 ↗」跳转（新窗口）；非法/伪装链接发布与编辑均拒绝（8008）；v2 接 CPS 联盟返利
- [x] G13 个性化推荐：v1 标签偏好重排（无 ML 依赖）——Feed「推荐」档登录后按近 90 天发帖/收藏/点赞偏好标签重排（命中 +50 加成叠加热度分，冷启动回退热门，排除自己）；v2 独立推荐服务
- [x] 个人中心补全（模块状态表收尾）：前端个人中心页面已齐备（资料/五 Tab/创作数据/收藏夹管理），本次补全唯一缺口「账号注销」——`DELETE /users/me` 软注销（软删全部帖子 + 置注销状态 + ES 异步清理），前端 destructive 入口 + 二次确认，注销后登录被拒（1006）、他人查资料 2001；模块状态表「关注粉丝/标签管理」「个人中心」过时行同步更新
- [ ] G14 第三方登录（与 E 组联动）：微信扫码/公众号 OAuth；短信验证码登录（依赖短信服务）
- [ ] G15 视频内容：短视频（≤60s）上传 + 首帧封面 + 视频详情；MinIO 直传 + 转码（FFmpeg 或云服务）；建议 DAU 破千后启动

**P3（远期，暂缓）**：

- [ ] G16 创作者等级/勋章体系、签到/金币任务（什么值得买范式）
- [ ] G17 会员订阅、广告位系统
- [ ] G18 移动端 App/小程序（uniapp X，与既有待办联动）
- [ ] G19 好价爆料频道（价格日历、比价；运营重）

#### 二、已完成归档（截至 2026-09-17）

**架构补齐与风险治理（P0-P3，残留子项见上方 Open 区）**

- [x] P0.1 最小可用闭环（登录注册/Feed/详情/发布/评论/点赞收藏全部切换真实 API）
- [x] P0.2 Flyway V1-V5 部署验证（独立 MySQL 8.0 顺序执行通过）
- [x] P0.3 内容安全基线（敏感词 + 待人工审核 + 举报 + 审核台 + 并发状态迁移）
- [x] P1.1 前端路由（深链接 / 刷新恢复 / 路由过渡动画）
- [x] P1.2 前端数据访问层（services 领域拆分 + 统一错误处理 + JWT 自动刷新 + 用户资料 Redis 缓存）
- [x] P1.3 图片上传（MinIO pending→published + 解码重编码 + 缩略图生成 + 桶策略）
- [x] P1.4 站内互动通知（点赞 / 评论 / 关注）
- [x] P1.7 内容可发现性（meta / Sitemap / 结构化数据）
- [x] P1.8 生产配置加固（去硬编码密钥 + 关闭生产 debug）
- [x] P2.2 数据分析（前端埋点 + 事件收集 API + 管理看板）
- [x] P3.1 图片全屏预览与分享
- [x] P3.2 暗色模式 + PWA 离线
- [x] P3.3 Sentry 错误上报 + web-vitals 性能采集

**UI 体验优化（U1-U12，2026-09-14 审计）**

- [x] U1 暗色模式页面适配（token 变量化 + 全站 `bg-card/bg-background`）
- [x] U2-U7 逐页修复：Feed（键盘可达 / 空态 / 触底加载）、详情（骨架屏 / 404 出口 / 轮播键盘）、发布（objectURL 生命周期 / aria）、登录（form 提交 / role=alert）、消息（flex 高度 / aria）、审核台（二次确认 / 防连点）
- [x] U8 全局滚动与 smartBack 回退
- [x] U10 iOS 安全区（`safe-bottom` + viewport-fit）
- [x] U11 触控与无障碍（44px 热区 + aria-label/alt/focus-visible）
- [x] U12 低危打磨（瀑布流列数规范、审核台重试与骨架、死代码清理、页面拆分）

**旧版功能迁移（新架构）**

- [x] 1.1 用户名/手机号/邮箱 + 密码登录注册（多标识登录 + 唯一校验；验证码登录见 Open E）
- [x] 3.3 收藏用户列表（并入「关注与粉丝」）
- [x] 4.1 返回按钮统一（smartBack + TopNav 覆盖率）
- [x] 4.2 头像 ≤500KB（客户端 canvas 压缩）
- [x] 4.3 封禁页（BannedScreen + 兜底检查，被封用户全功能拦截）
- [x] 5.1 / 5.2 评论增强（删除 + 一级嵌套回复 + 桌面端输入）
- [x] 5.3 我的评论列表（旧版记录；新架构迁移见 7.1d）
- [x] 5.4 / 5.5 评论设计建议与嵌套显示（树组装）
- [x] 6.2 个人主页（他人 /user/:id + 我的 /profile）
- [x] 6.3 编辑帖子（/posts/:id/edit，重新过审）
- [x] 6.4 个人主页帖子管理（状态徽标 + 编辑/删除）
- [x] 7.1a 资料编辑（头像 / 昵称唯一校验 / 简介）
- [x] 7.1b 主页 Tab 分享/收藏/赞过
- [x] 7.1c 关注与粉丝列表
- [x] 7.2a 管理后台独立布局（AdminLayout + AdminRoute）
- [x] 7.2b 用户管理（搜索/筛选 + 封禁解封 + 帖子管理 v2：分页/搜索/预览）
- [x] 7.2e 举报中心拆分（目标摘要 + 采纳下架二次确认）
- [x] 7.2f 数据看板增强（待办卡 + KPI + 趋势 + TOP 榜）
- [x] 7.3 帖子申诉闭环（下架禁编辑 → 申诉 → 管理员审批 → 系统通知）
- [x] 7.4a 审批结果系统通知（申诉 / 人工审核结果，type=system）

**页面框架与导航（2026-09-16 迭代）**

- [x] U13 框架宽度全站统一（Feed 导航与主容器 5xl + 瀑布流 3/2 列档位）
- [x] U14 全站首页入口（8 个 Stack 页导航「首页」按钮，组内紧凑 + 负偏移对齐）
- [x] U17 滚动位置恢复（PUSH 回顶 / POP（详情→Feed 等）还原离开前位置，含异步加载短轮询重试）
- [x] U16 移动端 TabBar 全局化（AppTabBar 共享组件：Stack 页常驻、详情/发布/登录/管理后台例外、底部占位安全间距、同 Tab 点击回顶；Feed 搜索 Tab 联动自动聚焦）
- [x] U18 PC 全局 Header（DesktopHeader：Stack 页常驻 logo/首页/消息/搜索/发布/主题/用户区；Stack 导航 `md:top-14` 吸顶适配；详情页沉浸例外）
- [x] U19 手势与键盘收尾（全局 Esc 返回（弹窗/查看器自动豁免）、同 Tab 点击回顶；系统手势依赖原生 History 行为）
- [x] U9 图片性能（Feed 卡片与主页网格改 `<img loading="lazy">` + `alt` + 失败兜底渐变）
- [x] U20 Feed 下拉刷新（移动端触屏手势：下拉指示 → 松开刷新，阈值 56px）
- [x] 修复：页面过渡动画改纯透明度，全局 TabBar/Header 移出动画容器（消除 transform 对 fixed/sticky 定位基准的影响，长页面下 TabBar 零漂移）
- [x] 导航去重与规格统一（PC Stack 页隐藏 ⌂ 首页按钮由 Header 承担；Feed 导航 `h-14` 对齐 + 「首页/消息」频道按钮组 + logo 规格统一）
- [x] U21 去除 PC 二级导航条（profile/user/粉丝关注/编辑资料/消息/通知 `md:hidden`；编辑资料「保存」迁至表单底部；通知页补 PC 头部含「全部已读」；消息页补 PC 聊天头部）
- [x] U22 个人主页 Tab 滚动吸顶（`sticky top-14`，滚动后钉于 Header 下方，资料卡随滚动移出）
- [x] 三页导航菜单一致性（首页/消息/个人主页同构：图标化频道按钮、头像+昵称+退出用户区、发布最右、logo 可点击；登出确认在 Header 复用 Feed 同款弹窗）
- [x] 消息页框架统一修复（内容容器补 `w-full`+`px-4`，消除 flex-col shrink-to-fit 导致 max-w-5xl 失效；双栏/提示条与首页内容边界 0px 对齐；清理 1px 纵向溢出）
- [x] U25 导航按钮位置调整（发布前移至身份区左侧、头像为最右锚点；UserMenu 头像下拉菜单（我的主页/编辑资料/退出二次确认）；未登录登录位最右；Feed 与 Header 同步）
- [x] 全局导航登录态修复（常驻 Header 随路由重新同步登录态，登出后重置）

**待办推进（2026-09-16 第二批）**

- [x] 7.4d 通知 follow 类型点击跳用户主页（点击关注通知 → `/user/{actorId}`）
- [x] 7.5 详情页分享与购买入口：互动栏「分享」（系统分享优先，回退复制链接）；商品卡「复制商品信息」「复制链接」
- [x] 7.2e-补充 举报处置联动：确认弹窗新增「同时封禁作者账号」复选（后端 `banAuthor`；管理员/自己等不可封禁场景静默跳过）；**附带修复驳回动作（前端 REJECT→DISMISS，此前驳回必 400）**
- [x] 1.3（部分）密码修改：登录态改密（旧密码校验 + 新旧不同校验；头像菜单「修改密码」入口 + `/profile/change-password` 页；`/auth/**` 白名单精确化）——忘记密码依赖邮件服务保留 Open
- [x] Esc 菜单豁免修复（下拉菜单打开时 Esc 仅关菜单，不再触发返回）

**待办推进（2026-09-16 第三批）**

- [x] 7.1d 我的评论 `/profile/comments`：列表（内容 + 所属帖子跳转 + 审核中/未通过徽标 + 删除二次确认 + 触底加载）；原头像菜单「我的评论」入口（已迁至个人主页「评论」Tab，见第十六批）；后端 `GET /users/me/comments`（游标分页，仅本人可见）
- [x] 7.6 发布草稿自动保存：localStorage 防抖暂存（600ms）→ 进入发布页提示「恢复/丢弃」→ 发布成功清除；图片不随草稿（恢复后需重新上传）

**待办推进（2026-09-16 第四批）**

- [x] 5.6 评论点赞：`comment_likes` 表（V8 迁移，唯一键防重）+ 幂等切换接口（`POST /comments/{id}/like`）+ 列表 per-viewer `isLiked` 子查询 + 前端点赞按钮（计数/高亮，树形局部更新）
- [x] 5.9 评论排序：「最新 / 最热」头部切换（hot 按 `like_count DESC` 独立查询；切换即时重拉）

**待办推进（2026-09-16 第五批，评论迭代收尾）**

- [x] 5.7 评论收藏：`POST /comments/{id}/favorite` 幂等切换 + 列表 per-viewer `isFavorited` 子查询；「我的评论」页双 Tab（我的评论 / 收藏的评论，含取消收藏）+ 后端 `GET /users/me/favorite-comments`（fc.id 游标）
- [x] 5.10 评论编辑：发布后 5 分钟内作者可编辑（后端强制 4005 超时码 + 重新过审）；行内编辑 UI + 「已编辑」标记（V9 迁移 `edited_at` 专用列）
- [x] 5.8 评论分页：顶层评论游标分页（ID 游标，仅最新排序）+ 前端「查看更多评论」（首屏 20 条，加载追加）

**修复（2026-09-16，通知感知闭环）**

- [x] 驳回通知感知链路：后端通知本已正常（实测驳回即入库），前端感知断裂修复——① 三个消息入口接入未读红点（Feed 导航 / 全局 Header / 底部 TabBar；`useUnreadCount`：路由刷新 + 30s 轮询 + 已读事件广播）② 「消息」页「互动通知」Tab 接真实数据（替换静态 mock；真实列表/未读徽标/全部已读/点击跳转）③ 「全部已读」后全站徽标**即时**同步（事件广播，实测 ≤46ms）

**待办推进（2026-09-16 第六批）**

- [x] P1.5 搜索能力真实化：搜索面板接入真实 API（热门标签 / 联想防抖 300ms / localStorage 历史去重）；新增搜索页 `/search`（历史+热门推荐、结果列表、空态/骨架/错误三态、直链 `?q=` 自动搜索）；全站搜索入口打通（Feed 面板点击 / Header 跳首页聚焦 / 搜索页直链）；个性化推荐列为后续探索
- [x] U24 关闭（不再必要）：Header 搜索经「跳首页自动聚焦 + 下拉面板」已满足任意页发起搜索的完整链路

**待办推进（2026-09-16 第七批）**

- [x] 7.2d 审计日志：`admin_audit_log` 表（V10 迁移）+ 8 类管理员操作自动记录（审核帖子/评论、封禁/解封用户、封禁/解封帖子、举报处置（含联动封禁标注）、申诉处理；含操作说明与原因）；管理后台「审计日志」页（只读，动作分组筛选 + 页码分页，侧栏入口启用）；`GET /admin/audit-logs`（ADMIN 专属：Security 403 + 服务层双保险）

**待办推进（2026-09-16 第八批）**

- [x] 7.2c 标签管理：`GET /admin/tags` 聚合统计（基于 posts.tags JSON 列，JSON_TABLE 按使用量倒序 + 关键词过滤）+ 重命名（替换）/合并（替换 + 同帖保序去重）/删除（全帖移除）；管理页「标签管理」（搜索防抖 300ms、影响面预览弹窗、删除二次确认，侧栏启用）；操作记入审计日志（RENAME_TAG/MERGE_TAG/DELETE_TAG，审计页新增「标签管理」筛选组）

**待办推进（2026-09-16 第九批）**

- [x] 7.4b 私信功能 MVP：conversations/messages 模型（V11 迁移，user_a<b 唯一对 + 双方未读计数）+ 5 个 API（会话列表/发起/消息分页与增量/发送/已读）；隐私守卫（对方关注我后可发起）；发送侧敏感词拦截；消息页私信 Tab 真实化（会话列表/聊天气泡/发送/20s 轮询增量/进入即已读/`?c=` 直达）；他人主页「私信」发起入口

**待办推进（2026-09-16 第十批）**

- [x] 埋点性能上报修复：补 `POST /api/v1/analytics/performance` 端点（web-vitals 指标落库 `analytics_events.metadata`，未登录可上报→Security 放行），消除前端上报 500 噪音
- [x] P2.1 落地（API 冒烟）：新增 `scripts/smoke-test.py` 可复用冒烟脚本（覆盖核心链路+权限边界+验证码+在线状态，23 用例，退出码可接 CI）；浏览器级 E2E（Playwright）转为待业务驱动

**待办推进（2026-09-17 第十一批）**

- [x] U23 详情浮层：列表点击（Feed/搜索/主页）以小红书式浮层打开详情（暗化遮罩 + max-w-2xl 卡片，关闭回列表并恢复滚动位置）；关闭三径（返回按钮/Esc/点遮罩）；直链与刷新仍为整页（`location.state.modal` 双模式）；加载/错误态浮层内一致渲染

**待办推进（2026-09-17 第十二批）**

- [x] P1.6 图形验证码：注册必验（SVG 4 位、去混淆字符、Redis 5 分钟有效、一次性消费）；登录连续失败 3 次后要求验证码（1009），验证通过重置计数（1001→1009→1010→放行），超 5 次无验证码硬限流 15 分钟；`GET /auth/captcha` 公开端点；前端注册/登录页接入（点击刷新、错误自动换图）

**待办推进（2026-09-17 第十三批）**

- [x] 7.4c 轻量实时化 v1：在线状态（认证请求 touch Redis 60s TTL + 会话列表绿点/聊天头「在线」文案）；聊天增量轮询 20s→5s 且每轮同步会话列表（对方在线/最后消息/未读）；未读通知轮询 30s→15s；页面隐藏时全部暂停、恢复可见即补拉

**待办推进（2026-09-17 第十四批）**

- [x] U26【P0】标签体系真实化：新增公开 `GET /api/v1/tags`（TagService 基于 JSON_TABLE 聚合，公开帖口径）；Feed 标签栏与发布页标签选择器接入真实数据（纯文字、无 emoji 与假计数），移除 mockTags 依赖；修复统计口径（不再计入已下架帖，与 Feed 筛选结果一致）
- [x] U27【P0】他人主页移动端资料卡：外层 flex-wrap + 操作区 `max-md:w-full`（关注/私信换行独占一行）；信息列不再被压至 29px（统计按钮 14px 竖排 → 41px 横排，昵称单行 28px）
- [x] U28【P1】发布页表单可访问性：星级评分 radiogroup/radio + aria-label（N 星）+ 36×44 热区 + 「未评分」提示 + 点同分清零；标题/正文 label htmlFor 关联修复（点击 label 正确聚焦）+ 商品名/价格 aria-label；草稿提示条 amber 半透明 token 化（暗色适配）
- [x] U29【P1】Feed 排序钮补 aria-label；搜索页 PC 隐藏第二行导航（md:hidden）+ PC 搜索入口移至内容区（非吸顶）
- [x] U30【P2】触控达标：TabBar 按钮 44（全站两处 + Feed 自带底栏）/Feed logo 44/发布 CTA 40/标签 chips 36/排序钮 36/资料 Tab 42/关注·私信 40/消息 tabs 44/搜索框 40
- [x] U31【P3】一致性：Feed/搜索页补 sr-only h1；新增 `coral-contrast` token（#9A3412，浅珊瑚底文字对比 ≥4.5:1）并替换 13 文件 19 处；Feed 底栏 z-50 对齐层叠约定；评分默认 0 星可清空；EmptyState 公共组件落地（components/ui/empty-state.tsx，已接入 Feed/搜索/消息/通知 4 处空态）

**待办推进（2026-09-17 第十五批：前端一致性体系 G 组 U32-U36）**

- [x] U32 Token 契约化 + 防漂移：`frontend/DESIGN_THEME.md` 契约（对抵 UI 规范 v1.3）；`npm run check:ui` 静态检查（裸色值 error / emoji·z-index 越界·任意像素 warn；当前 0 error）；基线复核（裸色值存量 0）
- [x] U33/U34 组件原语收敛与三态补齐：新增 `ConfirmDialog`（破坏性二次确认，autoFocus 在取消）、`AppDialog`（基于 Base UI Dialog 封装：Esc/遮罩/焦点/aria 内置）、`LoadingState`、`ErrorState`（支持自定义 action）；替换 11 处自撸弹窗（登出×2/删除帖/申诉×2/离开确认/删除评论/采纳举报/标签操作/申诉处理/封禁确认）+ 9 处手写错误块 + 3 处加载块；`check:ui` 自撸弹窗从 14 降至 4（剩余 3 处 U23 页面级浮层 + 1 处复合预览弹窗，已登记豁免）
- [x] U35 导航单一配置源：`lib/navigation.ts`（CHANNELS/TABBAR_ORDER/PC_CHANNEL_ORDER/isChannelActive）；DesktopHeader、AppTabBar、Feed 导航与 Feed 自带底栏四处接入；频道/菜单变更单点修改
- [x] U36 走查清单工具化：`前端一致性走查清单.md`（自动检查三项命令 + 六类人工走查 + 周例行 + 豁免记录表）；`check:ui` 作为提交前门禁

**待办推进（2026-09-17 第十六批）**

- [x] 「我的评论」入口迁移（截图需求）：从头像下拉菜单移除，迁至个人主页内容 Tab 体系；个人主页 Tab 变为 分享/收藏/赞过/**评论**（「评论」仅本人主页可见，他人主页保持 3 Tab——评论互动上下文不对外）；Tab 内嵌 `MyCommentsPanel`（内层切换「我的评论/收藏的评论」，懒加载、三态齐备）；下拉菜单收敛为 4 项（我的主页/编辑资料/修改密码/退出登录）；直链 `/profile/comments` 保留兼容（外壳 + 同面板）

**待办推进（2026-09-17 第十七批：导航位置分析驱动优化）**

- [x] 导航右操作区双层分组：工具组（主题/审核台｜组内 8px）与操作组（发布+身份区｜组内 8px）以 **16px** 分隔——主 CTA 组归属明确（分析结论：顺序合理不动，仅优化分组表现）；同步修复两导航右区间距不一致（Feed 原 16px 平铺 vs Header 8px）与发布按钮高度不一（h-9/h-10 统一 40px）；实测两导航坐标 byte 级一致，发布紧邻身份区保持 8px；搜索真居中等 2 项判定成本/收益后维持现状（已记录于分析报告）

**待办推进（2026-09-18 第十八批：P4.1 服务端状态层）**

- [x] P4.1 TanStack Query 接入：① Provider 接入 main.tsx（staleTime 默认 30s + DevTools 开发态）② `hooks/use-me.ts` 当前用户单一缓存（`['me']` 单飞共享：Header/UserMenu/Feed/个人主页同一事实源；登录/编辑资料 `invalidateQueries` 全站即时同步，消除重复拉取与头部不同步）③ Feed 列表 `useInfiniteQuery`（标签+排序独立缓存秒开，`keepPreviousData` 防闪烁，旧请求 AbortSignal 自动取消替代 requestVersion 守卫，下拉刷新重拉首页后裁剪旧页）④ 详情页互动精准局部更新（`patchFeedPost` 赞/藏/评论计数与状态 patch 单卡片，免整列表重拉）；验证 tsc + check:ui(0 error) + 40/40 测试 + 构建 117KB gzip 通过

**待办推进（2026-09-18 第十九批：P4.3 大文件拆分）**

- [x] P4.3 大文件拆分：5 个超 500 行红线的屏幕文件全部拆至红线内——detail.tsx 910→489（image-carousel/post-content/comment-item/detail-states/action-bar/appeal-dialog）、feed.tsx 690→425（waterfall/post-card/search-panel）、users.tsx 584→409（post-preview-dialog/user-posts-panel）、publish.tsx 542→459（image-grid/publish-states）、messages.tsx 501→387（chat-views/notification-views）；模式：纯函数/无状态子组件抽同目录模块，主组件保留状态与编排，受控 props 传递；验证 tsc + check:ui(0 error) + 40/40 测试 + 构建通过

**待办推进（2026-09-18 第二十批：P4.2 Zustand 跨页 UI 状态）**

- [x] P4.2 全局会话 store：落地 `stores/ui-store.ts`（Zustand）承载跨页 UI 状态——①未读通知数：原先 feed/desktop-header/app-tabbar 三入口各自 15s 轮询 + window 事件广播同步，改为 App 根 `UnreadCountPoller` 全局单一轮询写入 store，各入口徽标订阅同一事实源，消息页「全部已读」直接置零全站同步（删除 NOTIFICATIONS_UPDATED_EVENT 事件 hack）②主题偏好：状态与 localStorage 持久化入 store，多入口共享同一主题 state（修复切换后另一入口图标不同步）；currentUser 单一事实源部分已由 P4.1 `useMe`（`['me']` Query 缓存）覆盖，不再重复建设；验证 tsc + check:ui(0 error) + 40/40 测试 + 构建 + 浏览器冒烟（14 篇帖子渲染/主题切换/未读徽标）通过

**待办推进（2026-09-18 第二十一批：P4.4 核心流程测试补强）**

- [x] P4.4 核心流程测试补强：40 → 99 用例（13 文件）——①services 层 4 新文件：comments（12，encodeCursor/getComments 参数组合/createComment 回复/删改/点赞收藏）、users（10，profile 数据面含游标编码）、uploads（6，validateImageFile 三拒绝路径/uploadImage FormData/delete objectName 编码）、tags（2）②纯逻辑：waterfall.distributePosts（6，最短列优先基于确定性 hash 高度精确断言）+ cardImageClass 确定性③组件 3 新文件：image-grid（6，封面标记/删除回调/上限隐藏入口/file change）、action-bar（9，发送禁用态/Enter 提交/回复态/点赞收藏 aria 切换）、post-content（8，商品卡条件渲染/复制 toast/buildPostJsonLd SEO 结构）；profile 页面级测试留待 E2E（数据面由 users 服务测试覆盖）；验证 tsc + check:ui(0 error) + 99/99 测试通过

**待办推进（2026-09-18 第二十二批：P4.5 bundle 体积门禁）**

- [x] P4.5 bundle 体积门禁：`npm run size:check`（构建后门禁：index.html 静态引用 JS + feed 首屏 chunk 的 gzip 合计，阈值 150KB，当前 143.84 达标；CSS 单列参考不计入门禁，与「首屏 JS gzip < 150KB」架构目标口径一致）+ `npm run build:report`（rollup-plugin-visualizer 条件启用 --mode report，生成 dist/stats.html）；顺带 Sentry 初始化改为 DSN 懒加载（error-monitoring.ts 动态 import：无 DSN 环境零成本为显式契约，未来配置 DSN 时首屏不加载 SDK）

**待办推进（2026-09-18 第二十三批：P4.6 ScrollRestoration 评估）**

- [x] P4.6 ScrollRestoration 自制轮询评估：结论维持现状——60ms×20 次（≤1.2s）轮询仅作「等数据渲染」兜底，Query 缓存命中（staleTime 30s + keepPreviousData）时首次 rAF 即恢复到位、零空转；替代方案对比：ResizeObserver 事件驱动（观察目标/稳定判定复杂度高）、react-router data router 迁移（影响面大）收益均不抵风险；触发重做条件：慢网络下 POP 恢复失败率可测升高或轮询耗电成为可测问题时再升级

**待办推进（2026-09-18 第二十四批：UI 收尾 U37-U39）**

- [x] U37 destructive token 语义修复：`--destructive-foreground` 由与 `--destructive` 同色改为近白 oklch(0.985 0 0)（实底 destructive 按钮文字可读，light/dark 双模式）
- [x] U38 chart 品牌色板：chart-1..5 由灰阶改为品牌色系（chart-1 珊瑚橙 + 青绿/蓝/紫/暖黄，light/dark 各一档亮度），管理台图表接入时直接用
- [x] U39 依赖收敛：卸载 `cn`（^0.2.6），`lib/utils.ts` 本地封装 `cn = twMerge(clsx(...))`（依赖 clsx + tailwind-merge 两个官方维护包）；15 个 ui 组件 import 改指 `@/lib/utils`；验证 tsc + check:ui(0 error) + 99/99 测试 + 构建 + 体积门禁 143.84 通过

**待办推进（2026-09-18 第二十五批：P2.1 浏览器级 E2E 落地）**

- [x] P2.1-补充 浏览器级 E2E 落地：Playwright 基础设施全链路（`frontend/e2e/`）——①@playwright/test + chromium 依赖与 `test:e2e` 脚本 ②vite /api proxy（`API_PROXY_TARGET` 环境变量可覆盖，顺带修复 dev 模式直连 API 404 的既有缺陷）③playwright.config.ts（setup project 注册专用账号 + storageState 注入 token 共享登录态、workers 1 串行、webServer 自动拉起 5273 端口 dev 服务）④global-setup 注册独立测试账号（图形验证码明文经 `redis-cli` 从 Redis `captcha:{id}` 读取，非侵入绕过）⑤5 个 spec：auth（登出→重登）、feed（列表加载+详情浮层开关）、post-detail（浮层内发评论+点赞）、publish（发布→详情页→个人主页删除全旅程）、search（热门搜索+关键词检索）；过程中发现并修复 2 个真实缺陷：①CORS 白名单缺 5273 → 发布请求 403「Invalid CORS request」（application.yml 白名单补齐）②未评分发布传 rating=0 违反 DB 约束 `chk_post_rating` → 后端 `normalizeRating` 归一化 0/null→NULL（PostService 两处 + 单测）；验证 5/5 全绿（23.7s）

**待办推进（2026-09-18 第二十六批：1.5 用户表扩展字段落地）**

- [x] 1.5 用户表扩展字段落地：原 Supabase auth.users/profiles 双表冗余需求在当前单一 users 表中已消解（email/phone 已并入 users，旧版 profiles 字段全覆盖），本次落地其风控向扩展——users 表新增 `last_login_at`（V12 迁移），登录成功后更新（失败不影响登录主流程，AuthService try-catch + WARN），管理后台用户列表新增「最后登录」展示（从未登录显示「从未」）；顺带修复 vitest 误收集 `e2e/specs` 的问题（test.exclude 排除 e2e/，5 个 Playwright spec 不再干扰单元测试）；验证：后端全量用例通过（含新增 AuthService 更新/容错 2 用例 + AdminUserService listUsers 映射用例）+ 前端 99/99 + tsc/check:ui(0 error) + V12 真库迁移与登录写入实测（last_login_at 落库）

**待办推进（2026-09-20 第二十七批：P1.2 业务缓存扩展落地）**

- [x] P1.2 业务缓存扩展落地：新增 `feed:anonymous` Spring Cache（60s TTL）——仅匿名用户 + 无游标 Feed 首屏命中（登录用户 Feed 含用户维度、翻页游标不稳定均不缓存），key 为 `tag|sort|limit`；13 个写点（发帖/删帖/编辑/审核/举报处置/用户与帖子封禁解封/申诉处置/标签改名合并删除）统一 `@CacheEvict(allEntries)` 主动失效 + 60s TTL 兜底（点赞/评论/收藏计数变化不失效，漂移由 TTL 兜底）；修复两个反序列化缺陷（`CursorPage`/`PostDTO` 仅 @Builder 无默认构造器，命中时抛 InvalidDefinitionException → 补 `@NoArgsConstructor`+`@AllArgsConstructor`），新增 `RedisCacheSerializerTest` 序列化往返回归（非空/空列表两用例）；真机验证：miss→hit 均 200（耗时 0.43s→0.028s）、登录态请求不写缓存、发帖/删帖后缓存键即时消失；验证：后端全量单测通过 + E2E 5/5 全绿 + 前端 99/99 + tsc/check:ui(0 error)

**待办推进（2026-09-20 第二十八批：G1 关注流 Feed 落地）**

- [x] G1 关注流 Feed 落地：后端 `GET /posts` 新增 `scope` 参数（默认 `all`，`following` 需登录否则 1003），关注流 = 当前用户关注对象的公开帖按发帖时间倒序（复用 follows 表 + 游标分页）；缓存条件追加 `scope=all` 白名单防串流（关注流不缓存，匿名 following 被拒也不写键）；前端 Feed 排序档由「最新/热门」扩为「最新/热门/关注」三档（关注档未登录点击 toast 引导登录），关注流空态「关注一些感兴趣的买家吧 + 去逛逛最新分享」；真机验证：关注/取消关注后流内容即时过滤（仅返回关注对象帖子）、游标分页 hasMore/nextCursor 正常、缓存键无污染；验证：后端全量单测通过（新增关注流 3 用例）+ 前端 100/100（posts service 新增 scope 用例）+ E2E 5/5 + tsc/check:ui(0 error)

**待办推进（2026-09-20 第二十九批：G2 多图发布落地）**

- [x] G2 多图发布落地：后端 `CreatePostRequest.images` 补 `@Size(max = 9)` 上限校验（发布/编辑共用同一 DTO，一条校验覆盖两条链路）；新增 `CreatePostRequestValidationTest`（1 张通过/9 张通过/10 张拒绝/0 张拒绝 4 用例）；真机验证：双图上传发帖成功且详情页图片保序返回，10 张发帖被拒（code 6001「最多9张图片」）；前端九宫格上传区与多图发布链路（逐张上传 + 保序组装 + 失败回滚）为既有能力无需改动；验证：后端全量单测通过 + E2E 5/5 全绿

### 评论功能设计建议（5.4）

**已实现（前后端分离版）**：
- 评论删除（作者可删）
- 一级嵌套回复（`parent_id` 外键，只允许回复顶级评论，不支持回复的回复）
- 评论审核状态隔离（待审/驳回不公开，计数按实际通过数量维护）
- 评论举报（随 P0.3 落地，`content_reports` 表 + 管理员审核台处置）

---

## 七、管理后台功能设计（2026-09-16，按 ui-ux-pro-max「Data-Dense Dashboard」准则）

### 7.1 设计原则与现状基线

**设计原则**

- **独立后台布局**：与前台（TabBar/Stack）彻底分离，桌面优先、数据密度高（KPI 卡 + 数据表 + 筛选栏）；移动端可用（侧栏折叠为抽屉）
- 沿用珊瑚主题与既有 Token（§5.1）；图表复用 `chart-1..5` 色板；统一 Lucide 图标
- 表格基线：行 hover 高亮、可排序列带 `aria-sort`、空态/骨架屏/错误重试、破坏性操作二次确认
- 权限：前端路由守卫 + 后端 `hasRole('ADMIN')` 双重校验；管理操作全部审计留痕

**现状基线（已实现）**

| 能力 | 现状 |
|------|------|
| 内容审核 | `/admin/moderation` 三 Tab：待审帖子 / 待审评论 / 待处理举报；驳回需理由确认、举报可采纳下架/驳回 |
| 数据看板 | `/admin/analytics`：今日活跃 / 近 7 天 / 近 30 天 KPI 卡（后端 `/analytics/overview|stats|top-posts|trend`） |
| 后端接口 | 6 个：审核队列 GET ×3 + 处置 POST ×3（`/api/v1/admin/**`） |
| 缺失 | 独立后台布局、用户管理、标签管理、审计日志、看板增强（时间范围/趋势/待办卡） |

### 7.2 信息架构（AdminLayout）

```
管理后台（/admin/*，仅 ADMIN；非管理员访问提示无权限并跳回首页）
├── 数据看板   /admin/analytics    核心指标、趋势、待办入口
├── 内容审核   /admin/moderation   待审帖子 / 待审评论（处置：通过 / 驳回）
├── 举报中心   /admin/reports      待处理举报（处置：采纳下架 / 驳回；可联动封禁作者）
├── 用户管理   /admin/users        列表 / 搜索 / 封禁 / 解封 / 角色（P1）
├── 标签管理   /admin/tags         标签统计 / 重命名 / 合并 / 删除（P2）
└── 审计日志   /admin/audit-log    管理操作留痕（P2）
顶栏：面包屑 + 「返回前台」+ 管理员昵称/头像
```

- 侧栏：桌面常驻（约 240px，当前项珊瑚色高亮）；`<1024px` 折叠为抽屉（汉堡按钮）
- 待办数量徽标：待审 N / 举报 M 在侧栏与页面标题旁同步展示

### 7.3 页面功能设计

#### A1 数据看板（P0 增强）

| 模块 | 功能 |
|------|------|
| 待办卡片 | 「待审内容 N」「待处理举报 M」→ 一键直达对应页 |
| KPI 卡片 | 保留今日/7 天/30 天活跃；新增：今日发帖、今日互动（赞+评+藏）、今日注册 |
| 时间范围 | 近 7 天 / 近 30 天切换，驱动趋势图与 TOP 榜 |
| 趋势图 | 活跃用户 / 发帖量折线（`/analytics/trend`），tooltip 显示精确值，空态「暂无数据」 |
| TOP 榜 | 热门帖子 TOP10（`/analytics/top-posts`）+ 热门标签 TOP10（聚合 `posts.tags`） |

#### A2 内容审核（P0 完善）

- 结构：两个 Tab（帖子 / 评论），举报处理移出为独立页 A3
- 字段：缩略图/内容摘要、作者、提交时间；评论 Tab 附「所属帖子」跳转
- 处置：通过（发布）/ 驳回（**必填理由** + 二次确认）；二级筛选「待处理 / 已处理」
- 批量（P1）：勾选批量通过（≤100 条）；驳回保持逐条

#### A3 举报中心（P1）

- 列表：类型（帖子/评论）、目标摘要（跳原内容）、举报人、理由、时间、状态
- 处置：采纳并下架内容 / 驳回举报；采纳时可勾选「**同时封禁作者**」（必填理由，封禁后其内容自动隐藏）
- 排序：待处理优先、时间倒序；筛选：待处理 / 已处理 / 全部

#### A4 用户管理（2026-09-16 已实现：列表/封禁/解封；角色管理待续）

- 表格：ID、头像+昵称、用户名、邮箱/手机（**默认脱敏** `139****1219`）、角色、状态、发帖数、注册时间、最近活跃
- 搜索（昵称/用户名/邮箱/手机，后端参数绑定）＋筛选（角色、状态）
- 行操作（全部二次确认）：
  | 操作 | 规则 |
  |------|------|
  | 封禁 | 必填理由；联动隐藏其帖子/评论；**不能封禁自己** |
  | 解封 | 恢复状态并恢复其内容可见 |
  | 设为/取消管理员 | **禁止降级最后一个管理员**；不能操作自己 |
- 已实现补充：展开行展示该用户帖子（含封禁/待审状态标识），支持单帖**封禁/解封**；封禁/解封用户与帖子的规则由后端 Service 层校验（防自己、防封管理员、状态机幂等）
- 行抽屉（P2）：基本信息 + 最近发帖/评论各 5 条

**A4 迭代设计 v2（2026-09-16 已实现）**：

*① 管理后台是否需要"首页瀑布流"？—— 不需要，明确区分消费与管理两种呈现*

- 首页瀑布流是**消费场景**（视觉优先、无序浏览、无状态信息）；管理场景需要**数据密度 + 状态可见 + 可筛选可操作**，两者目标冲突，瀑布流会隐藏"哪些被封禁/待审"这一管理核心信息
- 决策：管理后台**不复制瀑布流**；如需全站内容的视觉化浏览，提供"内容管理"视图（A7）：规整网格/列表 + 状态徽标 + 筛选 + 搜索 + 分页
- 管理员随时可回到前台首页查看真实消费效果（前台视图不变，保持游客/用户视角）

*② 用户帖子管理面板 v2（替换现有"展开行 + 加载更多"）*

| 维度 | 现版本 | v2 设计 |
|------|--------|---------|
| 分页 | 「加载更多」追加式，帖子多时无法翻页/定位 | **页码分页器**：总数 + 每页 20 + 上一页/下一页（管理需定位与总量感知，不再追加式） |
| 搜索 | 无 | 面板内搜索框：**按标题关键词**搜索该用户帖子（回车/按钮提交；提交后重置回第 1 页） |
| 筛选 | 无 | 状态筛选 chips：全部 / 公开中 / 已封禁 / 待审（与搜索组合生效） |
| 查看后再决定 | 行内只能盲封（看不到内容） | 每行「查看」→ **管理预览弹窗**：封面图 + 标题 + 正文摘要 + 商品信息（名称/价格/来源/评分）+ 状态 + 发布时间；弹窗底部操作：[封禁](destructive) / [解封] / [前台查看 ↗]（新标签打开真实详情）/ [关闭] |
| 行内快路径 | 封禁/解封按钮 | 保留（熟悉内容时快捷操作；破坏性操作仍有确认弹窗） |

- 交互细节：预览弹窗内的封禁/解封复用统一理由 + 二次确认流程；操作成功后弹窗状态即时刷新、列表行同步；切换展开用户时重置搜索/筛选/页码
- API 扩展：`GET /api/v1/admin/users/{id}/posts` 增加 `search`（标题模糊）与 `moderationStatus` 参数；新增 `GET /api/v1/admin/posts/{id}`（管理端帖子详情，供预览——前台详情接口对已封禁帖子返回 3001，管理员无法预览）

*③ 全站内容管理（A7，P2 设计保留）*

- 若需要"全站帖子维度"的管理入口（不限于单用户），复用同一套"状态优先呈现 + 筛选/搜索/分页/预览"模式，数据源为全量帖子（含所有状态），「内容管理」入口加入 AdminLayout 侧栏

#### A5 标签管理（P2，新）

- 数据源：`posts.tags` JSON 聚合（`tags` 表暂未承载数据）
- 列表：标签名、使用量（帖子数）、最近使用时间，按使用量排序
- 操作（全部先**影响面预览**「N 篇帖子将受影响」再确认）：重命名（批量替换）、合并（A→B）、删除（批量移除）
- 实现约束：分批 ≤500/批 事务更新；操作写审计；远期迁 `tags` 表 + `post_tags` 关联表

#### A6 审计日志（P2，新，需 `admin_audit_log` 表）

- 字段：操作人、动作（审核/举报/封禁/角色/标签）、目标类型+ID、理由、结果、时间、IP
- 只读表格 + 筛选（操作人 / 动作 / 时间范围）

#### A8 申诉处理（2026-09-16 已实现）

- 数据：`post_appeals` 表（V7 迁移）——一次申诉一条记录，保留历史；同一帖子待处理期间不可重复提交
- 用户侧：已下架帖子**不可编辑**（后端强制拦截 + 前端隐藏编辑入口，编辑页直达时预检跳回）；「我的主页」卡片与详情页提供「发起申诉」（必填理由 ≤500 字），提交后卡片显示「申诉中」
- 管理侧：`/admin/appeals` 队列（状态筛选：待处理/已通过/已驳回/全部，待处理优先）；**通过**=解封恢复公开（帖子状态回公开，审计记入 moderated_by/at）；**驳回**=维持下架并记录处理说明
- 计数：`我的帖子`列表与作者视角详情返回 `appealStatus`（0 待处理/1 通过/2 驳回）

### 7.4 后端 API 契约

```
现有（/api/v1/admin/**）
GET  /moderation/posts|comments        # 待审队列（状态过滤）
GET  /reports                          # 举报列表
POST /moderation/posts|comments/{id}   # 处置（通过 / 驳回+理由）
POST /reports/{id}                     # 处置举报（采纳下架 / 驳回）

复用
GET  /api/v1/analytics/overview|stats|top-posts|trend   # 看板数据（已有）

新增（随对应页面实现）
GET  /api/v1/admin/users                     # 用户列表（search/role/status/分页）
POST /api/v1/admin/users/{id}/ban|unban      # 封禁（理由）/ 解封
POST /api/v1/admin/users/{id}/role           # 角色设置（防最后管理员降级）
POST /api/v1/admin/reports/{id}              # 扩展参数：采纳时联动封禁作者
GET  /api/v1/admin/tags                      # 标签聚合统计
POST /api/v1/admin/tags/rename|merge|delete  # 批量操作（返回影响数）
GET  /api/v1/admin/audit-logs                # 审计日志
```

### 7.5 安全与风控规则

- 权限双层校验：SecurityConfig `hasRole('ADMIN')` + Service 层复核（规范：权限必须在业务层校验）
- 破坏性操作三件套：**理由必填 + 二次确认 + 审计留痕**（现有 moderated_by/at 机制扩展至 audit 表）
- 防误伤：不能封禁/降级自己；至少保留 1 个管理员；批量操作 ≤100 条/次
- 敏感数据：列表默认脱敏，查看完整手机/邮箱需显式操作并记录

### 7.6 迭代优先级

| 优先级 | 内容 | 依赖 |
|--------|------|------|
| P0 | AdminLayout 独立布局 + 审核台拆分（内容审核 / 举报中心）+ 看板增强 | 复用现有接口，前端为主 |
| P1 | 举报中心封禁联动 + 用户管理（列表/封禁/角色） | 新增用户管理接口组 |
| P2 | 标签管理 + 审计日志 + 用户抽屉 | 新增表与批量接口 |

---

## 八、消息中心设计（系统通知 + 私信，2026-09-16）

### 8.1 系统通知（审批 / 系统提示）——本期已实现

**通知类型矩阵**（`notifications.type`）：

| 类型 | 触发点 | 接收者 | 点击跳转 |
|------|--------|--------|---------|
| like | 帖子被点赞 | 楼主 | 帖子详情 |
| comment | 帖子被评论 | 楼主 | 帖子详情 |
| follow | 被关注 | 被关注者 | （预留：用户主页） |
| **system** | **审批与系统提示（本期新增）** | 内容作者 | 帖子详情（targetType=post） |

**system 通知触发点（本期已接入）**：
1. **申诉结果**：通过（「你的申诉已通过，「xxx」已恢复公开」）/ 驳回（含处理说明，提示可再次申诉）——**解决"帖子被静默释放 / 驳回无感知"问题**
2. **人工审核结果**：帖子审核通过 / 未通过（含原因 + 申诉引导）；评论审核通过 / 未通过

**展示规范**：通知中心（/notifications）中系统通知身份显示为「系统通知」+ ShieldCheck 珊瑚色图标；正文即通知内容；点击跳转关联帖子；未读计数复用现有红点机制。

### 8.2 私信功能设计（P2 规划）

**数据模型**：

| 表 | 关键字段 |
|----|---------|
| conversations | id、user_a_id、user_b_id（约定 a<b，唯一索引 `uk_pair`）、last_message_id、last_message_at、a_unread、b_unread、created_at |
| messages | id、conversation_id、sender_id、receiver_id、content、is_read、created_at（索引 `conversation_id + id`） |

**隐私规则（防骚扰）**：发起私信条件为**互相关注或对方已关注我**（陌生人不可发起）；预留拉黑与举报会话（复用举报体系，targetType=conversation）。

**API 契约**：

```
GET  /api/v1/conversations                # 会话列表（按 last_message_at 倒序，含未读数）
POST /api/v1/conversations                # 发起/复用会话 { targetUserId }
GET  /api/v1/conversations/{id}/messages  # 消息分页（游标）
POST /api/v1/conversations/{id}/messages  # 发送消息
POST /api/v1/conversations/{id}/read      # 标记已读
```

**页面交互（沿用 Flow 4 既有原型）**：/messages 双栏（PC）/ 单栏切换（移动）；左=会话列表（Tab：私信/通知），右=聊天窗口；新建会话入口＝主页「私信」按钮（Flow 5 已预留）与详情页作者区；消息气泡（收=白/发=珊瑚色）+ 时间分组 + 已读回执。

**实时方案分期**：P2 轮询增量（进入会话拉取 + 30s 轮询，MVP）→ P3 WebSocket（STOMP）实时推送 + 在线状态。

**与通知中心的关系**：通知（互动+系统）与私信分离、同页双 Tab；底部 TabBar「消息」红点＝未读通知 + 未读私信之和。

### 8.3 分期计划

| 阶段 | 内容 | 状态 |
|------|------|------|
| P1 | 系统通知：申诉/审核结果推送（type=system）+ 通知中心展示与跳转 | ✅ 已实现 |
| P2 | 私信 MVP：数据表 + 5 个 API + 会话列表/聊天页（轮询） | ✅ 已实现 |
| P3 | 实时化：WebSocket 推送、在线状态、账号级系统公告 | ✅ 已实现（G11） |

---

> 文档版本：v3.12 | 最后更新：2026-09-21 | 下一版由《技术方案文档》《架构设计文档》承接技术与架构细节